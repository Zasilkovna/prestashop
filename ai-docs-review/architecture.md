## ai-docs/architecture.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17 | prestashop is one PrestaShop module whose slices meet in the module class `Packetery` | CONFIRMED | `packetery/packetery.php:71` `public function __construct()` on `class Packetery extends CarrierModule`; every slice under `packetery/libs/` is resolved from its container. |
| 18 | "That class is the only object PrestaShop instantiates" | INCORRECT | PrestaShop's dispatcher also instantiates the module's controllers: `packetery/controllers/admin/PacketeryOrderGridController.php:29` `class PacketeryOrderGridController extends ModuleAdminController`, plus `PacketeryCarrierGridController`, `PacketeryLogGridController`, `PacketerySettingController`, `PacketeryCheckoutModuleFrontController` and `PacketeryCronModuleFrontController` — none of them is created by `Packetery`. |
| 18–20 | it builds the container and registers the hooks [VERIFY: packetery/libs/DI/ContainerFactory.php#register] | CONFIRMED | `packetery/packetery.php:81` `$this->diContainer = Packetery\DI\ContainerFactory::create($this);`; hooks are registered from `Installer::run` over `Packetery::getModuleHooksList()`. |
| 19–20 | "hands itself to every slice that needs a service" [VERIFY: packetery/libs/DI/ContainerFactory.php#register] | INCORRECT | `ContainerFactory::create` registers only two factories, `\Db::class` and `\ControllerCore::class`; `\Packetery` is never registered. A service declaring `\Packetery $module` is therefore built by `Container::create`'s reflection fallback — `packetery/libs/DI/Container.php:63` `$reflection = new \ReflectionClass($class);` … `newInstanceArgs($paramInstances)` — i.e. a *new* `Packetery` instance, not the one PrestaShop holds. |
| 20–22 | no network hop between the slices; every internal edge is a PHP call through the container or a direct `new` | CONFIRMED | All cross-slice edges checked are `$module->diContainer->get(X::class)` or constructor injection; the only HTTP/SOAP clients (`SoapApi`, `Downloader`, `VersionChecker`, `PickupPointValidate`) address systems outside the repository. |
| 24–37 | the Mermaid diagram's edge set | INCORRECT | The 11 edges drawn contradict the module pages. Missing edges the reference pages state: checkout→tools (`packetery-checkout.md:185` "`ConfigHelper`, `LogRepository` and `HttpClientWrapper` of `packetery-tools`", code `PickupPointValidator::__construct`), checkout→module (`packetery-checkout.md:183` "`Packetery\Module\Cart` of `packetery-module`"), adminui→tools (`packetery-admin-ui.md:135-136` `MessageManager`/`ConfigHelper`, code `PacketeryCarrierGridController.php:16,120`), adminui→module (`packetery-admin-ui.md:137` `VersionChecker`, code `PacketeryLogGridController.php:230`), adminui→packet-tracking (`packetery-admin-ui.md:125` `PacketStatusFactory`, code `PacketeryOrderGridController.php:25-26`), order→packet-tracking (`packetery-order.md:105` "references → packetery-packet-tracking"), tracking→module (`packetery-module.md:138` "`SoapApi` … by packetery-packet-tracking"). The prose beside the diagram also says "every slice reaches packetery-tools" (line 55), which the diagram's three tools edges contradict; and the drawn edge `module --> order` appears in no sentence of the prose. |
| 39–44 | "Textual description of the diagram's links" — four `calls →` targets | CONFIRMED | The four targets (order, carrier, tools, module) are exactly the diagram's target nodes. |
| 46–47 | packetery-checkout reaches packetery-order to persist the chosen pickup point against the cart [VERIFY: packetery/controllers/front/checkout.php#savePickupPointInCart] | CONFIRMED | `packetery/controllers/front/checkout.php:35-38` `case 'savePickupPointInCart': … echo $orderSaver->savePickupPointInCartGetJson();`. |
| 47–49 | packetery-checkout reaches packetery-carrier to learn the vendors the paired carrier allows [VERIFY: packetery/libs/PickupPointValidate/PickupPointValidator.php#__construct] | CONFIRMED | The edge is real, but the anchor is on the wrong symbol: `__construct` (lines 43-55) injects only `ConfigHelper`, `LogRepository`, `\Packetery`, `CartService`, `HttpClientWrapper`. The carrier dependency is in `createPickupPointValidateRequest`: `PickupPointValidator.php:109` `if ($packeteryCarrier['allowed_vendors'] !== null)` plus `CarrierVendors::INTERNAL_PICKUP_POINT_CARRIER` and `CarrierTools::findExternalCarrierId`. |
| 50–51 | packetery-admin-ui renders grids over rows packetery-order and packetery-carrier own and triggers their actions [VERIFY: …/PacketeryOrderGridController.php#processBulkCreatePacket] | CONFIRMED | `PacketeryOrderGridController.php:254` `public function processBulkCreatePacket()` → `createPackets($ids)`; the carrier grid resolves `CarrierRepository`/`CarrierAdminForm`. |
| 52–53 | packetery-packet-tracking drives packetery-order and packetery-carrier from its cron tasks [VERIFY: packetery/libs/Cron/Tasks/GetConsignPassword.php#execute] | CONFIRMED | `GetConsignPassword.php:48` `public function execute(int $maxOrders, int $maxOrderAgeDays)` uses `OrderRepository` and `ConsignPasswordProvider` (packetery-order); `Cron/Tasks/DownloadCarriers.php` wraps `Downloader::run` (packetery-carrier). |
| 53–54 | packetery-order calls back into packetery-module for the SOAP facade [VERIFY: packetery/libs/Order/PacketCanceller.php#soapApi] | CONFIRMED | `PacketCanceller.php:34` constructor param `SoapApi $soapApi`, line 49 `$this->soapApi->cancelPacket(...)`; `SoapApi` lives in `packetery/libs/Module/`. |
| 55–56 | every slice reaches packetery-tools for configuration, database access and logging [VERIFY: packetery/libs/Tools/DbTools.php#execute] | CONFIRMED | `DbTools.php:144` `public function execute($sql, $useCache = true)`; all six other slices use `ConfigHelper`, `DbTools`, `LogRepository` or `MessageManager`. (The diagram at 24-37 shows only three of these edges — see that row.) |
| 62–64 | step 1: the storefront script mounts the widget and posts the picked point to the front controller `checkout` [VERIFY: packetery/views/js/front.js#savePickupPointInCart] | CONFIRMED | `packetery/views/js/front.js:525` `savePickupPointInCart: function (…)` → `PacketaModule.ajax.post('savePickupPointInCart', {…})`. |
| 65–66 | step 2: the controller writes the point against the cart id in `packetery_order` [VERIFY: packetery/libs/Order/OrderSaver.php#savePickupPointInCartGetJson] | CONFIRMED | `OrderSaver.php:224` → `savePickupPointInCart()`; lines 197-206 `$this->orderRepository->updateByCart($packeteryOrderFields, (int) $cartId, true)` / `$packeteryOrderFields['id_cart'] = ((int) $cartId); … insert(...)`. |
| 67–69 | step 3: the hook handler completes the same row with the order id and the weight [VERIFY: packetery/libs/Order/OrderSaver.php#saveNewOrder] | CONFIRMED | `packetery.php:1043` `$orderSaver->saveNewOrder($params['cart'], $params['order']);` from `hookActionValidateOrder`; `OrderSaver::save` writes `'id_order' => (int) $order->id` and `$data['weight'] = $packeteryWeight`. |
| 67–69 | step 3: the same handler writes "the cash-on-delivery amount and the prices" | INCORRECT | `OrderSaver::save` writes `$data['is_cod'] = ($carrierIsCod \|\| $paymentIsCod);` — a `tinyint(1)` flag (`Installer.php:159` `` `is_cod` tinyint(1) NOT NULL DEFAULT 0 ``), not an amount. The amount columns `price_total` and `price_cod` (`Installer.php:180-181`) are written by `packetery/libs/Order/OrderDetailsUpdater.php:140` from the admin order detail, never by `saveNewOrder`. |
| 70–71 | step 4: an employee selects orders in the grid and runs the submit action [VERIFY: …/PacketeryOrderGridController.php#processBulkCreatePacket] | CONFIRMED | `PacketeryOrderGridController.php:254-263`, `$ids = $this->boxes;` then `$this->createPackets($ids);`. |
| 72–74 | step 5: the submitter builds the packet attributes, calls the SOAP API and stores the returned packet id as `tracking_number` [VERIFY: packetery/libs/Order/PacketSubmitter.php#createPacketSoap] | CONFIRMED | `PacketSubmitter.php:203-207` `new \SoapClient($this->soapApi->resolveWsdlUrl()); … $client->createPacket($this->configHelper->getApiPass(), $packetAttributes)`; line 163 `$packeteryTracking->updateOrderTrackingNumber($orderId, $trackingNumber)`. |
| 76–77 | an unpaired carrier gets no widget and no row | CONFIRMED | `packetery.php:778-781` `$packeteryCarrier = $carrierRepository->getPacketeryCarrierById(...); if (!$packeteryCarrier) { return; }` in `hookDisplayCarrierExtraContent`; `OrderSaver.php:79` `$this->orderRepository->deleteByCartId($cart->id);` when no pairing exists. |
| 77–79 | a fault is collected per order and reported in the grid; failed orders keep no tracking number [VERIFY: packetery/libs/Order/PacketSubmitter.php#ordersExport] | CONFIRMED | `PacketSubmitter.php:144` `ordersExport`; `catch (ExportException $e) { $errors[] = $e; } catch (ApiClientException $e) { $errors[] = $e; }` then `throw new AggregatedException($errors);` — no tracking number is written on that path. |
| 83–84 | background work runs through one public front controller `cron`, which the shop's own scheduler must call [VERIFY: packetery/controllers/front/cron.php#display] | CONFIRMED | `cron.php:22` `public $auth = false;`, `cron.php:44` `public function display()`; the repository holds no scheduler (manifest unknown "Which scheduler calls the `cron` front controller"). |
| 84–87 | the controller compares `token` with `PACKETERY_CRON_TOKEN`, maps `task` onto one of five task classes and renders the message rows [VERIFY: packetery/controllers/front/cron.php#validateToken] | CONFIRMED | `cron.php:186-190` `$storedToken = ConfigHelper::get('PACKETERY_CRON_TOKEN'); … return $storedToken === $token;`; the dispatch switch names exactly `DeleteLabels`, `DownloadCarriers`, `PurgeLogs`, `UpdatePacketStatus`, `GetConsignPassword`; `renderMessage` fetches `cron-message-row.tpl`. |
| 89 | "`cron?task=UpdatePacketStatus` is the only task with a loop of its own" | INCORRECT | `packetery/libs/Cron/Tasks/GetConsignPassword.php:84` `foreach ($orders as $row) {` — one SOAP `packetInfo` call per order; and `packetery/libs/Cron/Tasks/DeleteLabels.php:73` `foreach ($files as $label) {`. |
| 89–92 | it reads the orders with an open packet, asks the SOAP API for each tracking number's history, compares with the stored rows and writes the difference into `packetery_packet_status` [VERIFY: …/PacketTrackingCron.php#getPacketTracking] | CONFIRMED | `PacketTrackingCron.php:100` `getOrdersByStateAndLastUpdate(...)` excluding `getFinalStatusIds()`, line 110 `$this->soapApi->getPacketTracking($order['tracking_number'])`, line 163 `isDifferenceBetweenApiAndDatabase(...)`, lines 171-190 `delete(...)` / `insert(...)` on `PacketTrackingRepository`. |
| 92–94 | when the last status matches a configured mapping the same run changes the PrestaShop order state [VERIFY: …/PacketTrackingCron.php#updateOrderStatus] | CONFIRMED | `PacketTrackingCron.php:193-195` `if ($isStatusChangeEnabled) { $this->updateOrderStatus($lastRecord, $order['id_order']); }`; line 212-230 reads `PACKETERY_ORDER_STATUS_CHANGE_<code>` and calls `$order->setCurrentState((int) $newOrderStatus);`. |
| 95–97 | the other four tasks: DownloadCarriers refreshes the carrier catalogue, GetConsignPassword fills missing consignment codes, DeleteLabels removes old label PDFs, PurgeLogs trims the log table | CONFIRMED | `Cron/Tasks/DownloadCarriers.php:40` wraps `Downloader::run`; `GetConsignPassword.php:84-95` `setConsignPassword(...)`; `DeleteLabels.php:51` `glob(PACKETERY_PLUGIN_DIR . '/labels/*.pdf', …)` + `unlink`; `PurgeLogs.php:45` `$this->logRepository->purge($logExpirationDays);`. |
| 101–102 | the module authenticates nothing itself; two PrestaShop mechanisms plus one shared secret of its own | CONFIRMED | Matches the four boundaries below: employee tab permission and the `ajax_front` token are PrestaShop's, `PACKETERY_CRON_TOKEN` is the module's own. |
| 106 | employee → admin grids: PrestaShop employee session plus the tab permission attached at install [VERIFY: packetery/libs/Module/Installer.php#insertMenuItems] | CONFIRMED | `Installer.php:70` `insertMenuItems()` adds the five tabs (`Packetery`, `PacketeryOrderGrid`, `PacketeryCarrierGrid`, `PacketerySetting`, `PacketeryLogGrid`) via `addTab`; no controller declares a `viewAccess` override, matching `manifest.yaml` `auth: prestashop-employee-tab-permission`. |
| 107 | storefront → `checkout`: comparison with the front AJAX token PrestaShop issues [VERIFY: packetery/controllers/front/checkout.php#ajax_front] | CONFIRMED | `checkout.php:28-32` `$real_token = Tools::getToken('ajax_front'); if ($token !== $real_token) { return; }`. |
| 108 | scheduler → `cron`: shared secret in the `token` request value [VERIFY: packetery/controllers/front/cron.php#validateToken] | CONFIRMED | `cron.php:183-191`. Consistent with `manifest.yaml` `exposes: cron` auth. |
| 109 | prestashop → packeta-soap-api: the API password kept in the shop configuration [VERIFY: packetery/libs/Module/SoapApi.php#getApiPass] | CONFIRMED | Every SOAP call passes `$this->configHelper->getApiPass()` (e.g. `SoapApi.php:97, 253, 321`); `ConfigHelper.php:104-107` `return self::get(self::KEY_APIPASS);`. The method itself is defined on `ConfigHelper`, not on `SoapApi`. |
| 111–112 | "The cron secret is generated once at install and never rotated by the module" [VERIFY: packetery/libs/Module/Installer.php#updateConfiguration] | INCORRECT | Generation at install is right (`Installer.php:247` `ConfigHelper::update('PACKETERY_CRON_TOKEN', \Tools::passwdGen(32))`), but the module does rotate it: `packetery/upgrade/upgrade-3.0.0.php:45` `&& ConfigHelper::update('PACKETERY_CRON_TOKEN', Tools::passwdGen(32))` — upgrading to 3.0.0 replaces the secret and invalidates the previously printed cron URLs. |
| 112–113 | the module identifies no caller of the `cron` route beyond that secret, so any client holding it can run every task | CONFIRMED | `cron.php:22` `public $auth = false;` and `validateToken()` is the only check before the task switch. |
| 117–118 | everything is stored in the shop's MySQL database, in tables created at install and dropped at uninstall [VERIFY: packetery/libs/Module/Installer.php#installDatabase] | CONFIRMED | `Installer.php:149` `installDatabase()` creates seven tables; `Uninstaller::uninstallDatabase` drops six of them. The seventh, `packetery_order`, is renamed rather than dropped — the exception the page itself states at lines 132-134. |
| 119–121 | the module owns five tables: `packetery_order` + `packetery_payment` (packetery-order), `packetery_address_delivery` (packetery-carrier), `packetery_product_attribute` (packetery-module), `packetery_log` (packetery-tools) | CONFIRMED | Exactly the five `owns_data` entries of `ai-docs/manifest.yaml`, with the same module attribution. |
| 121–124 | `packetery_carriers` mirrors the carrier feed and `packetery_packet_status` mirrors a packet's delivery history [VERIFY: …/PacketTrackingRepository.php#getCreateTableSql] | CONFIRMED | `PacketTrackingRepository.php:94` `public function getCreateTableSql()`; both entities appear under `reads_external_data` in `manifest.yaml` (owners `packeta-pickup-point-api` and `packeta-soap-api`), not under `owns_data`. |
| 126 | `calls → mysql (sync, SQL)` | CONFIRMED | `manifest.yaml` `calls: service: mysql, kind: db, module: packetery-tools, evidence: packetery/libs/Tools/DbTools.php#execute`. |
| 128–129 | every statement goes through `DbTools`, which turns a failed query into a `DatabaseException` instead of a silent `false` [VERIFY: packetery/libs/Tools/DbTools.php:66] | CONFIRMED | Line 66 is `private function logAndThrow($query, $exception = null)`, lines 70/75 `throw new DatabaseException(...)`. The only direct `\Db::getInstance()` in the module is the container factory (`ContainerFactory.php:21`). |
| 130–132 | the schema has no migration framework; the upgrade script runs the `ALTER TABLE` statements itself [VERIFY: packetery/upgrade/upgrade-3.4.0.php#upgrade_module_3_4_0] | CONFIRMED | `upgrade-3.4.0.php:17` `function upgrade_module_3_4_0(Packetery $module): bool` with three literal `ALTER TABLE` strings passed to `$dbTools->executeQueries($sql, …)`. |
| 130 | "each released version ships one `upgrade_module_*` function" | INCORRECT | `packetery/upgrade/` holds 14 upgrade scripts (2.1.3–2.1.15, 3.0.0–3.4.0), while `git tag` lists 30 released tags; v2.1.8, v2.1.10, v2.1.11, v2.1.13, v2.1.16–v2.1.20, v3.0.1–v3.0.4, v3.2.1 and v3.2.2 ship no `upgrade_module_*` function at all. |
| 132–134 | uninstall renames `packetery_order` to a backup table instead of dropping it [VERIFY: packetery/libs/Module/Uninstaller.php:117] | CONFIRMED | Line 117: ``$sql[] = 'RENAME TABLE `' . _DB_PREFIX_ . 'packetery_order` TO `' . _DB_PREFIX_ . 'packetery_order_backup`';``. |
| 138–140 | three cross-cutting mechanisms; the hook system carries the in-process events, has no channel and is not a manifest entry | CONFIRMED | The table at 152-156 lists exactly three; `manifest.yaml` contains no hook under `exposes` or `calls`. |
| 140–142 | "The module registers 21 hooks, one list branching on the PrestaShop version" [VERIFY: packetery/packetery.php#getModuleHooksList] | INCORRECT | `packetery.php:1424-1451` lists 19 fixed names and then appends exactly one of `displayAdminOrderLeft` / `displayAdminOrderMain`, so any installation registers **20** hooks; 21 is the count only if both branches are added together. |
| 142–148 | the three named hook families (storefront / order / back office) | CONFIRMED | All 16 named hooks appear in `getModuleHooksList` (`packetery.php:1424-1449`). |
| 148–150 | "Three of the hooks are dispatched by this repository itself for its own grids" [VERIFY: packetery.php#hookActionPacketeryOrderGridListingResultsModifier] | INCORRECT | The repository contains no `Hook::exec` call at all (grep over `packetery/`); it only *registers* the names and implements the handlers (`packetery.php:1596, 1611, 1744`). The dispatcher is PrestaShop's list helper — as `ai-docs/reference/packetery-admin-ui.md:145-146` states: "are named after these controllers and are fired by the PrestaShop list helper, not by packetery-admin-ui", and `ai-docs/reference/packetery-module.md:149`: "registers its own hook names, which PrestaShop dispatches in process". |
| 154 | table row "PrestaShop hooks as in-process events — 21 registered hooks" [VERIFY: packetery.php#getModuleHooksList] | INCORRECT | Same defect as line 140: `getModuleHooksList` returns 20 entries per installation (19 + one version branch). |
| 155 | table row "container lookup by class name — every service of every module" [VERIFY: packetery/libs/DI/Container.php#ReflectionClass] | CONFIRMED | `Container.php:63` `$reflection = new \ReflectionClass($class);`, and `get($class)` is keyed by class name only. |
| 156 | table row "API call logging — every SOAP and HTTP call the module makes" [VERIFY: packetery/libs/Log/LogRepository.php#insertRow] | INCORRECT | `insertRow` exists (`LogRepository.php:94`), but two outbound HTTP calls write nothing to `packetery_log`: `packetery/libs/ApiCarrier/Downloader.php` (the packeta-pickup-point-api carrier feed) contains no `LogRepository` reference at all, and `packetery/libs/Module/VersionChecker.php` logs only to `\PrestaShopLogger::addLog` (lines 60, 102). Both are `calls` entries in `manifest.yaml` (`packeta-pickup-point-api`, `github-api`). |
| 158–160 | the container is a hand-written map with two registered factories and a reflection fallback; services are asked for by class name, never by a string id [VERIFY: packetery/libs/DI/Container.php#register] | CONFIRMED | `ContainerFactory.php:20,24` register exactly `\Db::class` and `\ControllerCore::class`; `Container::create` falls back to `new \ReflectionClass($class)`; `Container::get($class)`/`register($class, \Closure $factory)` take class names. |
| 162 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder. |

CONFIRMED 39 · INCORRECT 11 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/architecture.md`. `confidence` stays `draft`: the confirmed share (39 of 50 ratio rows, 78 %) is below the 80 % the promotion rule asks for, so raising it needs a second verification pass on the corrected text.

## ai-docs/architecture.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22 (second pass)

| line | claim | class | evidence |
|---|---|---|---|
| 17 | prestashop is one PrestaShop module whose slices meet in the module class `Packetery` | CONFIRMED | `packetery/packetery.php` `class Packetery extends CarrierModule`; every slice under `packetery/libs/` is resolved from `$this->diContainer`. |
| 18 | that class is the object PrestaShop instantiates for the hooks | CONFIRMED | Hook handlers are methods on `Packetery` (`packetery.php:1026 hookActionValidateOrder`, `:757 hookDisplayCarrierExtraContent`, …); PrestaShop calls them on the module instance. |
| 18–20 | the four admin controllers and the two front controllers are instantiated by the PrestaShop dispatcher instead [VERIFY: packetery/controllers/front/cron.php#PacketeryCronModuleFrontController] | CONFIRMED | `packetery/controllers/admin/` holds exactly four controllers (`PacketeryCarrierGridController`, `PacketeryLogGridController`, `PacketeryOrderGridController`, `PacketerySettingController`); `packetery/controllers/front/` holds `checkout.php` and `cron.php`; `cron.php:19` `class PacketeryCronModuleFrontController extends ModuleFrontController`. None is created by `Packetery`. |
| 20–23 | the container registers exactly two factories, for `\Db` and for `\ControllerCore`, and builds anything else by reflection over the constructor signature [VERIFY: packetery/libs/DI/ContainerFactory.php#register] | CONFIRMED | `ContainerFactory.php:20` `$container->register(\Db::class, …)` and `:24` `$container->register(\ControllerCore::class, …)` — the only two `register` calls in the repository; `Container.php:63-66` `$reflection = new \ReflectionClass($class); … newInstanceArgs($paramInstances)` with `getParamInstances` reading the constructor parameter types. |
| 23–25 | there is no network hop between the slices — every internal edge is a PHP call resolved through the container or through a direct `new` | CONFIRMED | Every cross-slice edge checked is `$module->diContainer->get(X::class)` or constructor injection; the only outbound clients (`Module/SoapApi`, `ApiCarrier/Downloader`, `Module/VersionChecker`, `PickupPointValidate/PickupPointValidate`, `Tools/HttpClientWrapper`) address systems outside the repository. |
| 29–47 | the 19 edges the Mermaid diagram draws | CONFIRMED | Each drawn edge exists in code: checkout→order (`controllers/front/checkout.php:35,43` `OrderSaver`, `Ajax`), checkout→carrier (`PickupPointValidator.php:103-105` `CarrierTools`, `CarrierVendors`), checkout→module (`checkout.php:40` `Packetery\Module\Cart`), adminui→order/carrier/tracking/module/tools (`PacketeryOrderGridController.php:25` `PacketStatusFactory`, `:254` `processBulkCreatePacket`, `PacketeryCarrierGridController` `CarrierAdminForm`/`MessageManager`, `PacketeryLogGridController` `VersionChecker`), tracking→order (`Cron/Tasks/GetConsignPassword.php:68` `OrderRepository`), tracking→carrier (`Cron/Tasks/DownloadCarriers.php:42` `$this->downloader->run()`), tracking→module (`PacketTrackingCron.php:15,110` `SoapApi::getPacketTracking`), tracking→tools (`PacketTrackingCron.php:14` `LogRepository`, `ConfigHelper`), module→order (`packetery.php:1042` `OrderSaver`), order→module (`Order/PacketCanceller.php:34` `SoapApi $soapApi`), order→tracking (`OrderRepository.php:572-611`, joins `packetery_packet_status`), and the six `→ tools` edges. |
| 29–47 | the diagram as the picture of the module composition (page-vs-page completeness) | INCORRECT | Five edges that the reference pages assert, and that the code confirms, are not drawn: **module→carrier** (`ai-docs/reference/packetery-carrier.md`: "the module class renumbers the pairing row when PrestaShop replaces a carrier"; code `packetery.php:160` `$carrierRepository = $this->diContainer->get(Packetery\Carrier\CarrierRepository::class);` in `hookActionCarrierUpdate`); **module→checkout** (`ai-docs/reference/packetery-checkout.md`: "`packetery-module` drives the whole checkout: its `ActionValidateStepComplete` hook calls `PickupPointValidator`"; code `libs/Hooks/ActionValidateStepComplete.php:19,88-89`, plus `packetery.php:918-919` `Packetery\Cart\CartService`, `:1185` `Packetery\Weight\Calculator`); **module→packet-tracking** (`ai-docs/reference/packetery-packet-tracking.md`: "the installer creates the table"; code `libs/Module/Installer.php:225-228` `$this->module->diContainer->get(PacketTrackingRepository::class)`); **order→carrier** (`ai-docs/reference/packetery-carrier.md`: "order handling reads the pairing when it saves an order"; code `libs/Order/OrderSaver.php:16` `use Packetery\Carrier\CarrierRepository;` and `:72` `getPacketeryCarrierById`); **order→checkout** (`ai-docs/reference/packetery-checkout.md`: "`packetery-order` uses `Weight\Calculator` when it exports an order"; code `libs/Order/OrderExporter.php:15,30` and `libs/Order/OrderSaver.php:20`). With those missing, `carrier`, `checkout` and `tracking` are drawn as sinks that the rest of the module never re-enters. |
| 50 | "Textual description of the diagram's links:" — the prose that follows enumerates the diagram's links | INCORRECT | Two drawn edges have no counterpart in that prose: line 34 `adminui --> module` (real — `PacketeryLogGridController` resolves `VersionChecker`, `PacketeryOrderGridController` the `SoapApi` client, as `ai-docs/reference/packetery-admin-ui.md` states) and line 38 `tracking --> module` (real — `PacketTrackingCron.php:15` `use Packetery\Module\SoapApi;`). The paragraph at 58–75 names admin-ui's links to order, carrier and packet-tracking only, and packet-tracking's to order and carrier only. |
| 52–56 | the five `calls → …` lines | CONFIRMED | The five targets (order, carrier, tools, module, packet-tracking) are exactly the target nodes of the diagram; nothing points at `checkout` or `adminui` there either. |
| 58–59 | packetery-checkout reaches packetery-order to persist the chosen pickup point against the cart [VERIFY: packetery/controllers/front/checkout.php#savePickupPointInCart] | CONFIRMED | `checkout.php:35-38` `case 'savePickupPointInCart': $orderSaver = $this->module->diContainer->get(OrderSaver::class); … echo $orderSaver->savePickupPointInCartGetJson();`. |
| 59–62 | packetery-checkout reaches packetery-carrier to learn which vendors the paired carrier allows [VERIFY: packetery/libs/PickupPointValidate/PickupPointValidator.php#createPickupPointValidateRequest] | CONFIRMED | `PickupPointValidator.php:97` `public function createPickupPointValidateRequest(...)`, `:103-105` `CarrierTools::findExternalCarrierId` / `CarrierVendors::INTERNAL_PICKUP_POINT_CARRIER`, `:109-117` decodes `allowed_vendors`. The anchor now points at the right symbol. |
| 62–63 | packetery-checkout reaches packetery-module for the carrier extra content [VERIFY: packetery/controllers/front/checkout.php#fetchExtraContent] | CONFIRMED | `checkout.php:39-42` `case 'fetchExtraContent': $packeteryCart = $this->module->diContainer->get(Cart::class); echo $packeteryCart->packeteryCreateExtraContent();` with `use Packetery\Module\Cart;`. |
| 63–66 | packetery-admin-ui renders the grids over the rows packetery-order and packetery-carrier own and triggers their actions [VERIFY: packetery/controllers/admin/PacketeryOrderGridController.php#processBulkCreatePacket] | CONFIRMED | `PacketeryOrderGridController.php:254` `public function processBulkCreatePacket()`; the carrier grid resolves `CarrierRepository`/`CarrierAdminForm` (`ai-docs/reference/packetery-admin-ui.md` names the same set). |
| 66–67 | packetery-admin-ui translates the stored packet status of packetery-packet-tracking for the list [VERIFY: packetery/controllers/admin/PacketeryOrderGridController.php#PacketStatusFactory] | CONFIRMED | `PacketeryOrderGridController.php:25` `use Packetery\PacketTracking\PacketStatusFactory;`, `:752-756` `getTranslatedPacketStatus()` resolving `PacketStatusFactory` from the container, wired as the `callback` of the status column (`:179`). |
| 68–69 | packetery-packet-tracking drives packetery-order and packetery-carrier from its cron tasks [VERIFY: packetery/libs/Cron/Tasks/GetConsignPassword.php#execute] | CONFIRMED | `GetConsignPassword.php:48` `public function execute(int $maxOrders, int $maxOrderAgeDays): array`, `:68` `$this->orderRepository->getOrdersMissingConsignPassword(...)`, `:89` `setConsignPassword(...)` (packetery-order). The carrier half sits in a different file than the anchor: `Cron/Tasks/DownloadCarriers.php:42` `$result = $this->downloader->run();`. |
| 69–70 | packetery-module registers the hooks that call packetery-order [VERIFY: packetery/packetery.php#hookActionValidateOrder] | CONFIRMED | `packetery.php:1026` `public function hookActionValidateOrder($params)`, `:1042-1043` `$orderSaver = $this->diContainer->get(Packetery\Order\OrderSaver::class); $orderSaver->saveNewOrder(...)`; the name is in `getModuleHooksList` (`:1426`). |
| 70–72 | packetery-order calls back into packetery-module for the SOAP facade [VERIFY: packetery/libs/Order/PacketCanceller.php#soapApi] | CONFIRMED | `PacketCanceller.php:24` `/** @var SoapApi */ private $soapApi;`, `:34` constructor param, `:49` `$this->soapApi->cancelPacket(new CancelPacketRequest($packetId));`; `SoapApi` lives in `packetery/libs/Module/`. |
| 72–73 | packetery-order reads the status rows of packetery-packet-tracking [VERIFY: packetery/libs/Order/OrderRepository.php#getOrdersByStateAndLastUpdate] | CONFIRMED | `OrderRepository.php:572` `public function getOrdersByStateAndLastUpdate(...)`, `:595-601` `LEFT JOIN (SELECT … FROM `…packetery_packet_status` …)`. |
| 74–75 | every slice reaches packetery-tools for configuration, database access and logging [VERIFY: packetery/libs/Tools/DbTools.php#execute] | CONFIRMED | `DbTools.php:144` `public function execute($sql, $useCache = true)`. All six other slices use `DbTools`, `ConfigHelper`, `LogRepository`, `MessageManager` or `Logger`: checkout (`PickupPointValidator`), order (`OrderSaver`, `OrderRepository`), carrier (`Downloader`, `CarrierRepository`), adminui (`MessageManager`, `ConfigHelper`), tracking (`PacketTrackingCron`, `PurgeLogs`), module (`Installer`, `SoapApi`). |
| 79 | the cart becomes a Packeta packet in five steps | CONFIRMED | The five numbered steps below match the five code stages verified in the rows that follow. |
| 81–83 | step 1: the storefront script mounts the widget under the selected carrier and posts the picked point to the front controller `checkout` [VERIFY: packetery/views/js/front.js#savePickupPointInCart] | CONFIRMED | `front.js:27` loads the widget library, `:70` `PacketaModule.ui.initializeWidget();`, `:525` `savePickupPointInCart: function (…)` and `:540` the `'savePickupPointInCart'` action posted to the module's front controller. |
| 84–85 | step 2: the controller writes the point against the cart id in `packetery_order` [VERIFY: packetery/libs/Order/OrderSaver.php#savePickupPointInCartGetJson] | CONFIRMED | `OrderSaver.php` `savePickupPointInCartGetJson()` → `savePickupPointInCart()`, which ends in `$this->orderRepository->updateByCart($packeteryOrderFields, (int) $cartId, true)` or `$packeteryOrderFields['id_cart'] = ((int) $cartId); … insert($packeteryOrderFields, true)`. |
| 86–88 | step 3: on order validation the hook handler completes the same row with the order id, the weight and the cash-on-delivery flag [VERIFY: packetery/libs/Order/OrderSaver.php#saveNewOrder] | CONFIRMED | `packetery.php:1043` `$orderSaver->saveNewOrder($params['cart'], $params['order']);` from `hookActionValidateOrder`; `OrderSaver::save` builds `'id_order' => (int) $order->id`, `$data['is_cod'] = ($carrierIsCod \|\| $paymentIsCod);` (a flag, matching `` `is_cod` tinyint(1) `` in `Installer.php:159`) and `$data['weight'] = $packeteryWeight;`. It also writes `id_cart`, `id_carrier` and, for address delivery, the branch columns — the sentence names three of the writes and claims no exclusivity. |
| 87–88 | step 3: the amounts are written later from the admin order detail | CONFIRMED | `price_total` / `price_cod` never appear in `OrderSaver`; they are written by `libs/Order/OrderDetailsUpdater.php:140-149`, whose only caller is `packetery.php:1077-1079` `packeteryHookDisplayAdminOrder` → `$orderDetailsUpdater->orderUpdate($messages, $packeteryOrder, $orderId);`. |
| 89–90 | step 4: an employee selects the orders in the Packeta order grid and runs the submit action [VERIFY: packetery/controllers/admin/PacketeryOrderGridController.php#processBulkCreatePacket] | CONFIRMED | `PacketeryOrderGridController.php:254` `public function processBulkCreatePacket()`. |
| 91–93 | step 5: the submitter builds the packet attributes, calls the Packeta SOAP API and stores the returned packet id as `tracking_number` [VERIFY: packetery/libs/Order/PacketSubmitter.php#createPacketSoap] | CONFIRMED | `PacketSubmitter.php:72-100` builds `$packetAttributes`, `:105` `$trackingNumber = $this->createPacketSoap($packetAttributes);` (`:203` `private function createPacketSoap(array $packetAttributes)`), `:163` `$packeteryTracking->updateOrderTrackingNumber($orderId, $trackingNumber)` writing `packetery_order.tracking_number`. |
| 95–96 | an unpaired carrier gets no widget and no row | CONFIRMED | `packetery.php:777-781` `$packeteryCarrier = $carrierRepository->getPacketeryCarrierById((int) $id_carrier); if (!$packeteryCarrier) { return; }` in `hookDisplayCarrierExtraContent`; `OrderSaver.php:75-77` `else { $this->orderRepository->deleteByCartId($cart->id); }`. |
| 96–98 | a fault from the API is collected per order and reported in the grid, and the orders that failed keep no tracking number [VERIFY: packetery/libs/Order/PacketSubmitter.php#ordersExport] | CONFIRMED | `PacketSubmitter.php:144` `public function ordersExport(array $orderIds)`, `:176-180` `catch (ExportException $e) { $errors[] = $e; } catch (ApiClientException $e) { $errors[] = $e; }`, `:183` `throw new AggregatedException($errors);`; the `updateOrderTrackingNumber` call is never reached on that path. |
| 102–103 | background work runs through one public front controller `cron`, which the shop's own scheduler must call [VERIFY: packetery/controllers/front/cron.php#display] | CONFIRMED | `cron.php:22` `public $auth = false;`, `:44` `public function display()`; the repository contains no scheduler (`manifest.yaml` unknown: "Which scheduler calls the `cron` front controller"). |
| 103–106 | the controller compares the `token` request value with `PACKETERY_CRON_TOKEN`, then maps `task` onto one of five task classes and renders the returned message rows [VERIFY: packetery/controllers/front/cron.php#validateToken] | CONFIRMED | `cron.php:180-191` `validateToken()` with `$storedToken = ConfigHelper::get('PACKETERY_CRON_TOKEN'); return $storedToken === $token;`; the dispatch switch (`:73-116`) names exactly `DeleteLabels`, `DownloadCarriers`, `PurgeLogs`, `UpdatePacketStatus`, `GetConsignPassword`; `renderMessage` fetches `views/templates/front/cron-message-row.tpl`. |
| 109 | `cron?task=UpdatePacketStatus` "does the most work of the five" | UNCERTAIN | Not objectively checkable from the code. `PacketTrackingCron::run` is the longest task and makes one SOAP call per order, but `GetConsignPassword::execute` also loops orders with one SOAP call each (`GetConsignPassword.php:68-95`), so "most work" has no measurable referent in the repository. |
| 109–112 | it reads the orders that still have an open packet, asks the SOAP API for each tracking number's history, compares it with the stored rows and writes the difference into `packetery_packet_status` [VERIFY: packetery/libs/PacketTracking/PacketTrackingCron.php#getPacketTracking] | CONFIRMED | `PacketTrackingCron.php:99-104` `getOrdersByStateAndLastUpdate($enabledOrderStatuses, $finalStatusIds, …)` (final packet statuses excluded), `:110` `$this->soapApi->getPacketTracking($order['tracking_number'])`, `:158-190` compares `getPacketStatusesByOrderId` and calls `delete(...)` / `insert(...)` on `PacketTrackingRepository` (`$tableName = 'packetery_packet_status'`). |
| 112–113 | when the last status matches a configured mapping, the same run changes the PrestaShop order state [VERIFY: packetery/libs/PacketTracking/PacketTrackingCron.php#updateOrderStatus] | CONFIRMED | `PacketTrackingCron.php:193-195` `if ($isStatusChangeEnabled) { $this->updateOrderStatus($lastRecord, $order['id_order']); }`; `:212-229` `ConfigHelper::get('PACKETERY_ORDER_STATUS_CHANGE_' . $lastStatusCode)` then `$order->setCurrentState((int) $newOrderStatus);`. |
| 114–116 | DownloadCarriers refreshes the carrier catalogue, GetConsignPassword fills the missing consignment codes, DeleteLabels removes old label PDFs, PurgeLogs trims the log table | CONFIRMED | `DownloadCarriers.php:42` `$this->downloader->run()`; `GetConsignPassword.php:68,89` `getOrdersMissingConsignPassword(...)` / `setConsignPassword(...)`; `DeleteLabels.php:51,83` `glob(PACKETERY_PLUGIN_DIR . '/labels/*.pdf', GLOB_NOSORT)` / `unlink($label)`; `PurgeLogs.php:45` `$this->logRepository->purge($logExpirationDays);`. |
| 120–121 | the module authenticates nothing itself; it leans on the two mechanisms PrestaShop provides and on one shared secret of its own | CONFIRMED | Matches the four rows below: the employee tab permission and `Tools::getToken('ajax_front')` are PrestaShop's, `PACKETERY_CRON_TOKEN` is the module's own; the SOAP password is an outbound credential, not an inbound check. |
| 125 | employee → admin grids: PrestaShop employee session plus the tab permission attached at install [VERIFY: packetery/libs/Module/Installer.php#insertMenuItems] | CONFIRMED | `Installer.php:70-115` `insertMenuItems()` adds the five tabs (`Packetery`, `PacketeryOrderGrid`, `PacketeryCarrierGrid`, `PacketerySetting`, `PacketeryLogGrid`) through `addTab` → `$tab->add()`; no controller under `packetery/controllers/` declares `viewAccess` or `checkAccess`, so access follows the profile permission on the installed tab, as `manifest.yaml` records. |
| 126 | storefront → `checkout`: comparison with the front AJAX token PrestaShop issues [VERIFY: packetery/controllers/front/checkout.php#ajax_front] | CONFIRMED | `checkout.php:28-32` `$real_token = Tools::getToken('ajax_front'); if ($token !== $real_token) { return; }`. |
| 127 | scheduler → `cron`: shared secret in the `token` request value [VERIFY: packetery/controllers/front/cron.php#validateToken] | CONFIRMED | `cron.php:180-191`; consistent with the `exposes: cron` auth text in `manifest.yaml`. |
| 128 | prestashop → packeta-soap-api: the API password kept in the shop configuration [VERIFY: packetery/libs/Module/SoapApi.php#getApiPass] | CONFIRMED | Every SOAP operation passes `$this->configHelper->getApiPass()` (`SoapApi.php:97, 166, 253, …`). The method itself is defined on `ConfigHelper`, so the anchor names a call site rather than a declaration. |
| 130–133 | the cron secret is generated at install [VERIFY: packetery/libs/Module/Installer.php#updateConfiguration] and replaced once, by the 3.0.0 upgrade, which invalidates every cron URL printed before it [VERIFY: packetery/upgrade/upgrade-3.0.0.php:45] | CONFIRMED | `Installer.php:247` `&& ConfigHelper::update('PACKETERY_CRON_TOKEN', \Tools::passwdGen(32))` inside `updateConfiguration()` (`:240`); `upgrade-3.0.0.php:45` `&& ConfigHelper::update('PACKETERY_CRON_TOKEN', Tools::passwdGen(32))`. Those are the only two writes of the key in the repository (the third and fourth hits are the read in `cron.php:187` / `packetery.php:502` and `Uninstaller.php:171` `deleteByName`), so "once" is exact. |
| 133–135 | the module identifies no caller of the `cron` route beyond that secret, so any client that holds it can run every task | CONFIRMED | `cron.php:22` `public $auth = false;` and `validateToken()` is the only guard before the task switch; no IP, referer or user check anywhere in the file. |
| 137–139 | everything is stored in the shop's own MySQL database, in tables created at install and dropped at uninstall [VERIFY: packetery/libs/Module/Installer.php#installDatabase] | CONFIRMED | `Installer.php:149` `private function installDatabase()` creates seven tables; `Uninstaller.php:50-125` drops six of them. The seventh, `packetery_order`, is renamed — the exception this page itself states at 153–155. |
| 139–142 | the module owns five tables: `packetery_order` and `packetery_payment` (packetery-order), `packetery_address_delivery` (packetery-carrier), `packetery_product_attribute` (packetery-module), `packetery_log` (packetery-tools) | CONFIRMED | Exactly the five `owns_data` entries of `ai-docs/manifest.yaml`, with the same module attribution; the DDL of the first three is in `Installer::installDatabase`, the other two come from `ProductAttributeRepository`/`LogRepository` (`$tableName = 'packetery_product_attribute'`, `'packetery_log'`). |
| 142–145 | `packetery_carriers` mirrors the carrier feed and `packetery_packet_status` mirrors the delivery history of a packet [VERIFY: packetery/libs/PacketTracking/PacketTrackingRepository.php#getCreateTableSql] | CONFIRMED | `ApiCarrierRepository.php:77` `public static $tableName = 'packetery_carriers';`, `PacketTrackingRepository.php:18` `public static $tableName = 'packetery_packet_status';` and `:94` `public function getCreateTableSql()`; both appear under `reads_external_data` in `manifest.yaml`, not under `owns_data`. |
| 147 | `calls → mysql (sync, SQL)` | CONFIRMED | `manifest.yaml` `calls:` `service: mysql`, `kind: db`, `module: packetery-tools`, `evidence: packetery/libs/Tools/DbTools.php#execute`. |
| 149–150 | every statement goes through `DbTools`, which turns a failed query into a `DatabaseException` instead of a silent `false` [VERIFY: packetery/libs/Tools/DbTools.php:66] | CONFIRMED | Line 66 is `private function logAndThrow($query, $exception = null)`; `:69-70` and `:74-75` `throw new DatabaseException(...)`. The only direct `\Db::getInstance()` in the module is the container factory (`ContainerFactory.php:21`). |
| 151 | the schema has no migration framework | CONFIRMED | `packetery/upgrade/` holds plain `upgrade_module_*($module)` functions with literal SQL strings; no migration library is required in `composer.json` and no version table exists. |
| 152 | `packetery/upgrade/` holds 14 `upgrade_module_*` functions | CONFIRMED | `grep -c "function upgrade_module" packetery/upgrade/*` returns 14, one per file: 2.1.3, 2.1.4, 2.1.5, 2.1.6, 2.1.7, 2.1.9, 2.1.12, 2.1.14, 2.1.15, 3.0.0, 3.1.0, 3.2.0, 3.3.0, 3.4.0. |
| 152 | fewer than the released versions | CONFIRMED | `packetery/CHANGE_LOG.txt` lists 42 released versions and `git tag` 32; both exceed 14. |
| 152–153 | and each runs its `ALTER TABLE` statements itself [VERIFY: packetery/upgrade/upgrade-3.4.0.php#upgrade_module_3_4_0] | INCORRECT | The anchored file does (`upgrade-3.4.0.php:17` `function upgrade_module_3_4_0(Packetery $module): bool` with three `ALTER TABLE` strings), but 6 of the 14 contain no `ALTER TABLE` at all: `upgrade-2.1.3.php`, `upgrade-2.1.4.php`, `upgrade-2.1.7.php`, `upgrade-2.1.12.php`, `upgrade-2.1.14.php` and `upgrade-3.3.0.php` (that one only re-registers hooks and writes configuration keys). "Each" is false as a universal claim; the point about there being no migration engine is not. |
| 153–155 | uninstall renames `packetery_order` to a backup table instead of dropping it [VERIFY: packetery/libs/Module/Uninstaller.php:117] | CONFIRMED | Line 117: ``$sql[] = 'RENAME TABLE `' . _DB_PREFIX_ . 'packetery_order` TO `' . _DB_PREFIX_ . 'packetery_order_backup`';`` preceded by the `DROP TABLE IF EXISTS … packetery_order_backup` at :116. |
| 159–161 | three cross-cutting mechanisms; the hook system carries every in-process event the module reacts to, those hooks have no channel and are not manifest entries | CONFIRMED | The table at 176–179 lists exactly three; `ai-docs/manifest.yaml` contains no hook under `exposes` or `calls`. |
| 161–163 | the module registers 20 hooks in any one installation — 19 fixed names plus one of two that branch on the PrestaShop version [VERIFY: packetery/packetery.php#getModuleHooksList] | CONFIRMED | `packetery.php:1424-1444` holds 19 literal names; `:1445-1449` `if (Tools::version_compare(_PS_VERSION_, '1.7.7', '<')) { $hooks[] = 'displayAdminOrderLeft'; } else { $hooks[] = 'displayAdminOrderMain'; }` adds exactly one. |
| 163–170 | the storefront, order and back-office hook families (16 named hooks) | CONFIRMED | All 16 appear in `getModuleHooksList`: `displayBeforeCarrier`, `displayCarrierExtraContent`, `displayHeader`, `actionCarrierProcess`, `actionValidateStepComplete`; `actionValidateOrder`, `actionObjectOrderUpdateBefore`, `actionObjectCartUpdateBefore`, `displayOrderConfirmation`, `displayOrderDetail`, `sendMailAlterTemplateVars`; `displayAdminOrderMain` (the ≥ 1.7.7 branch), `actionAdminControllerSetMedia`, `displayAdminProductsExtra`, `actionProductUpdate`, `actionProductDelete`. |
| 170–173 | three of the hook names belong to this repository's own grids, but the repository only registers and handles them — no `Hook::exec` call exists anywhere under `packetery/` [VERIFY: packetery/packetery.php#hookActionPacketeryOrderGridListingResultsModifier] | CONFIRMED | The three are `displayPacketeryOrderGridListAfter`, `actionPacketeryOrderGridListingResultsModifier`, `actionPacketeryCarrierGridListingResultsModifier` (`packetery.php:1436-1439`), handled at `:1596`, `:1611`, `:1744`. `grep -rn "Hook::exec" packetery/` and `grep -rn "HookCore" packetery/` both return nothing; the dispatcher is the PrestaShop list helper, as `ai-docs/reference/packetery-admin-ui.md` also states. |
| 177 | table row "PrestaShop hooks as in-process events — 20 registered hooks" | CONFIRMED | Same evidence as the 161–163 row; consistent with the prose. |
| 178 | table row "container lookup by class name — every service of every module" [VERIFY: packetery/libs/DI/Container.php#ReflectionClass] | CONFIRMED | `Container.php:63` `$reflection = new \ReflectionClass($class);`; `get($class)` and `register($class, \Closure $factory)` are keyed by class name only, and every call site passes `X::class`. |
| 179 | table row "API call logging — the SOAP calls and the widget validation call, not the carrier feed or the version check" [VERIFY: packetery/libs/Log/LogRepository.php#insertRow] | CONFIRMED | `LogRepository.php:94` `public function insertRow(...)`. Its call sites cover every SOAP call site — `PacketSubmitter.php:106,118`, `Labels.php:75,105`, `BarcodeProvider.php:69,83`, `PacketCanceller.php:51,64`, `ConsignPasswordProvider.php:46`, `Options.php:70,89,99`, `SoapApi.php:203`, `PacketTrackingCron.php:116,125` — plus the widget validation at `PickupPointValidator.php:67,79,81,91`. `packetery/libs/ApiCarrier/Downloader.php` (carrier feed) and `packetery/libs/Module/VersionChecker.php` (version check) contain no `LogRepository` reference at all. One nuance the row does not state: the carrier-tracking-number SOAP call logs failures only (`SoapApi.php:203` sits in the fault branch). |
| 181–183 | the container is a hand-written map with two registered factories and a reflection fallback, so a service is asked for by its class name and never by a string id [VERIFY: packetery/libs/DI/Container.php#register] | CONFIRMED | `Container.php:46-48` `public function register($class, \Closure $factory) { $this->factories[$class] = $factory; }`; `:58-66` the factory lookup then the `ReflectionClass` fallback; the only two registrations are in `ContainerFactory::create`. |
| 185 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder. |

CONFIRMED 55 · INCORRECT 3 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22, second pass):** the three INCORRECT rows were fixed — the Mermaid diagram gained the five missing edges (`module → carrier`, `module → checkout`, `module → packet-tracking`, `order → carrier`, `order → checkout`), the prose beside it now names every drawn edge including `adminui → module` and `tracking → module`, and the `ALTER TABLE` claim is limited to the 8 of 14 upgrade scripts that change the schema. The UNCERTAIN comparative ("does the most work of the five") was reworded to a claim the code supports. `confidence` was set to `reviewed`.
