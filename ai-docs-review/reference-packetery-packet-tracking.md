## ai-docs/reference/packetery-packet-tracking.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 19-22 | One public front controller `PacketeryCronModuleFrontController` authorises first, then dispatches to one of five task classes | CONFIRMED | `cron.php:45` `if ($this->validateToken() === false)` returns early; dispatch `switch ($task)` at `cron.php:83-116` has exactly 5 cases: DeleteLabels, DownloadCarriers, PurgeLogs, UpdatePacketStatus, GetConsignPassword |
| 21-22 | Each task class derives its name from its own short class name | CONFIRMED | `Cron/Tasks/Base.php:19-24` `getTaskName()` returns `(new \ReflectionClass(static::class))->getShortName()` |
| 24-25 | "Four of the five tasks are thin: they call a service of another module and turn its result into cron output" | INCORRECT | `DeleteLabels::execute()` calls no service at all: `DeleteLabels.php:51` `glob(PACKETERY_PLUGIN_DIR . '/labels/*.pdf', GLOB_NOSORT)`, then `filemtime()`/`unlink()` inline (~40 lines of own logic). Only three of the four (DownloadCarriers, GetConsignPassword, PurgeLogs) delegate to a service |
| 26-27 | `UpdatePacketStatus` drives `PacketTrackingCron`, which reads tracking history from the Packeta API and writes it to the shop DB | CONFIRMED | `UpdatePacketStatus.php:39` `$this->packetTrackingCron->run()`; `PacketTrackingCron.php:110` `$this->soapApi->getPacketTracking($order['tracking_number'])`, insert at `:176`. ("every open order" is a simplification — the query is capped by `MAX_PROCESSED_ORDERS` and the age limit) |
| 28-30 | Module owns the catalogue of packet statuses, their translated names and the final flag | CONFIRMED | `PacketStatusFactory.php:32-131` builds `PacketStatus(id, code, translated, isFinal)` for every status |
| 30-31 | The catalogue is shared vocabulary for the order grid, the order detail page and two configuration forms | CONFIRMED | `getPacketStatuses(` used in `controllers/admin/PacketeryOrderGridController.php:757`, `libs/Order/OrderDetailView.php:54`, `libs/Order/OrderStatusChangeFormService.php:58`, `libs/PacketTracking/PacketStatusTrackingFormService.php:115` |
| 31-32 | "…the two configuration forms that map packet statuses onto PrestaShop order states" | INCORRECT | Only `OrderStatusChangeFormService.php:60-71` maps statuses onto order states (`PACKETERY_ORDER_STATUS_CHANGE_<id>` selects). `PacketStatusTrackingFormService.php:44-105` defines only `PACKETERY_PACKET_STATUS_TRACKING_*` fields — it offers non-final statuses as tracking triggers, exactly as this document itself says at lines 133-134 (page-internal drift) |
| 34-36 | No scheduler of its own; the module prints ready-made controller URLs on its configuration page | CONFIRMED | `packetery.php:500-558` `generateCronInfoBlock()` builds `getModuleLink(..., 'cron', [...])` URLs and assigns them to Smarty |
| 36-38 | Nothing in the repository starts a timer, registers a system cron entry or subscribes to a PrestaShop cron module | CONFIRMED | No `actionCronJob`/`cronjob`/`crontab` match under `packetery/`; `packetery.php:1422-1447` `getModuleHooksList()` contains no cron hook |
| 40 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 44-45 | Exposes one registered front controller and five task names | CONFIRMED | `controllers/front/cron.php` is the only cron controller; 5 dispatch cases (`cron.php:83-116`) |
| 45-47 | Shared secret in the `token` query parameter compared with configuration key `PACKETERY_CRON_TOKEN` | CONFIRMED | `cron.php:182-192` `Tools::getValue('token')` vs `ConfigHelper::get('PACKETERY_CRON_TOKEN')`, strict `===` |
| 47-49 | A request without a matching secret, and one without a `task` value, both end with a rendered error row and no work | CONFIRMED | `cron.php:45-56` — both paths call `renderError()` and `return` |
| 53 | Controller row: validates the secret, resolves the task from the container, prints BEGIN and END rows | CONFIRMED | `cron.php:45`, `:85/:90/:95/:100/:105` `$this->module->diContainer->get(...)`, `:75-81` BEGIN, `:122-128` END |
| 54 | `cron?task=DeleteLabels` with `number_of_days`, `number_of_files`; removes label PDFs older than N days, in batches | CONFIRMED | `DeleteLabels.php:53-54` reads both values; `:67-71` filters by mtime and `array_slice($files, 0, $deleteNumberOfFiles)` (one batch per run, as the limitations section states) |
| 55 | `cron?task=DownloadCarriers` runs the carrier feed download, reports only a `danger` result as an error | CONFIRMED | `DownloadCarriers.php:42-47` `if ($result['class'] === 'danger') return [$result['text']];` |
| 56 | `cron?task=UpdatePacketStatus` runs `PacketTrackingCron` over the selected orders | CONFIRMED | `UpdatePacketStatus.php:39-44` |
| 57 | `cron?task=GetConsignPassword` with `max_orders`, `max_order_age_days`; fetches missing consignment codes for recent orders | CONFIRMED | `cron.php:106-109` passes both; `GetConsignPassword.php:68` `getOrdersMissingConsignPassword($maxOrders, $maxOrderAgeDays)`, `:87` `fetchFromApi(...)` |
| 58 | `cron?task=PurgeLogs` with `log_expiration_days`; deletes module log rows older than N days | CONFIRMED | `PurgeLogs.php:43-45`; `LogRepository::purge()` `delete(... '`date` < DATE_SUB(NOW(), INTERVAL ' . (int) $logExpirationDays . ' DAY)')` |
| 60-62 | Sets `ajax` true and writes every message through a Smarty row template, so the response is HTML fragments, not JSON | CONFIRMED | `cron.php:24` `public $ajax = true;`; `:152-161` renders `views/templates/front/cron-message-row.tpl`, whose body is `<p>{$message}</p>` |
| 62-63 | Client aborts are ignored for the whole run | CONFIRMED | `cron.php:43` `ignore_user_abort(true);` as the first statement of `display()` |
| 63-65 | Every error row is also written to the PrestaShop log | CONFIRMED | `cron.php:174` `PrestaShopLogger::addLog('[packetery:cron]: ' . $message, 3, ...)` inside `renderError()` |
| 67 | 40 public methods on 15 types | CONFIRMED | 15 classes (8 in `libs/PacketTracking`, 6 in `libs/Cron/Tasks` incl. `Base`, plus the controller); public methods counted 25 + 11 + 4 = 40 |
| 68 | `PacketStatusFactory::getPacketStatuses` is used by other modules | CONFIRMED | called from `Order/OrderDetailView.php`, `Order/OrderStatusChangeFormService.php`, `controllers/admin/PacketeryOrderGridController.php` |
| 69-71 | `PacketTrackingRepository::getLastStatusCodeByOrderAndPacketId` returns the last stored status of a packet | CONFIRMED | `PacketTrackingRepository.php:70-81` `ORDER BY \`event_datetime\` DESC`; callers in `packetery.php:1249`, `OrderDetailView.php:49`, `PacketeryOrderGridController.php:799` |
| 71-73 | `PacketStatusTrackingFormService` is the form definition keyed by `submitPacketStatusTrackingSubmit` | CONFIRMED | `PacketStatusTrackingFormService.php:19` `public const SUBMIT_ACTION_KEY = 'submitPacketStatusTrackingSubmit';` |
| 77-79 | `PacketTrackingCron::run` stops early unless `PACKETERY_PACKET_STATUS_TRACKING_ENABLED` is set | CONFIRMED | `PacketTrackingCron.php:72-78` |
| 80-82 | Reads `PACKETERY_PACKET_STATUS_TRACKING_ORDER_STATES`, a JSON map, and keeps the ids whose value is `on` | CONFIRMED | `PacketTrackingCron.php:80-86` `json_decode(...)`, `array_keys($orderStatuses, 'on', true)` |
| 83-84 | An empty selection ends the run with `getNoOrderStatusesMessage` | CONFIRMED | `PacketTrackingCron.php:82-89`, method at `:235-241` |
| 86-89 | Candidate orders selected by order states, final statuses to skip, `MAX_PROCESSED_ORDERS` and an age limit from `MAX_ORDER_AGE_DAYS` | CONFIRMED | `PacketTrackingCron.php:97-106`; `OrderRepository::getOrdersByStateAndLastUpdate($enabledOrderStatuses, $finalStatusIds, $maxProcessedOrdersLimit, $oldestOrderDate)` at `OrderRepository.php:572` |
| 90-91 | The set of final statuses comes from every catalogue entry whose `isFinal` flag is true | CONFIRMED | `PacketTrackingCron.php:246-258` `getFinalStatusIds()` |
| 93-96 | Per order the outcome is logged under `ACTION_PACKET_TRACKING`, success with the response or error with the fault string | CONFIRMED | `PacketTrackingCron.php:116-132`; `LogRepository::ACTION_PACKET_TRACKING = 'packet-tracking'` (`LogRepository.php:25`), `STATUS_SUCCESS`/`STATUS_ERROR` |
| 96-99 | An order whose last record is neither final nor selected in `…_PACKET_STATUSES` is skipped | CONFIRMED | `PacketTrackingCron.php:146-151` `!in_array($lastRecord->statusCode, $finalStatusIds, true) && !in_array(..., array_keys($packetStatuses, 'on', true), false)` → `continue` |
| 101-102 | Both API and stored records are converted into `PacketStatusRecord` objects | CONFIRMED | `PacketTrackingCron.php:154-162` via `PacketStatusRecordFactory::createFromSoapApi` / `createFromDatabase` |
| 102-104 | The comparator reports a difference as soon as one API hash is missing from the stored set | CONFIRMED | `PacketStatusComparator.php:32-38` returns `true` on the first `!isset($databasePacketStatusHashes[$hash])` |
| 105-106 | On a difference the stored rows of the order are deleted and the whole API history is inserted again | CONFIRMED | `PacketTrackingCron.php:170-192` `delete($order['id_order'])` then insert of every `$statusRecords->record` |
| 106-108 | With `PACKETERY_ORDER_STATUS_CHANGE_ENABLED` the order moves to the state configured under `PACKETERY_ORDER_STATUS_CHANGE_` + last status code | CONFIRMED | `PacketTrackingCron.php:108`, `:193-195`, `:215` `ConfigHelper::get('PACKETERY_ORDER_STATUS_CHANGE_' . $lastStatusCode)`, `:229` `$order->setCurrentState(...)` |
| 108-110 | The order is stamped with the current tracking timestamp | CONFIRMED | `PacketTrackingCron.php:197` `setLastUpdateTrackingStatus(new \DateTimeImmutable('now'), ...)` |
| 114-115 | Stores one table and holds two in-memory record types | CONFIRMED | only `packetery_packet_status` (`PacketTrackingRepository.php:18`); record types `PacketStatus` and `PacketStatusRecord` |
| 115-117 | The table is created and dropped by the module repository; rows are a local copy of the tracking history | CONFIRMED | `PacketTrackingRepository.php:86-107` `getDropTableSql()` / `getCreateTableSql()` |
| 121 | `id` int unsigned, auto increment, primary key | CONFIRMED | `` `id` int unsigned NOT NULL AUTO_INCREMENT PRIMARY KEY `` |
| 122 | `id_order` int unsigned, indexed | CONFIRMED | `` `id_order` int unsigned NOT NULL ``, `` KEY `id_order` (`id_order`) `` |
| 123 | `packet_id` varchar(15), indexed | CONFIRMED | `` `packet_id` varchar(15) NOT NULL ``, `` KEY `packet_id` (`packet_id`) `` |
| 124 | `event_datetime` datetime, used for the newest-first ordering | CONFIRMED | column in DDL; `PacketTrackingRepository.php:74` `ORDER BY \`event_datetime\` DESC` |
| 125 | `status_code` tinyint unsigned, one of the catalogue ids | CONFIRMED | `` `status_code` tinyint unsigned NOT NULL ``; ids in `PacketStatus.php:16-31` |
| 126 | `status_text` text, the text Packeta sent | CONFIRMED | `` `status_text` text NOT NULL ``; `insert()` writes `'status_text' => $statusText` from the API record |
| 127 | `created_at` datetime defaulting to insert time, written by the database, never by the module | CONFIRMED | `` `created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ``; `insert()` (`:52-61`) does not set it |
| 129-130 | `PacketStatus` is id, machine code, translated name, final flag | CONFIRMED | `PacketStatus.php:53-83` |
| 130-132 | Sixteen entries from `RECEIVED_DATA` to `UNKNOWN`; four final: `DELIVERED`, `RETURNED`, `CANCELLED`, `UNKNOWN` | CONFIRMED | `PacketStatusFactory.php:34-131` — 16 entries; `true` passed only for DELIVERED (`:75`), RETURNED (`:87`), CANCELLED (`:93`), UNKNOWN (`:129`) |
| 133-134 | Only the non-final entries are offered as tracking triggers in the configuration form | CONFIRMED | `PacketStatusTrackingFormService.php:115-118` `if ($packetStatus->isFinal() === true) { continue; }` |
| 136-138 | `PacketStatusRecord` is date + status code + status text, its only behaviour is the hash over all three | CONFIRMED | `PacketStatusRecord.php:38-41` `md5($dateTime->format('Y-m-d H:i:s') . '|' . $statusCode . '|' . $statusText)`; no other public method besides the constructor |
| 138-140 | The same record type is built from an API response and from a database row | CONFIRMED | `PacketStatusRecordFactory.php:23` / `:39` |
| 144-145 | Reaches two Packeta systems, both through services living in other modules | CONFIRMED | `Packetery\Module\SoapApi` (`libs/Module`) and `Packetery\ApiCarrier\Downloader` (`libs/ApiCarrier`) |
| 147 | calls → packeta-soap-api (sync, SOAP) | CONFIRMED | `SoapApi.php:251-253` `new \SoapClient($this->resolveWsdlUrl())`, `$client->packetTracking(...)` |
| 148 | calls → packeta-pickup-point-api (sync, HTTP/JSON) | CONFIRMED | `ApiCarrier/Downloader.php:21` private const API_URL — pickup-point JSON endpoint fetched over `HttpClientWrapper` |
| 150-151 | The tracking run sends the tracking number and receives the packet history | CONFIRMED | `PacketTrackingCron.php:110`; `SoapApi::getPacketTracking($packetId)` |
| 151-153 | The consignment-code task sends the same identifier and receives the `consignPassword` | CONFIRMED | `GetConsignPassword.php:87` → `ConsignPasswordProvider::fetchFromApi()` `:43` `$this->soapApi->getPacketInfo($packetId)`, `:62` `return $response->getConsignPassword();` |
| 153-155 | The carrier task only triggers the download and forwards the failure text | CONFIRMED | `DownloadCarriers.php:42-47` |
| 155-156 | The endpoint values are not repeated here | CONFIRMED | no URL or host appears anywhere in the document |
| 158-159 | Inside the repository the module is wired through the module container only | CONFIRMED | all collaborators are constructor-injected and resolved via `$this->module->diContainer->get(...)` (`cron.php:85-105`); note the static exceptions `ConfigHelper::get()` and `ConsignPasswordSettings::fromConfig()` bypass the container |
| 159-161 | Reads and stamps orders through `Packetery\Order\OrderRepository` | CONFIRMED | `GetConsignPassword.php:68` `getOrdersMissingConsignPassword()`; `PacketTrackingCron.php:197` `setLastUpdateTrackingStatus()` |
| 161-162 | Writes API traffic through `Packetery\Log\LogRepository` and purges the same store | CONFIRMED | `PacketTrackingCron.php:116/125` `insertRow(...)`; `PurgeLogs.php:45` `$this->logRepository->purge($logExpirationDays)` |
| 162-163 | Reads its settings through `Packetery\Tools\ConfigHelper` | CONFIRMED | `PacketTrackingCron.php:72/80/91/97/104/108/215` `ConfigHelper::get(...)`; `ConfigHelper::get()` delegates to `\Configuration::get()` |
| 163-164 | …and reads its settings through `Packetery\Tools\DbTools` | INCORRECT | `DbTools` is the SQL access layer, not a settings source: `PacketTrackingRepository.php` uses it only as `$this->dbTools->insert/getValue/delete/getRows` against `packetery_packet_status`. No configuration value is read through it |
| 164-167 | The consignment-code task first asks `ConsignPasswordSettings` whether the cron mode is configured and reports a message instead of working when it is not | CONFIRMED | `GetConsignPassword.php:50-57` `if (ConsignPasswordSettings::fromConfig()->isCron() === false) { return [ ... ]; }` |
| 169-170 | The order detail view renders the stored history | INCORRECT | `OrderDetailView::addPacketStatus()` (`OrderDetailView.php:43-62`) reads **one** value — `getLastStatusCodeByOrderAndPacketId(...)` — and assigns `packetStatusTranslatedCode` / `statusCssClass`. The history reader `getPacketStatusesByOrderId()` has exactly one caller, `PacketTrackingCron.php:158`; no view renders the stored history |
| 170-172 | The order grid translates a stored status code for display | CONFIRMED | `PacketeryOrderGridController.php:752-763` `getTranslatedPacketStatus()` maps the code through `getPacketStatuses()` |
| 173-174 | The order status form builds one select per catalogue entry | CONFIRMED | `OrderStatusChangeFormService.php:58-71` loops the catalogue and creates a `select` per `PACKETERY_ORDER_STATUS_CHANGE_<id>` |
| 175-176 | The installer creates the table | CONFIRMED | `libs/Module/Installer.php:226-228` `$packetTrackingRepository->getDropTableSql()` / `getCreateTableSql()` |
| 178-182 | Platform coupling: controller extends `ModuleFrontController`, renders through Smarty, tasks resolved from the container, configuration through `Configuration`, order state change uses `Order`/`OrderState` | CONFIRMED | `cron.php:18`, `:155`, `:85-105`; `ConfigHelper::get()` → `\Configuration::get()`; `PacketTrackingCron.php:217/223` `new \Order(...)`, `new \OrderState(...)` |
| 186-189 | Comparison is all or nothing; the hash covers the status text, so a changed text alone triggers a full rewrite | CONFIRMED | `PacketStatusComparator.php:35-37` + `PacketTrackingCron.php:170-192`; `PacketStatusRecord.php:40` hashes the status text |
| 191-194 | `updateOrderStatus` returns without a message and without a log entry when the order or the target state does not load | CONFIRMED | `PacketTrackingCron.php:217-227` two bare `return;` after `Validate::isLoadedObject(...) === false`; a missing `PACKETERY_ORDER_STATUS_CHANGE_<code>` yields an unloadable `\OrderState` |
| 195-197 | The consignment-code task catches API and database failures per order and returns an empty message list | CONFIRMED | `GetConsignPassword.php:91-95` `catch (ApiClientException) { continue; } catch (DatabaseException) { continue; }`, `:98` `return [];` |
| 199-201 | The controller reads `max_orders` and `max_order_age_days` from the request | CONFIRMED | `cron.php:107-108` `Tools::getValue('max_orders', ...)`, `Tools::getValue('max_order_age_days', ...)` |
| 201-203 | `DeleteLabels` and `PurgeLogs` read their own parameters from the request inside `execute` | CONFIRMED | `DeleteLabels.php:53-54`; `PurgeLogs.php:43` — both use `Tools::getValue(...)` inside `execute()`, which takes no arguments |
| 205-206 | `PurgeLogs` is reachable through the controller but the configuration page builds no URL for it, unlike the four other tasks | CONFIRMED | `cron.php:94-98` dispatches it; `packetery.php:500-556` builds URLs only for DeleteLabels, DownloadCarriers, UpdatePacketStatus, GetConsignPassword |
| 206-209 | The label cleanup deletes at most one batch per run; the batch size falls back to a constant when the request value is missing or not positive | CONFIRMED | `DeleteLabels.php:54` default `DEFAULT_NUMBER_OF_FILES = 500`, `:60-62` `if ($deleteNumberOfFiles <= 0)`, `:71` `array_slice($files, 0, $deleteNumberOfFiles)` |
| 209-211 | The task lists the whole label directory with `glob` on every run | CONFIRMED | `DeleteLabels.php:51` `glob(PACKETERY_PLUGIN_DIR . '/labels/*.pdf', GLOB_NOSORT)` before any filtering |

### manifest.part.packetery-packet-tracking.yaml

| entry | class | evidence |
|---|---|---|
| `service: prestashop` / `source_commit: b85243b…` | CONFIRMED | `git log -1` → `b85243bbeb5f9f91c0bde739f5195b2285b867f9`, matches the document front matter |
| assumption 1 — generated on branch `docs/packetery` at that commit, working tree clean | CONFIRMED | `git rev-parse --abbrev-ref HEAD` → `docs/packetery`; `git status --porcelain` shows only untracked `ai-docs/` output, no tracked modification |
| assumption 2 — the module is `libs/PacketTracking` + `libs/Cron` + the `cron` front controller, and nothing in the code declares that grouping | CONFIRMED | no grouping metadata in the repo; the three paths are the ones the document covers |
| assumption 3 — every table lives in the shop MySQL database, name configured outside the module, recorded as `mysql/prestashop` | CONFIRMED | `PacketTrackingRepository.php:38` `_DB_PREFIX_ . self::$tableName`; all access through PrestaShop `DbTools` |
| assumption 4 — five cron task names as separate `exposes` entries; the code registers one front controller only | CONFIRMED | one file `controllers/front/cron.php`, dispatch on `Tools::getValue('task')` with five cases |
| assumption 5 — "The SOAP calls of `GetConsignPassword` and `DownloadCarriers`…" | INCORRECT | `DownloadCarriers` makes no SOAP call. `ApiCarrier/Downloader.php:21` uses a pickup-point JSON endpoint over `HttpClientWrapper` (REST) — which this same fragment states correctly in its own `calls` entry (`kind: rest`, `packeta-pickup-point-api`). Only `GetConsignPassword` reaches SOAP (`ConsignPasswordProvider.php:43` `$this->soapApi->getPacketInfo(...)`) |
| exposes `cron` (kind html, auth `token` vs `PACKETERY_CRON_TOKEN`, requires `task`) | CONFIRMED | `cron.php:180-193` auth; `:51-56` requires `task`; `:24` `$ajax = true`, `:152-161` renders `cron-message-row.tpl` (`<p>{$message}</p>`) → HTML |
| exposes `cron?task=DeleteLabels` (+ note on `number_of_days`, `number_of_files` read inside the task) | CONFIRMED | dispatched at `cron.php:84`; `DeleteLabels::getTaskName()` = `DeleteLabels`; parameters read in `DeleteLabels.php:53-54` |
| exposes `cron?task=DownloadCarriers` | CONFIRMED | dispatched at `cron.php:89`; task name = short class name |
| exposes `cron?task=UpdatePacketStatus` | CONFIRMED | dispatched at `cron.php:99` |
| exposes `cron?task=GetConsignPassword` (+ note that `max_orders`, `max_order_age_days` are read by the controller) | CONFIRMED | dispatched at `cron.php:104`; parameters read at `cron.php:107-108` and passed into `execute()` |
| exposes `cron?task=PurgeLogs` (+ note `log_expiration_days`; no configuration-page URL) | CONFIRMED | dispatched at `cron.php:94`; `PurgeLogs.php:43` reads the parameter; `packetery.php:500-556` builds no URL for it |
| auth line, identical on all six entries | CONFIRMED | `cron.php:182-192` — `Tools::getValue('token')` strictly compared with `ConfigHelper::get('PACKETERY_CRON_TOKEN')`; no other check, no per-task authorisation. The six entries dispatch exactly the five task names the controller knows, plus the bare controller |
| calls → packeta-soap-api, packet tracking history | CONFIRMED | `PacketTrackingCron.php:110`; `SoapApi.php:249-259` `$client->packetTracking(...)` |
| calls → packeta-soap-api, consignment code | CONFIRMED | `GetConsignPassword.php:87` → `ConsignPasswordProvider.php:43` `getPacketInfo(...)`, `:62` `getConsignPassword()` |
| calls → packeta-pickup-point-api (rest), carrier feed refresh | CONFIRMED | `DownloadCarriers.php:42` `$this->downloader->run()`; `Downloader.php:21` JSON pickup-point endpoint via `HttpClientWrapper` |
| reads_external_data `packetery_packet_status` (owner packeta-soap-api, via api packetTracking, store mysql/prestashop) | CONFIRMED | API records inserted into the local table at `PacketTrackingCron.php:176-191` via `PacketTrackingRepository::insert()` |
| reads_external_data `consignPassword` (owner packeta-soap-api, via api packetInfo, store mysql/prestashop) | CONFIRMED | `ConsignPasswordProvider.php:43` `getPacketInfo()`; stored by `OrderRepository::setConsignPassword()` into `packetery_order` |
| unknown 1 — which scheduler calls the `cron` controller and how often | CONFIRMED | nothing in the repo schedules it (no cron hook in `getModuleHooksList()`, no crontab/CI entry); the question is genuinely open |
| unknown 2 — which systems may call the cron route (`consumers: [unknown]`) | CONFIRMED | the only gate is the shared token (`cron.php:180-193`); no allow-list or caller identification in the code |
| unknown 3 — whether the missing configuration-page URL for `PurgeLogs` is intended | CONFIRMED | the premise holds: `cron.php:94` dispatches it, `packetery.php:500-556` builds no URL |

CONFIRMED 93 · INCORRECT 5 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/reference/packetery-packet-tracking.md` (and in its manifest fragment where the row pointed there). `confidence` was set to `reviewed`; `verified` needs another pass by someone who did not write or fix the document.
