## ai-docs/reference/packetery-module.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 26-27 | The `Packetery` class extends `CarrierModule` | CONFIRMED | `packetery/packetery.php:19` `class Packetery extends CarrierModule` |
| 28 | declares the module name `packetery` | CONFIRMED | `packetery/packetery.php:72` `$this->name = 'packetery';` |
| 28 | declares its version | CONFIRMED | `packetery/packetery.php:74` `$this->version = '3.4.0';` |
| 29 | supported PrestaShop range [VERIFY: packetery/packetery.php#ps_versions_compliancy] | CONFIRMED | `packetery/packetery.php:100` `$this->ps_versions_compliancy = ['min' => '1.7.7.0', 'max' => _PS_VERSION_];` |
| 29-31 | builds a dependency-injection container in its constructor [VERIFY: packetery/libs/DI/ContainerFactory.php#create] | CONFIRMED | `packetery/packetery.php:79` `$this->diContainer = Packetery\DI\ContainerFactory::create($this);`; `ContainerFactory::create()` returns a `Container` with `Db` and `ControllerCore` factories |
| 30-31 | every other module of this repository is reached through `diContainer` instead of through `new` | CONFIRMED | no `new Packetery\…` occurrence in `packetery/packetery.php`; all module classes come from `$this->diContainer->get(...)` |
| 33-35 | owns the install and uninstall lifecycle — database schema, configuration keys, admin menu tabs, hook registration [VERIFY: packetery/libs/Module/Installer.php#run] | CONFIRMED | `Installer::run()` chains `updateConfiguration()`, `installDatabase()`, `registerHook(getModuleHooksList())`, `insertMenuItems()` |
| 35-37 | `getModuleHooksList` names the hooks the module registers, and the handler methods on `Packetery` receive them [VERIFY: packetery/packetery.php#getModuleHooksList] | CONFIRMED | `packetery/packetery.php:1422` returns 19 hook names + one version-dependent one; matching `hook…` methods exist on the class |
| 37-39 | owns the module configuration screen: option map, validation of entered values, rendered form [VERIFY: packetery/packetery.php#getConfigurationOptions] | CONFIRMED | `getConfigurationOptions()` (:561) is the option map, `getContent()` validates through `Options::validate`, `displayForm()` renders |
| 40-42 | owns **the two** clients that talk out of the shop: `SoapApi` and `VersionChecker` — framed as a responsibility "no other module of this repository holds" | INCORRECT | other modules also call out: `packetery/libs/ApiCarrier/Downloader.php:21` holds an HTTPS Packeta pickup-point API endpoint constant and calls `$this->httpClient->get($url)` (:122), and `packetery/libs/PickupPointValidate/PickupPointValidator.php` uses `HttpClientWrapper`. The sibling fragments confirm it: `manifest.part.packetery-carrier.yaml:12` `- service: packeta-pickup-point-api`, `manifest.part.packetery-checkout.yaml:34` `- service: packeta-widget` |
| 44-45 | a per-product age-verification flag edited in the product detail of the back office [VERIFY: packetery/packetery.php#hookDisplayAdminProductsExtra] | CONFIRMED | `packetery/packetery.php:1789` assigns `packeteryAgeVerification` and renders `displayAdminProductExtra.tpl` |
| 46-47 | the class-level autoloader maps the `Packetery` namespace onto `packetery/libs/` [VERIFY: packetery/autoload.php#spl_autoload_register] | CONFIRMED | `packetery/autoload.php:12-20` `__DIR__ . '/libs/' . str_replace(['\\', 'Packetery'], ['/', ''], $class) . '.php'` |
| 49 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 53 | exposes 111 public methods on 20 types | CONFIRMED | `ai-docs/ast/map.json` over the `covers` paths minus `index.php` stubs and `upgrade/`: 20 types, 111 listed (public) methods |
| 53-55 | the `Packetery` class is the interface PrestaShop calls; the rest are getters, helpers and the per-hook classes in `packetery/libs/Hooks/` | CONFIRMED | hook methods live on `Packetery`; `packetery/libs/Hooks/` holds `SendMailAlterTemplateVars`, `ActionObjectOrderUpdateBefore`, `ActionValidateStepComplete` |
| 59 | `install`/`uninstall`: refuse to install without cURL, then delegate to `Installer` and `Uninstaller` [VERIFY: packetery/libs/Module/Installer.php#run] | CONFIRMED | `packetery/packetery.php:114` `if (extension_loaded('curl') === false) { … return false; }`; :121 `$installer->run($this)`; :135 `$uninstaller->run()` |
| 60 | `getContent`: renders the settings form, applies the three submit actions, reports SOAP absence and a newer release [VERIFY: packetery/packetery.php#getContent] | CONFIRMED | :275 soap check, :280 `isNewVersionAvailable`, submits for `orderStatusChangeFormService`, `packetStatusTrackingFormService` and `submit.$this->name`, then `$output .= $this->displayForm();` |
| 61 | `displayForm`: builds a `HelperForm` from the option map and appends the packet-tracking, order-status and cron blocks [VERIFY: packetery/packetery.php#displayForm] | CONFIRMED | :475-495 `$helper->generateForm([$form]) . $packetStatusTrackingFormService->generateForm(…) . $orderStatusChangeFormService->generateForm(…) . $this->generateCronInfoBlock()` |
| 62 | `getModuleHooksList`: hook names registered at install; the list branches on the PrestaShop version for the admin order hook [VERIFY: packetery/packetery.php#getModuleHooksList] | CONFIRMED | :1446 `if (Tools::version_compare(_PS_VERSION_, '1.7.7', '<')) { $hooks[] = 'displayAdminOrderLeft'; } else { $hooks[] = 'displayAdminOrderMain'; }` |
| 63 | `hookDisplayCarrierExtraContent`: renders the pickup-point widget block or the address-validation block from the saved order row [VERIFY: packetery/packetery.php#hookDisplayCarrierExtraContent] | CONFIRMED | :797 `$orderRepository->getByCartAndCarrier(...)`, then `widgetHd.tpl` for address delivery or `widget.tpl` for pickup points |
| 64 | `packeteryHookDisplayAdminOrder`: packet status, widget options, weight, prices, submit and cancel buttons [VERIFY: packetery/packetery.php#packeteryHookDisplayAdminOrder] | CONFIRMED | :1056-1262 assigns `widgetOptions`, `orderWeight`, `total`/`cod`, `postParcelButtonAllowed`, `showCancelButton`, `$orderDetailView->addPacketStatus(...)` |
| 65 | `hookActionValidateOrder`: hands cart and order to the order saver, or logs the parameter types and returns [VERIFY: packetery/packetery.php#hookActionValidateOrder] | CONFIRMED | :1028-1044 `gettype($params['cart'])` log then `return;`, else `$orderSaver->saveNewOrder(...)` |
| 66 | `hookActionProductUpdate`: inserts or updates the age-verification row [VERIFY: packetery/packetery.php#hookActionProductUpdate] | CONFIRMED | :1855-1866 `$productAttributeRepository->update(...)` / `->insert(...)` |
| 67 | `Container::get`/`register`: resolves by reflection over constructor parameter types and caches one instance per class [VERIFY: packetery/libs/DI/Container.php#getParamInstances] | CONFIRMED | `Container::get()` caches in `$this->services[$class]`; `getParamInstances()` reflects constructor parameter types |
| 68 | `SoapApi` methods: `senderGetReturnRouting`, `getPacketInfo`, `packetCarrierNumber`, `getPacketTracking`, `getPacketsLabelsPdf`, `getPacketsCourierLabelsPdf`, `cancelPacket`, `createShipment`, `barcodePng` [VERIFY: packetery/libs/Module/SoapApi.php#senderGetReturnRouting] | CONFIRMED | all nine exist in `packetery/libs/Module/SoapApi.php` (:67, :91, :161, :249, :268, :294, :316, :330, :349); no completeness claim is made — three further public methods (`resolveWsdlUrl`, `getTrackingUrlInProperLanguage`, `getPacketIdsWithCarrierNumbers`) are not listed |
| 70-73 | `hookSendMailAlterTemplateVars`, `hookActionObjectOrderUpdateBefore`, `hookActionValidateStepComplete` only resolve a class from `packetery/libs/Hooks/` and call its `execute` [VERIFY: packetery/libs/Hooks/ActionValidateStepComplete.php#execute] | CONFIRMED | :1542, :1552, :1685 do exactly that; `hookActionValidateStepComplete` additionally pushes the returned error into `$this->context->controller->errors[]` |
| 73-75 | `AbstractFormService` builds a `HelperForm`, fills it from the stored configuration and persists the submitted values through `Options` [VERIFY: packetery/libs/AbstractFormService.php#generateForm] | CONFIRMED | `generateForm()` → `createHelperForm()` + `fillHelperForm()` (reads via `ConfigHelper::getMultiple`); `persistFormData()` calls `$this->options->formatOption()` / `validate()` before `ConfigHelper::update` |
| 75-77 | the templates rendered by these handlers live in `packetery/views/templates/hook/`, the admin order panel among them [VERIFY: packetery/views/templates/hook/displayOrderMain.tpl#packetaPickupPointChange] | CONFIRMED | directory holds `displayOrderMain.tpl`, `displayAdminProductExtra.tpl`, `display_order_confirmation.tpl`, `display_order_detail.tpl`, `displayOrderListFooter.tpl`; `displayOrderMain.tpl:6` `id="packetaPickupPointChange"` |
| 81-83 | install in four steps, a failure of any step fails the install: configuration keys, tables, hooks, menu items [VERIFY: packetery/libs/Module/Installer.php#run] | CONFIRMED | `Installer::run()` is a single `&&` chain in exactly that order |
| 83-86 | schema step drops and recreates `packetery_order`, `packetery_payment`, `packetery_address_delivery` inline, remaining tables come from the owning repositories with their own create/drop [VERIFY: packetery/libs/Module/Installer.php#installDatabase] | CONFIRMED | `installDatabase()` inlines the three DDL blocks, then `getDropTableSql()`/`getCreateTableSql()` of `ProductAttributeRepository`, `ApiCarrierRepository`, `LogRepository`, `PacketTrackingRepository` |
| 86-88 | configuration step writes default label formats, widget settings, generated cron token, default package price and weight, consignment-code mode [VERIFY: packetery/libs/Module/Installer.php#updateConfiguration] | CONFIRMED | `updateConfiguration()` writes `PACKETERY_LABEL_FORMAT`, `PACKETERY_CARRIER_LABEL_FORMAT`, `PACKETERY_WIDGET_AUTOOPEN`, `KEY_WIDGET_VALIDATION_MODE`, `PACKETERY_CRON_TOKEN` (`\Tools::passwdGen(32)`), the price/weight defaults and `KEY_CONSIGN_PASSWORD_RETRIEVAL` |
| 89-91 | menu step adds a `Packetery` tab under `SELL` and four child tabs, names translated only for `TRANSLATED_LANGUAGES` [VERIFY: packetery/libs/Module/Installer.php#insertMenuItems] | CONFIRMED | `insertMenuItems()` menu config + `createMultiLangField()` `in_array($language['iso_code'], self::TRANSLATED_LANGUAGES)`; `TRANSLATED_LANGUAGES = ['cs', 'sk']` |
| 93-94 | uninstall in mirror order: five tabs, tables, hooks, configuration keys [VERIFY: packetery/libs/Module/Uninstaller.php#uninstallDatabase] | CONFIRMED | `Uninstaller::run()`: `deleteMenuItems()` (5 `deleteTab` calls) `&& uninstallDatabase() && unregisterHooks() && deleteConfiguration()` |
| 95-96 | the order table is not dropped but renamed to a backup table | CONFIRMED | `Uninstaller::uninstallDatabase()` `RENAME TABLE …packetery_order TO …packetery_order_backup` |
| 96-97 | the shop `carrier` rows that name this module are reset instead of removed | CONFIRMED | `UPDATE …carrier SET is_module = 0, external_module_name = NULL, need_range = 0 WHERE external_module_name = "packetery"` |
| 97-98 | a hook that cannot be unregistered is collected and logged, and the step still reports success [VERIFY: packetery/libs/Module/Uninstaller.php#unregisterHooks] | CONFIRMED | `$failedHooks[]` + `PrestaShopLogger::addLog(...)` then `return true;` |
| 99-100 | the configuration keys deleted at uninstall are fewer than the keys the settings form writes [VERIFY: packetery/libs/Module/Uninstaller.php#deleteConfiguration] | INCORRECT | `deleteConfiguration()` deletes 17 keys; `getConfigurationOptions()` offers 14. Neither the count nor the subset relation holds: the delete list contains five keys the form never writes (`PACKETERY_LAST_CARRIERS_UPDATE`, `PACKETERY_CRON_TOKEN`, `KEY_LAST_VERSION_CHECK_TIMESTAMP`, `KEY_LAST_VERSION`, `KEY_LAST_VERSION_URL`) and omits two the form does write (`KEY_SHOW_CONSIGN_PASSWORD`, `KEY_CONSIGN_PASSWORD_RETRIEVAL`). The manifest fragment states it correctly in its third `unknowns` entry |
| 102-104 | `packetery/upgrade/` holds one file per released version, each declaring a single `upgrade_module_*` function [VERIFY: packetery/upgrade/upgrade-3.4.0.php#upgrade_module_3_4_0] | CONFIRMED | 14 upgrade files, each with exactly one such function; `upgrade-3.4.0.php:17` `function upgrade_module_3_4_0(Packetery $module): bool` |
| 104-107 | the scripts stay outside the 2940 lines of logic counted for this module | CONFIRMED | `ai-docs/ast/map.json`: the `covers` paths total 3433 lines, the 14 `upgrade/` files 493 → 2940 |
| 111 | reaches two systems outside the shop and one data store | CONFIRMED | SOAP (`SoapApi`), GitHub (`VersionChecker`/`ApiClientFacade`), MySQL via `DbTools` |
| 113 | calls → packeta-soap-api (sync, SOAP over HTTP) | CONFIRMED | `SoapApi` creates `\SoapClient` against the WSDL in every method |
| 114 | calls → github-api (sync, HTTP/JSON) | CONFIRMED | `VersionChecker::GITHUB_RELEASES_ENDPOINT_URL` fetched through `ApiClientFacade::getWithGithubAuthorizationToken()`, decoded with `json_decode` |
| 115 | calls → mysql (sync, SQL) | CONFIRMED | `Installer`/`Uninstaller` run DDL through `DbTools::executeQueries`, `ProductAttributeRepository` runs DML |
| 117-119 | `SoapApi` creates a `SoapClient` per call against the WSDL address, overridable with `_PACKETERY_SOAP_WSDL_URL_` [VERIFY: packetery/libs/Module/SoapApi.php#WSDL_URL] | CONFIRMED | `new \SoapClient($this->resolveWsdlUrl())` in each of the nine methods; `resolveWsdlUrl()` returns `_PACKETERY_SOAP_WSDL_URL_` when defined and non-empty, else `self::WSDL_URL` |
| 120 | every call sends the stored API password | INCORRECT | `senderGetReturnRouting($senderIndication, $apiPassword)` sends the password it is handed, not the stored one — `Options::validate()` passes the value from the POST (`$this->soapApi->senderGetReturnRouting('', $value)`). The other eight methods use `$this->configHelper->getApiPass()` |
| 120-122 | faults are mapped to typed responses or to `IncorrectApiPasswordException` and `SenderNotExistsException` [VERIFY: packetery/libs/Module/Options.php#validate] | CONFIRMED | `SoapApi::senderGetReturnRouting()` throws both on `$e->detail->IncorrectApiPasswordFault` / `->SenderNotExists`; other methods fill `PacketInfo::setFault()` etc. |
| 121-122 | the settings validation turns them into a message **next to the field** | INCORRECT | the message is a page-level alert above the whole form: `packetery/packetery.php:414` `$output .= $this->displayError($errorMessage);` and only afterwards `$output .= $this->displayForm();` (:497) |
| 122-126 | `VersionChecker` fetches at most once per `CHECK_INTERVAL_IN_SECONDS`, accepts only the release types in `_PACKETERY_ALLOWED_RELEASE_TYPES_`, stores version, download address and release notes [VERIFY: packetery/libs/Module/VersionChecker.php#checkForUpdate] | CONFIRMED | `shouldCheckApi()` compares against `CHECK_INTERVAL_IN_SECONDS`; `getLatestReleaseResponse()` filters by `$allowedReleaseTypes` (default `['stable']`); `checkForUpdate()` writes `KEY_LAST_VERSION`, `KEY_LAST_VERSION_URL`, `KEY_LAST_RELEASE_NOTES` |
| 126-127 | the request carries a GitHub token when `_GITHUB_ACCESS_TOKEN_` is defined [VERIFY: packetery/libs/Module/ApiClientFacade.php#getWithGithubAuthorizationToken] | CONFIRMED | `$token = defined('_GITHUB_ACCESS_TOKEN_') ? _GITHUB_ACCESS_TOKEN_ : null;` then an `Authorization` header |
| 129-131 | the coupling runs through the container: the `Packetery` class resolves repositories and services of other modules by class name when a hook fires [VERIFY: packetery/packetery.php#packeteryHookDisplayAdminOrder] | CONFIRMED | that handler resolves `PacketCanceller`, `OrderRepository`, `OrderDetailsUpdater`, `CarrierRepository`, `OrderDetailView`, `ConfigHelper`, `Calculator`, `LogRepository`, `OrderExporter`, `PacketTrackingRepository` from `diContainer` |
| 132-134 | `SoapApi` is resolved by the packet submitter of packetery-order [VERIFY: packetery/libs/Order/PacketSubmitter.php#SoapApi] | CONFIRMED | `PacketSubmitter.php:47` constructor parameter `SoapApi $soapApi` |
| 133-134 | …by the tracking cron of packetery-packet-tracking [VERIFY: packetery/libs/PacketTracking/PacketTrackingCron.php#SoapApi] | CONFIRMED | `PacketTrackingCron.php:55` constructor parameter `SoapApi $soapApi` |
| 135-136 | …and by the order grid of packetery-admin-ui [VERIFY: packetery/controllers/admin/PacketeryOrderGridController.php#SoapApi] | CONFIRMED | `PacketeryOrderGridController.php:390, :424, :543` `$this->getModule()->diContainer->get(SoapApi::class)` |
| 137-139 | `Packetery\Module\Cart` is called by the front checkout controller of packetery-checkout to rebuild the carrier extra content on PrestaShop 1.6 [VERIFY: packetery/controllers/front/checkout.php#packeteryCreateExtraContent] | CONFIRMED | `checkout.php:41-42` `$packeteryCart = $this->module->diContainer->get(Cart::class); echo $packeteryCart->packeteryCreateExtraContent();`; `Cart.php:24` "Endpoint is called in PS 1.6 only." |
| 140-141 | `CompanyAddress` is read by the collection-print handler of packetery-order [VERIFY: packetery/libs/Order/CollectionPrintHandler.php#CompanyAddress] | CONFIRMED | `CollectionPrintHandler.php:105-107` `CompanyAddress::fromCountry(…, \Packetery::PACKETA_ADDRESS)` |
| 143-145 | bound to the PrestaShop platform: extends `CarrierModule`, settings through `Configuration`, rendering through Smarty and `HelperForm`, own hook names dispatched in process | CONFIRMED | `ConfigHelper::get/update` wrap `\Configuration::get`/`updateValue`; `new HelperForm()` in `displayForm()`; custom hooks `displayPacketeryOrderGridListAfter`, `actionPacketeryOrderGridListingResultsModifier`, `actionPacketeryCarrierGridListingResultsModifier` in `getModuleHooksList()` |
| 149-150 | source of truth for `packetery_product_attribute` and for the module-level configuration keys the settings form writes | CONFIRMED | `ProductAttributeRepository` owns the DDL and all DML for that table; the form keys are written only from `getContent()`/`Installer` |
| 154 | `packetery_product_attribute.id_product` `int(11)`, primary key; one row per product, deleted with the product [VERIFY: packetery/libs/Product/ProductAttributeRepository.php#getCreateTableSql] | CONFIRMED | `getCreateTableSql()` `` `id_product` int(11) NOT NULL PRIMARY KEY ``; `packetery.php:1868 hookActionProductDelete` → `$productAttributeRepository->delete($params['product']->id)` |
| 155 | `is_adult` `tinyint(1)`, default 0, set from the checkbox in the product detail [VERIFY: packetery/packetery.php#hookActionProductUpdate] | CONFIRMED | `` `is_adult` tinyint(1) NOT NULL DEFAULT 0 ``; `$isAdult = (int) Tools::getIsset('packetery_age_verification');` |
| 156 | `ProductAttributes::isForAdults` bool, read model built from the table row [VERIFY: packetery/libs/Product/ProductAttributes.php#fromDbRow] | CONFIRMED | `fromDbRow(array $dbRow): ProductAttributes` → `new self((bool) $dbRow['is_adult'])`; `isForAdults(): bool` |
| 157 | `CompanyAddress` fields `country`, `company`, `street`, `zip`, `city` (string), built from the country map in the module class, falling back to `DEFAULT_COUNTRY` [VERIFY: packetery/libs/Module/CompanyAddress.php#fromCountry] | CONFIRMED | five `string` properties; `fromCountry()` falls back to `self::DEFAULT_COUNTRY` (`'CZ'`) both when the ISO cannot be resolved and when it is not in the map |
| 159 | the configuration keys are ordinary PrestaShop `Configuration` rows | CONFIRMED | `ConfigHelper::get()`/`update()` delegate to `\Configuration::get`/`\Configuration::updateValue` |
| 159-166 | the settings form writes `PACKETERY_APIPASS`, `PACKETERY_ESHOP_ID`, `PACKETERY_LABEL_FORMAT`, `PACKETERY_CARRIER_LABEL_FORMAT`, `PACKETERY_LABEL_NOTE`, `PACKETERY_ID_PREFERENCE`, `PACKETERY_WIDGET_AUTOOPEN`, `PACKETERY_WIDGET_VALIDATION_MODE`, the package price/weight defaults, `PACKETERY_USE_PS_CURRENCY_CONVERSION`, `PACKETERY_SHOW_CONSIGN_PASSWORD`, `PACKETERY_CONSIGN_PASSWORD_RETRIEVAL` [VERIFY: packetery/packetery.php#getConfigurationOptions] | CONFIRMED | all 14 appear in `getConfigurationOptions()` (partly via the `ConfigHelper::KEY_*` constants, whose values match the names given) and are written by the `submit`-branch of `getContent()` |
| 166-167 | **three** further keys are written by the code and never shown in the form | INCORRECT | the sentence itself then names five (`PACKETERY_CRON_TOKEN`, `PACKETERY_LAST_CARRIERS_UPDATE`, `PACKETERY_LAST_VERSION`, `PACKETERY_LAST_VERSION_URL`, `PACKETERY_LAST_RELEASE_NOTES`), and the code writes a sixth outside the form: `ConfigHelper::KEY_LAST_VERSION_CHECK_TIMESTAMP` (`Installer::updateConfiguration()`, `VersionChecker::checkForUpdate()`) |
| 167-170 | those keys: the cron token generated at install, `PACKETERY_LAST_CARRIERS_UPDATE`, and the version-check triple that `LatestReleaseResponse` fills [VERIFY: packetery/libs/Module/VersionChecker.php#getLatestReleaseResponse] | CONFIRMED | `Installer::updateConfiguration()` `PACKETERY_CRON_TOKEN`; `packetery/libs/ApiCarrier/Downloader.php:88` `ConfigHelper::update('PACKETERY_LAST_CARRIERS_UPDATE', time())`; `checkForUpdate()` writes the three keys from `$response->getVersion()/getDownloadUrl()/getReleaseNotes()` |
| 171-172 | the address constant is a hardcoded table of five country entries in the module class [VERIFY: packetery/packetery.php#PACKETA_ADDRESS] | CONFIRMED | `packetery/packetery.php:31` `PACKETA_ADDRESS` with `CZ`, `SK`, `HU`, `RO`, `PL` |
| 176-179 | the container resolves constructor parameters by class type only and throws on a builtin type, so a scalar argument needs a factory closure [VERIFY: packetery/libs/DI/Container.php#getParamInstances] | CONFIRMED | `if (!$paramType instanceof \ReflectionNamedType \|\| $paramType->isBuiltin()) { throw new \Exception(self::EXCEPTION_MESSAGE); }`; `register($class, \Closure $factory)` is the escape hatch, used by `ContainerFactory` |
| 179-181 | the module file may not use the PHP `use` keyword because PS 1.6 fails to load the main file with it, which is why every class in `packetery.php` is written with its full name [VERIFY: packetery/packetery.php#ps_versions_compliancy] | CONFIRMED | `packetery/packetery.php:7-9` `Do not use "use" PHP keyword. PS 1.6 can not load main plugin files with the keyword in them.`; the file has no `use` statements and uses fully-qualified names throughout. (The anchor points at `ps_versions_compliancy`; the evidence is the file header comment.) |
| 182-183 | the extra-content toggle passed to the front end is hardcoded to `false` with a TODO [VERIFY: packetery/packetery.php#toggleExtraContent] | CONFIRMED | `packetery/packetery.php:963` `'toggleExtraContent' => false, // TODO: make configurable?` |
| 183-185 | address validation is marked as unfixed for PrestaShop 1.6 in the 1.6-only carrier-content path [VERIFY: packetery/libs/Module/Cart.php#TODO] | CONFIRMED | `Cart.php:39` `// TODO: fix address validation in PS 1.6` inside `packeteryCreateExtraContent()`, documented as "called in PS 1.6 only" |
| 187 | the install refuses to continue without the cURL extension | CONFIRMED | `packetery/packetery.php:114-118` |
| 187-189 | the configuration check also warns when neither cURL nor `allow_url_fopen` is available and when the module directory is not writable [VERIFY: packetery/packetery.php#configurationErrors] | CONFIRMED | `configurationErrors()` `if (!is_writable($fn))` and `if (!self::transportMethod())`; (it also warns when the API password is empty) |
| 189-190 | …which makes both a hard dependency of the pickup-point selection | INCORRECT | `configurationErrors()` only collects strings that the constructor appends to `$this->warning`; nothing blocks. Only the writability message names the feature ("must be writable for the pickup point selection to work properly"); the transport message says "No way to access Packeta API is available on the web server" |
| 190-192 | the configuration page reports the absence of the SOAP extension as an error message rather than blocking the page [VERIFY: packetery/packetery.php#getContent] | CONFIRMED | `if (!extension_loaded('soap')) { $output .= $this->displayError(...); }` and execution continues |
| 192-194 | the API password is validated by length against `API_PASSWORD_LENGTH` and then against the API itself [VERIFY: packetery/libs/Module/Options.php#API_PASSWORD_LENGTH] | CONFIRMED | `API_PASSWORD_LENGTH = 32`; `validate()` returns the length error before calling `senderGetReturnRouting` |
| 196-199 | the version check swallows failures: outside developer mode the exception is logged only when `_PACKETERY_DEBUG_LOG_` is defined and true, and the timestamp is written anyway [VERIFY: packetery/libs/Module/VersionChecker.php#checkForUpdate] | CONFIRMED | the catch block logs under `(_PS_MODE_DEV_ === false) && (_PACKETERY_DEBUG_LOG_ === true)`, rethrows under `_PS_MODE_DEV_ === true`, then `ConfigHelper::update(KEY_LAST_VERSION_CHECK_TIMESTAMP, time()); return;` |
| 199-201 | whether these optional constants are defined does not follow from this repository, and the `unknowns` of the manifest fragment record that | CONFIRMED | `manifest.part.packetery-module.yaml` first `unknowns` entry names all four constants |

### manifest.part.packetery-module.yaml

| entry | class | evidence |
|---|---|---|
| `service: prestashop` | CONFIRMED | repository root module `packetery/` is a PrestaShop module; matches the document front matter `repo: prestashop` |
| `source_commit: b85243bb…` | CONFIRMED | equals HEAD and the document front matter `source-commit` |
| assumption 1 (branch `docs/packetery`, clean working tree) | CONFIRMED | current branch is `docs/packetery`; `git status --porcelain` shows only the untracked generated `ai-docs/` outputs, no modified tracked files |
| assumption 2 (2940 lines, 20 types, 111 public methods from `ai-docs/ast/map.json`, excluding `index.php` stubs and `upgrade/`) | CONFIRMED | recomputed from `map.json` over the `covers` paths: 3433 − 493 (upgrade) = 2940 lines, 20 types, 111 listed public methods |
| assumption 3 (the four admin controllers are listed under `exposes` of packetery-admin-ui) | CONFIRMED | `manifest.part.packetery-admin-ui.yaml:11-35` exposes `PacketeryOrderGrid`, `PacketeryCarrierGrid`, `PacketeryLogGrid`, `PacketerySetting` |
| assumption 4 (install DDL for `packetery_order`, `packetery_payment`, `packetery_address_delivery` lives in `Installer::installDatabase`, but is owned elsewhere) | CONFIRMED | the three inline `CREATE TABLE` blocks are in `Installer::installDatabase()`; reads/writes of those tables live in `packetery/libs/Order/`, `packetery/libs/Payment/`, `packetery/libs/Carrier/` |
| assumption 5 (the shop database is the PrestaShop MySQL database configured outside the module) | CONFIRMED | all access goes through `\Db::getInstance()` registered in `ContainerFactory`; no connection settings in the module |
| `exposes: Packetery::getContent` (html, auth prestashop-admin-token, evidence `packetery/packetery.php#getContent`, notes) | CONFIRMED | `getContent()` is the back-office configuration page reached through `AdminModules` with `Tools::getAdminTokenLite('AdminModules')`; it handles `submit.$this->name` plus the two form-service submits and renders the update notice |
| `calls: packeta-soap-api` (kind other, evidence `SoapApi.php#WSDL_URL`) | CONFIRMED | `SoapApi` covers packet submit/cancel, packet info, tracking, labels, shipment, barcode and the password/sender check |
| `calls: github-api` (kind rest, evidence `VersionChecker.php#checkForUpdate`) | CONFIRMED | `GITHUB_RELEASES_ENDPOINT_URL` is the releases list of the module repository, read to detect a newer version |
| `owns_data: packetery_product_attribute` (store mysql/prestashop, evidence `ProductAttributeRepository.php#getCreateTableSql`) | CONFIRMED | DDL and all DML for the table live in that repository; written from `hookActionProductUpdate`, deleted from `hookActionProductDelete` |
| `reads_external_data: LatestReleaseResponse` (owner github-api, via api releases, evidence `VersionChecker.php#getLatestReleaseResponse`) | CONFIRMED | `getLatestReleaseResponse(): LatestReleaseResponse` builds it from the decoded GitHub release list |
| unknown 1 (optional constants `_PACKETERY_SOAP_WSDL_URL_`, `_PACKETERY_ALLOWED_RELEASE_TYPES_`, `_GITHUB_ACCESS_TOKEN_`, `_PACKETERY_DEBUG_LOG_`) | CONFIRMED | each is read only through `defined(...)` in `SoapApi::resolveWsdlUrl()`, `VersionChecker::getLatestReleaseResponse()`, `ApiClientFacade::getWithGithubAuthorizationToken()`, `VersionChecker::checkForUpdate()`; none is defined in the repository |
| unknown 2 (which systems reach the configuration page) | CONFIRMED | only the admin tab registration and PrestaShop's `AdminModules` rendering are in the code |
| unknown 3 (uninstall deletes fewer keys than the module writes — `PACKETERY_LAST_RELEASE_NOTES`, `PACKETERY_SHOW_CONSIGN_PASSWORD`, `PACKETERY_CONSIGN_PASSWORD_RETRIEVAL` survive) | CONFIRMED | `Uninstaller::deleteConfiguration()` does not list those three keys |

CONFIRMED 85 · INCORRECT 6 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/reference/packetery-module.md` (and in its manifest fragment where the row pointed there). `confidence` was set to `reviewed`; `verified` needs another pass by someone who did not write or fix the document.
