## ai-docs/reference/packetery-order.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17–20 | Writes one `packetery_order` row per cart and order, fills it with the chosen pickup point or the validated home-delivery address, and reads it back to build the packet attributes [VERIFY: packetery/libs/Order/OrderExporter.php#prepareData] | CONFIRMED | `OrderExporter::prepareData()` (OrderExporter.php:45) reads `getWithShopById()` and assembles `$data`; `UNIQUE(id_order)`, `UNIQUE(id_cart)` in Installer.php:190-191 |
| 21–23 | After a successful submission the packet id goes into `tracking_number`, the row is marked `exported`, and the same number is copied into `OrderCarrier` [VERIFY: packetery/libs/Order/PacketSubmitter.php#ordersExport] | CONFIRMED | PacketSubmitter.php:163-171 → `Tracking::updateOrderTrackingNumber()` → `OrderRepository::setTrackingNumber()` writes `['tracking_number' => …, 'exported' => 1]` (OrderRepository.php:451-455); `$carrier->tracking_number = $trackingNumber; $carrier->update();` |
| 26 | Cancellation [VERIFY: packetery/libs/Order/PacketCanceller.php#cancelPacket] | CONFIRMED | `PacketCanceller::cancelPacket()` (PacketCanceller.php:44) |
| 26–27 | Label and carrier-label PDF printing [VERIFY: packetery/libs/Order/Labels.php#packetsLabelsPdf] | CONFIRMED | `Labels::packetsLabelsPdf()` (Labels.php:51), `TYPE_PACKETA` / `TYPE_CARRIER` branches |
| 27–28 | Bill-of-delivery collection print with its barcode [VERIFY: packetery/libs/Order/CollectionPrintHandler.php#renderPrint] | CONFIRMED | `CollectionPrintHandler::renderPrint()` (CollectionPrintHandler.php:70) assigns `barcodeImage` / `barcodeText` and renders `collectionPrint.tpl` |
| 29 | A CSV export in the Packeta import format [VERIFY: packetery/libs/Order/CsvExporter.php#outputCsvExport] | CONFIRMED | CsvExporter.php:104-118 writes the `version 8` marker row and the Packeta import column set (`orderNumber`, `pickupPointOrCarrier`, `affiliateId`, …) |
| 30–31 | Retrieval of the consignment code [VERIFY: packetery/libs/Order/ConsignPasswordProvider.php#fetchFromApi] | CONFIRMED | `ConsignPasswordProvider::fetchFromApi()` (ConsignPasswordProvider.php:41) returns `$response->getConsignPassword()` |
| 33–36 | `BaseResponse` carries the fault identifier and fault string; concrete responses add the payload — label PDF, barcode image, packet info [VERIFY: packetery/libs/Response/BaseResponse.php#hasFault] | CONFIRMED | BaseResponse.php:21,28 + `hasFault()`, `getFault()`, `getFaultString()`; `PacketsLabelsPdfResponse::getPdfContents()`, `BarcodePngResponse::getImage()`, `PacketInfo` |
| 36–39 | `Packetery\Payment` maps a payment module name to the COD flag and converts between order and pickup-point currency [VERIFY: packetery/libs/Payment/PaymentRepository.php#getRateTotal] | CONFIRMED | `PaymentRepository::isCod()` (PaymentRepository.php:67), `getRateTotal()` (PaymentRepository.php:151) "Converts price from order currency to branch currency" |
| 41 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 45–47 | Library module with no route of its own; 36 classes and 140 public methods | CONFIRMED | no controller under the four covered dirs (controllers live in `packetery/controllers`); ai-docs/ast/map.json over the four dirs minus `index.php`: 36 types, 140 methods, 2567 lines — note one of the 36 types is a trait (`Response/InvalidPacketIdsTrait.php`), so strictly 35 classes + 1 trait |
| 51 | `PacketSubmitter::ordersExport(array $orderIds)` skips orders that already have a `tracking_number`, writes the returned number to `packetery_order` and `OrderCarrier`, throws `AggregatedException` with every per-order failure | CONFIRMED | PacketSubmitter.php:144-194 (`continue` at :157, `throw new AggregatedException($errors)` at :190) |
| 52 | `PacketCanceller::cancelPacket(int $orderId, string $packetId)` cancels, clears `tracking_number` and `consign_password`, returns success flag and message | CONFIRMED | PacketCanceller.php:44-89, `return [$cancellationResult, implode(' ', $messages)]` |
| 53 | `Labels::packetsLabelsPdf($packets, $type, $packetsEnhanced, $offset, $fallbackToPacketaLabel)` picks Packeta or carrier call by `$type`, falls back, logs one row per packet, returns the PDF bytes | CONFIRMED | Labels.php:51-114, fallback at :63-65, per-packet `insertRow` at :103-111, `return $response->getPdfContents()` |
| 54 | `CollectionPrintHandler::handleBulkAction(array $orderIds)` / `renderPrint(string $rawIds)` build the auto-submit form variables and render the bill of delivery ending with `exit` | CONFIRMED | CollectionPrintHandler.php:46-65 and :70-124 (`exit;` at :123) |
| 55 | `CollectionPrintService::buildOrdersForPrint(array $orderIds)` drops orders without a `tracking_number`, returns print rows plus the packet id list | CONFIRMED | CollectionPrintService.php:67-70 (`if ($trackingNumber === '') { continue; }`), returns `['ordersForPrint' => …, 'trackingNumbers' => …]` |
| 56 | `BarcodeProvider::getBarcodeData(array $trackingNumbers)` creates a shipment, then fetches its barcode PNG and returns `null` on any fault or empty payload | CONFIRMED | BarcodeProvider.php:37-61 (`createShipment()` then `barcodePng()`; `return null` on faults and on `$barcode === ''` / `$image === ''`) |
| 56 | …"and logs" on any fault **or empty payload** | INCORRECT | only the two fault paths log (`logShipmentFault` :39, `logBarcodeFault` :51). The empty-payload paths return silently: `if ($barcode === '') { return null; }` (:46-47) and `if ($image === '') { return null; }` (:57-59) |
| 57 | `CsvExporter::outputCsvExport(array $orders)` streams the CSV, skips orders that fail to export, marks every exported order `exported` | CONFIRMED | CsvExporter.php:50-52 (`catch (ExportException …) { continue; }`), :90 `setExported(true, $order_id)`, :109-117 streams to `php://output` |
| 58 | `OrderSaver::saveNewOrder(Cart $cart, PrestaShopOrder $order)` / `savePickupPointInCartGetJson()` write the row at order creation, or store the widget's pickup point against the cart and answer in JSON | CONFIRMED | OrderSaver.php:73-81 and :224-241 (`return json_encode($result);`) |
| 59 | `OrderDetailsUpdater::orderUpdate(&$messages, $packeteryOrder, $orderId)` applies pickup point, address, dimensions, weight, prices, age verification, and refuses an already `exported` order | CONFIRMED | OrderDetailsUpdater.php:47-67; `if ($packeteryOrder['exported']) { return $packeteryOrder; }` (:53-55); `$inputConfigs` covers length/height/width/age_verification_required/price_total/price_cod/weight |
| 60 | `OrderExporter::prepareData(\Order $order)` assembles the packet attributes and throws `ExportException` when `id_branch` is missing or the carrier cannot verify age | CONFIRMED | OrderExporter.php:51-53 and :96-98 |
| 62–63 | `OrderRepository` holds the rest of the surface as plain reads/writes over `packetery_order` [VERIFY: …#getWithShopById] | CONFIRMED | `OrderRepository::getWithShopById()` (OrderRepository.php:371); all 33 methods operate on `packetery_order` |
| 64–65 | `CodResolver` resolves and rounds the COD amount [VERIFY: …#roundCodByCurrency] | CONFIRMED | CodResolver.php:27 `resolveCodValue()`, :38 `roundCodByCurrency()` (CZK `ceil`, HUF multiples of 5, else `round(…, 2)`) |
| 65–66 | `OrderNumberResolver` chooses between order id and order reference [VERIFY: …#getPreferredOrderNumber] | CONFIRMED | OrderNumberResolver.php:19-25 |
| 67–68 | `ConsignPasswordSettings` reads the two consignment-code configuration keys into a value object [VERIFY: …#fromConfig] | CONFIRMED | ConsignPasswordSettings.php:30-40 reads `ConfigHelper::KEY_SHOW_CONSIGN_PASSWORD` and `KEY_CONSIGN_PASSWORD_RETRIEVAL` |
| 68–69 | `Ajax` stores a validated home-delivery address against the cart [VERIFY: …#saveAddressInCart] | CONFIRMED | Ajax.php:41-76 (`updateByCart` / `insert` with `is_ad => 1` and the address fields) |
| 70–71 | `OrderStatusChangeFormService` builds the `PACKETERY_ORDER_STATUS_CHANGE_ENABLED` form plus one select per packet status [VERIFY: …#getConfigurationFormFields] | CONFIRMED | OrderStatusChangeFormService.php:58-75 (`'PACKETERY_ORDER_STATUS_CHANGE_' . $packetStatusId` select per status, then the `_ENABLED` radio) |
| 75–76 | Two tables, `packetery_order` and `packetery_payment`, created by the module installer in the shop's MySQL database [VERIFY: packetery/libs/Module/Installer.php#packetery_order] | CONFIRMED | Installer.php:154 and :197 (`CREATE TABLE … ENGINE=InnoDB`) |
| 80 | `id_order`, `id_cart` — `int`, both `UNIQUE`; cart-only row first, `id_order` at order creation | CONFIRMED | Installer.php:155-156, 190-191; OrderSaver.php:208 (insert with `id_cart` only) vs :91-92 (`id_order` on save) |
| 81 | `id_branch` `int`, `name_branch` `varchar(255)`, `currency_branch` `char(3)`; empty `id_branch` makes the export throw | CONFIRMED | Installer.php:157-159; OrderExporter.php:51-53 |
| 82 | `is_ad` `int`, `is_carrier` `tinyint(1)`, `carrier_pickup_point` `varchar(40)` — delivery mode | CONFIRMED | Installer.php:164-166; OrderSaver.php:196-202 (`external` → `is_carrier = 1`, `internal` → `0`) |
| 83 | `country`, `county`, `zip`, `city`, `street`, `house_number`, `latitude`, `longitude` — written only when the new country matches the order's country | CONFIRMED | Installer.php:171-178 (`country` is `varchar(2)`, the rest `varchar(255)`); OrderDetailsUpdater.php:292-299 |
| 84 | `point_place`, `point_street`, `point_city`, `point_zip` — the point's own address, shown when the point is internal | CONFIRMED | Installer.php:184-187; OrderDetailView.php:87-101 (`$isCarrier === false && $isAddressDelivery === false` branch) |
| 85 | `tracking_number` `varchar(15)`, `exported` `tinyint(1)` — the packet id returned by the API | CONFIRMED | Installer.php:161-162 |
| 85 | …"both are set together and cleared together" | INCORRECT | cleared together yes (`clearTrackingNumber()` sets both to `null` / `0`, OrderRepository.php:466-475), but `exported` is also set alone: CsvExporter.php:90 `$orderRepository->setExported(true, $order_id);` — and OrderRepository.php:526 `setExported()` touches only `exported`. The document itself states this in the CSV-export row (line 57) |
| 86 | `carrier_number` `varchar(255)` — the external carrier's own number, used for a carrier label | CONFIRMED | Installer.php:179; `setCarrierNumber()` OrderRepository.php:507; carrier numbers feed `getPacketsCourierLabelsPdf` |
| 87 | `is_cod` `tinyint(1)`, `price_cod`/`price_total` `decimal(20,6)`; `price_cod` overrides the total, `null` `price_total` falls back to the order's paid total | CONFIRMED | Installer.php:160,181-182; CodResolver.php:31-33; OrderExporter.php:155-157 (`$totalPrice = $order->total_paid;`) |
| 88 | `weight` `decimal(20,6)`, `length`/`width`/`height` `int`; dimensions must be whole numbers > 0, weight a number > 0 | CONFIRMED | Installer.php:167-170; OrderDetailsUpdater.php:173-194 and the two messages at :205 / :215 |
| 89 | `packetery_payment` — `module_name` `varchar(255)` primary key, `is_cod` `tinyint(1)`, one row per payment module read by the order saver | CONFIRMED | Installer.php:198-199; `PaymentRepository::setOrInsert()` :100; OrderSaver.php:117 `$this->paymentRepository->isCod($order->module)` |
| 91–95 | `age_verification_required` overrides the per-product adult flag, `last_update_tracking_status` records the last poll, `consign_password` + `consign_password_processed` hold the code and the last attempt | CONFIRMED | OrderExporter.php:91-95; `setLastUpdateTrackingStatus()` OrderRepository.php:619; `setConsignPassword()` :629 and `markConsignPasswordAttempts()` :646 |
| 95–97 | Retrieval order is untried rows first, then oldest attempt first [VERIFY: …#markConsignPasswordAttempts] | CONFIRMED | shown by the sibling method, not the anchored one: `getOrdersMissingConsignPassword()` OrderRepository.php:706-707 `ORDER BY po.consign_password_processed ASC, o.date_add ASC` with the docblock "Untried orders first, then oldest processed first (FIFO retry across runs)" |
| 103 | calls → packeta-soap-api (sync, SOAP over HTTP) | CONFIRMED | `new \SoapClient($this->soapApi->resolveWsdlUrl())` PacketSubmitter.php:205; SoapApi.php:49-56 |
| 104 | references → packetery-module | CONFIRMED | `Packetery\Module\SoapApi` used by PacketCanceller, Labels, BarcodeProvider, ConsignPasswordProvider; `packetery_product_attribute` read in OrderRepository.php:557-559 |
| 105 | references → packetery-packet-tracking | CONFIRMED | `packetery_packet_status` joined in OrderRepository.php:596; `Packetery\PacketTracking\PacketStatusFactory` used in OrderDetailView.php:14 |
| 107–108 | Sends packet attributes and receives a packet id [VERIFY: …PacketSubmitter.php#createPacketSoap] | CONFIRMED | PacketSubmitter.php:203-216 (`$client->createPacket($this->configHelper->getApiPass(), $packetAttributes)`, returns `$trackingNumber->id`) |
| 108–109 | Sends a packet id and receives a cancellation result [VERIFY: packetery/libs/Request/CancelPacketRequest.php#getPacketId] | CONFIRMED | CancelPacketRequest.php:25-28; SoapApi.php:316-321 `$soapClient->cancelPacket($apiPass, $request->getPacketId())` → `CancelPacketResponse` |
| 110 | Asks for label and carrier-label PDFs [VERIFY: …Labels.php#packetsLabelsPdf] | CONFIRMED | SoapApi.php:268/294 `packetsLabelsPdf` / `packetsCourierLabelsPdf` |
| 111 | Asks for a shipment barcode image [VERIFY: …BarcodeProvider.php#getBarcodeData] | CONFIRMED | SoapApi.php:330 `createShipment`, :349 `barcodePng` |
| 112–113 | Asks for the packet info carrying the consignment code [VERIFY: …ConsignPasswordProvider.php#fetchFromApi] | CONFIRMED | SoapApi.php:91-97 `packetInfo(...)`; `PacketInfo::getConsignPassword()` |
| 113–115 | Every call but the packet creation goes through the shared SOAP client of packetery-module; `PacketSubmitter` builds its own `\SoapClient` from the same resolved WSDL location | CONFIRMED | all other call sites go through `Packetery\Module\SoapApi`; PacketSubmitter.php:205 builds `new \SoapClient($this->soapApi->resolveWsdlUrl())` itself. Nuance: `SoapApi` is a shared wrapper class, not a shared client instance — each of its methods also does `new \SoapClient($this->resolveWsdlUrl())` |
| 115–116 | The API password is read from the configuration and never stored in this module | CONFIRMED | `$this->configHelper->getApiPass()` (PacketSubmitter.php:207, SoapApi.php:97…); no password field or table in the four covered dirs |
| 118–120 | `packetery_product_attribute` belongs to packetery-module and answers whether an order holds an adults-only product [VERIFY: …#isOrderForAdults] | CONFIRMED | OrderRepository.php:555-562 joins `packetery_product_attribute` on `is_adult = 1`; the table is created by packetery-module's installer |
| 120–122 | `packetery_packet_status` belongs to packetery-packet-tracking and supplies the last packet status when the tracking cron picks orders [VERIFY: …#getOrdersByStateAndLastUpdate] | CONFIRMED | OrderRepository.php:594-606 (sub-select for the latest `event_datetime` per order) |
| 124–126 | `PacketeryOrderGridController` drives submission, cancellation, labels, collection print and CSV export | CONFIRMED | PacketeryOrderGridController.php:242 `ordersExport`, :457 `PacketCanceller`, :342 `packetsLabelsPdf`, :496/:511 collection print, :483 `outputCsvExport` |
| 126 | The `checkout` front controller calls the cart-side savers | CONFIRMED | controllers/front/checkout.php:38 `savePickupPointInCartGetJson()`, :46 `saveAddressInCart()` |
| 126–127 | The `packetery.php` hook handlers call the order saver and the order-detail updater | CONFIRMED | packetery.php:1042 `OrderSaver::class`, :1078 `OrderDetailsUpdater::class` |
| 127 | The consignment-code cron task calls `ConsignPasswordProvider` | CONFIRMED | libs/Cron/Tasks/GetConsignPassword.php:87 `$this->consignPasswordProvider->fetchFromApi(...)` |
| 128–130 | Coupled to PrestaShop directly — constructs `\Order`, `\Address`, `\Currency`, `\OrderCarrier`, reads request values through `\Tools`, renders through the module context's Smarty | CONFIRMED | `new \Order` PacketSubmitter.php:160 / CollectionPrintService.php:72; `new \Address` OrderExporter.php:64; `new \Currency` OrderExporter.php:165; `new \OrderCarrier` PacketSubmitter.php:169; `\Tools::getValue` OrderSaver.php:154; `$this->module->getContext()->smarty` CollectionPrintHandler.php:93 |
| 134–136 | The affiliate id sent with every created packet is a compile-time constant of `PacketSubmitter`, so it cannot be changed per shop [VERIFY: …#AFFILIATE_ID] | CONFIRMED | `public const AFFILIATE_ID` (PacketSubmitter.php:26) used at :85 and CsvExporter.php:82; no configuration override |
| 136–139 | Collection print and CSV export write headers straight to the output and end the request themselves instead of returning a response object [VERIFY: …CsvExporter.php#outputCsvExport] | CONFIRMED | CsvExporter.php:109-110 `header(...)` + `php://output`; CollectionPrintHandler.php:121-123 `header(...)` … `exit;`. Nuance: for the CSV path the `exit` is in the caller — PacketeryOrderGridController.php:484 — not in the anchored method |
| 141–143 | `OrderDetailView` cannot translate an unknown packet status code, leaves the template variable unset, with a note that a `code_text` column would fix it [VERIFY: …OrderDetailView.php:61] | CONFIRMED | OrderDetailView.php:55-61: assignments happen only inside `if (isset($packetStatuses[$lastStatusCode]))`, followed by `// else TODO: after adding a new column code_text to the db, return the value from the db` on line 61 |
| 143–144 | `OrderSaver` carries a note that it should inherit from a common base class [VERIFY: …OrderSaver.php:43] | CONFIRMED | OrderSaver.php:43 `* TODO: later inherit from some Base class` |
| 146–147 | `OrderRepository` holds two methods that delete by cart id with the same body, `deleteByCart` and `deleteByCartId` | CONFIRMED | OrderRepository.php:155-159 and :164-167 — both `$this->db->delete('packetery_order', '`id_cart` = ' . (int) $cartId)`, differing only in where the cast happens |
| 147–149 | `CancelPacketResponse::hasCancelNotAllowedFault` has no call site anywhere in the repository | CONFIRMED | repo-wide grep finds only the definition at Response/CancelPacketResponse.php:22 |
| 148–149 | …and the tracking link of `PacketInfo` has no call site anywhere in the repository | INCORRECT | `PacketInfo::setTrackingLink()` is called: SoapApi.php:103 `$packetInfo->setTrackingLink($this->getTrackingUrlInProperLanguage(...))`. Only the getter `PacketInfo::getTrackingLink()` is unused (the `getTrackingLink` hits in PacketeryOrderGridController.php:171/645 are an unrelated method of the controller) |
| 151–153 | A failed tracking-number clearing after a successful cancellation shows a message, while a failed consignment-code clearing is only written to the PrestaShop log [VERIFY: …PacketCanceller.php#cancelPacket] | CONFIRMED | PacketCanceller.php:73-85 (`$messages[] = …` vs `\PrestaShopLoggerCore::addLog(...)`) |
| 154–156 | The consignment-code refresh right after packet creation swallows both the API and the database failure and leaves the code to the cron task [VERIFY: …#refreshConsignPasswordIfImmediate] | CONFIRMED | PacketSubmitter.php:252-261 (`catch (ApiClientException $e) { return; } catch (DatabaseException $e) { return; }`) |

### manifest.part.packetery-order.yaml

| entry | class | evidence |
|---|---|---|
| `service: prestashop` | CONFIRMED | matches `repo: prestashop` in the document front matter and the other fragments |
| `source_commit: b85243bb…` | CONFIRMED | equals the document front matter and `git rev-parse HEAD` |
| assumption — documented on branch `docs/packetery` at b85243bb with a clean working tree | CONFIRMED | `git rev-parse --abbrev-ref HEAD` = `docs/packetery`, HEAD = b85243bb…; only untracked ai-docs artefacts in the tree |
| assumption — module boundary is Order, Request, Response, Payment | CONFIRMED | identical to `covers:` in the document front matter |
| assumption — 2567 lines, 36 types, 140 public methods from ai-docs/ast/map.json excluding `index.php` | CONFIRMED | recomputed from ai-docs/ast/map.json over the four dirs without `index.php`: 36 files, 36 types, 140 methods, 2567 lines |
| assumption — the two tables live in the shop's PrestaShop MySQL database, real name not looked up | CONFIRMED | Installer.php:154/197 create them with `_DB_PREFIX_`; no database name anywhere in the module |
| assumption — SOAP calls classified as `kind: other` because the schema has no SOAP kind | CONFIRMED | templates/manifest.yaml:115 enumerates `rest | grpc | graphql | db | telemetry | other` |
| calls — packeta-soap-api, createPacket, evidence `PacketSubmitter.php#createPacketSoap` | CONFIRMED | PacketSubmitter.php:203-216 |
| calls — packeta-soap-api, cancel packet, evidence `PacketCanceller.php#cancelPacket` | CONFIRMED | PacketCanceller.php:49 → SoapApi.php:316 |
| calls — packeta-soap-api, Packeta/carrier label PDF, evidence `Labels.php#packetsLabelsPdf` | CONFIRMED | Labels.php:62/64/68 → SoapApi.php:268/294 |
| calls — packeta-soap-api, shipment + barcode image, evidence `BarcodeProvider.php#getBarcodeData` | CONFIRMED | BarcodeProvider.php:37/49 → SoapApi.php:330/349 |
| calls — packeta-soap-api, packet info with consignment code, evidence `ConsignPasswordProvider.php#fetchFromApi` | CONFIRMED | ConsignPasswordProvider.php:43 → SoapApi.php:91 |
| owns_data — `packetery_order`, `mysql/prestashop`, evidence `OrderRepository.php#getWithShopById` | CONFIRMED | OrderRepository.php:371; table created in Installer.php:154; no host/connection string in the entry |
| owns_data — `packetery_payment`, `mysql/prestashop`, evidence `PaymentRepository.php#setOrInsert` | CONFIRMED | PaymentRepository.php:100; table created in Installer.php:197 |
| reads_external_data — `packetery_product_attribute`, owner packetery-module, evidence `OrderRepository.php#isOrderForAdults` | CONFIRMED | OrderRepository.php:555-562. Note: `via: db <table>` is not one of the template's enumerated forms (`event <EventName> | api <interface name>`, templates/manifest.yaml:133) and is used only in this fragment; the validator does not check it |
| reads_external_data — `packetery_packet_status`, owner packetery-packet-tracking, evidence `OrderRepository.php#getOrdersByStateAndLastUpdate` | CONFIRMED | OrderRepository.php:585-606; same `via: db` note |
| unknown — full attribute contract of the SOAP `createPacket` operation | CONFIRMED | no WSDL and no API contract file in the repository (`find -iname '*.wsdl'` outside vendor: none); the WSDL is fetched from a URL constant in SoapApi.php:49-56 |
| unknown — which packet attribute faults the SOAP API can raise | CONFIRMED | faults are read generically: `$e->detail->PacketAttributesFault->attributes->fault` (PacketSubmitter.php:229-241), `getFaultIdentifier()` SoapApi.php:235; no enumeration in the repo |

CONFIRMED 82 · INCORRECT 3 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/reference/packetery-order.md` (and in its manifest fragment where the row pointed there). `confidence` was set to `reviewed`; `verified` needs another pass by someone who did not write or fix the document.
