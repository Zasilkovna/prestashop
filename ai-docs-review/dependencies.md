## ai-docs/dependencies.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17 | "prestashop communicates with 5 systems outside its own code" | CONFIRMED | `ai-docs/manifest.yaml` `calls:` names exactly five distinct services — github-api, mysql, packeta-pickup-point-api, packeta-soap-api, packeta-widget; no sixth outbound target found in the source. |
| 19–24 | the five `calls →` edge lines (manifest-style block) | CONFIRMED | Same five services, same `sync` direction; protocols match the clients (`\SoapClient` in `packetery/libs/Module/SoapApi.php`, `HttpClientWrapper` JSON in `Downloader`/`PickupPointValidate`/`ApiClientFacade`, PrestaShop `Db` in `packetery/libs/Tools/DbTools.php`). |
| 27 | packeta-soap-api, calls/sync/SOAP, in `packetery/libs/Module/SoapApi.php` — anchor `#_PACKETERY_SOAP_WSDL_URL_` | CONFIRMED | `SoapApi::resolveWsdlUrl()` (line 49) tests `defined('_PACKETERY_SOAP_WSDL_URL_')` and falls back to `self::WSDL_URL` (line 28); every call site builds `new \SoapClient($this->resolveWsdlUrl())`. (The class constant is `WSDL_URL`, which is what `manifest.yaml` anchors; the document's anchor symbol also exists in the file.) |
| 28 | packeta-pickup-point-api, calls/sync/HTTP-JSON, `packetery/libs/ApiCarrier/Downloader.php#API_URL` | CONFIRMED | `private const API_URL` at line 22; `downloadJson()` (line 117) calls `$this->httpClient->get($url)`. |
| 29 | packeta-widget, calls/sync/HTTP-JSON, `packetery/libs/PickupPointValidate/PickupPointValidate.php#URL_VALIDATE_ENDPOINT` | CONFIRMED | `private const URL_VALIDATE_ENDPOINT` at line 22; `validate()` posts to it via `HttpClientWrapper::post` with a JSON body. |
| 30 | github-api, calls/sync/HTTP-JSON, `packetery/libs/Module/VersionChecker.php#GITHUB_RELEASES_ENDPOINT_URL` | CONFIRMED | `public const GITHUB_RELEASES_ENDPOINT_URL` at line 23; used in `getLatestReleaseResponse()` line 93. |
| 31 | mysql, calls/sync/SQL, `packetery/libs/Tools/DbTools.php#execute` | CONFIRMED | `DbTools::execute($sql, $useCache)` at line 144, delegating to the PrestaShop `Db` instance. |
| 33–35 | packeta-soap-api carries packet creation, cancellation, labels, barcode, delivery history, API-password and sender-indication checks | CONFIRMED | `PacketSubmitter::createPacketSoap()` (line 203), `SoapApi::cancelPacket` (316), `getPacketsLabelsPdf` (268) / `getPacketsCourierLabelsPdf` (294), `barcodePng` (349), `getPacketTracking` (249), `senderGetReturnRouting` (67) with `IncorrectApiPasswordException` / `SenderNotExistsException`. |
| 35–36 | packeta-pickup-point-api delivers the carrier catalogue for the pairing form and is read-only | CONFIRMED | `Downloader::downloadJson()` uses `httpClient->get` only; no write call to that host anywhere in `packetery/`. |
| 36–37 | packeta-widget is reached twice — browser loads the library, server re-checks a chosen point | CONFIRMED | Matches the two `packeta-widget` entries in `manifest.yaml` (`packetery/views/js/back.js#getScript`, `PickupPointValidate.php#URL_VALIDATE_ENDPOINT`). Note: the library is actually loaded from three JS sites (`back.js:9`, `back.js:53`, `front.js:27`); the storefront one is missing from the manifest, but it is the same kind of reach the sentence describes. |
| 37–39 | github-api is only the update notice; when unreachable the module keeps the last stored answer and retries after the check interval — `VersionChecker.php#checkForUpdate` | CONFIRMED | `checkForUpdate()` line 47: on exception it updates only `KEY_LAST_VERSION_CHECK_TIMESTAMP` and returns, leaving `KEY_LAST_VERSION*` untouched; `shouldCheckApi()` compares against `CHECK_INTERVAL_IN_SECONDS` (line 22). |
| 40 | "mysql is the shop's own database and no failure of it is recoverable inside the module" | INCORRECT | `DatabaseException` is caught and recovered from in several places. `DbTools::executeQueries` lines 45–54: `} catch (DatabaseException $exception) { … if ($returnFalseOnException) { return false; } }` — it logs and keeps going. `Cron/Tasks/GetConsignPassword::execute` lines 92–94: `} catch (DatabaseException $e) { continue; }` — the run moves to the next order. `OrderRepository.php:90,476,680`, `OrderSaver.php:228`, `PacketSubmitter.php:259`, `PurgeLogs.php:46` catch it too. |
| 43–45 | "no module of this repository calls another over a network" | CONFIRMED | All inter-module use is `use Packetery\…` + `diContainer->get(...)`; the only network clients (`\SoapClient`, `HttpClientWrapper`) address the five external services above. |
| 47–50 | the four `calls →` inter-module edge lines (targets: packetery-order, packetery-carrier, packetery-module, packetery-tools) | INCORRECT | The set of call targets is larger than four. `packetery/controllers/admin/PacketeryOrderGridController.php:24-26` imports `Packetery\PacketTracking\PacketStatus`, `PacketStatusFactory`, `PacketTrackingRepository` and resolves them at lines 755–785 — an admin-ui → packetery-packet-tracking edge that is neither in this list nor in the table. `packetery/controllers/front/checkout.php:11` imports `Packetery\Module\Cart` and resolves it in the `fetchExtraContent` branch — a checkout → packetery-module edge, also absent. |
| 54 | packetery-checkout → packetery-order, `controllers/front/checkout.php#savePickupPointInCart` | CONFIRMED | `case 'savePickupPointInCart':` line 35 resolves `Packetery\Order\OrderSaver` and calls `savePickupPointInCartGetJson()`. |
| 55 | packetery-checkout → packetery-carrier, `PickupPointValidate/PickupPointValidator.php#CarrierTools` | CONFIRMED | `use Packetery\Carrier\CarrierTools;` line 16; `CarrierTools::findExternalCarrierId($orderData)` line 103. |
| 56 | packetery-admin-ui → packetery-order, `PacketeryOrderGridController.php#processBulkCreatePacket` | CONFIRMED | Method at line 254 calls `createPackets()`; the controller imports `Packetery\Order\PacketSubmitter`, `Labels`, `OrderRepository`, `PacketCanceller`, `Tracking` (lines 17–23). |
| 57 | packetery-admin-ui → packetery-carrier, `PacketeryCarrierGridController.php#CarrierAdminForm` | CONFIRMED | `use Packetery\Carrier\CarrierAdminForm;` line 12; instantiated at lines 157 and 171. |
| 58 | packetery-packet-tracking → packetery-order, `Cron/Tasks/GetConsignPassword.php#execute` | CONFIRMED | `execute(int $maxOrders, int $maxOrderAgeDays)` line 48 drives `$this->orderRepository` (`Packetery\Order\OrderRepository`) and `consignPasswordProvider->fetchFromApi`. |
| 59 | packetery-packet-tracking → packetery-carrier, `Cron/Tasks/DownloadCarriers.php#Downloader` | CONFIRMED | `use Packetery\ApiCarrier\Downloader;` line 14, injected in the constructor line 29; `manifest.yaml` attributes `ApiCarrier` to packetery-carrier. |
| 60 | packetery-order → packetery-module, `Order/PacketCanceller.php#soapApi` | CONFIRMED | `private $soapApi` line 24, typed `SoapApi` (`Packetery\Module\SoapApi`) line 34; used at line 49. |
| 61 | packetery-order → packetery-tools, `Order/OrderRepository.php#getWithShopById` | CONFIRMED | Method at line 371 runs its query through `$this->dbTools->getRow(...)` (`Packetery\Tools\DbTools`). |
| 63 | packetery-tools is depended on by every other module and depends on none of them | CONFIRMED | `packetery/libs/Tools/` and `packetery/libs/Log/` import only `Packetery\Exceptions\*` and their own namespace; `Packetery\Tools\` is imported from `libs/Module`, `libs/Order`, `libs/Carrier`, `libs/PacketTracking`, `libs/PickupPointValidate` and `controllers/admin`. |
| 64–66 | packetery-module is entry point and service provider: registers the hooks and owns the SOAP facade | CONFIRMED | `packetery/libs/Module/Installer.php:52` `$this->module->registerHook($this->module->getModuleHooksList())`; `SoapApi` lives in `packetery/libs/Module/`. |
| 66–67 | the container resolves edges by class name, so a static call graph shows them only through constructor signatures — `DI/Container.php#ReflectionClass` | CONFIRMED | `Container` line 61 `$reflection = new \ReflectionClass($class);` and `getParamInstances(\ReflectionClass $reflection)` line 74 resolve constructor parameters. |
| 71–75 | no runtime package dependency; `composer.json` requires PHP 8.1+ and nothing else for production; framework classes come from the PrestaShop installation; no `vendor/` shipped — `composer.json#require-dev` | CONFIRMED | `"require": {"php": "^8.1"}`; all eight other packages are under `require-dev`; `/vendor/` is in `.gitignore` and `git ls-files` tracks no `vendor/` path. |
| 79 | php `^8.1`, the only production requirement — `composer.json:2` | CONFIRMED | `"php": "^8.1"` — it sits on line 3; line 2 is the opening `"require": {`, so the anchor is one line short, but the version and the role are right. |
| 80 | phpstan/phpstan `^2.1`, used by the `phpstan` Composer script — `composer.json:9` | CONFIRMED | Line 9 `"phpstan/phpstan": "^2.1"`; script `"phpstan": "vendor/bin/phpstan analyse -c phpstan/phpstan.neon"`. |
| 81 | stancer/php-stubs-prestashop `^8.2`, PrestaShop class stubs — `composer.json:11` | CONFIRMED | Line 11 `"stancer/php-stubs-prestashop": "^8.2"`. |
| 82 | squizlabs/php_codesniffer `^3.12`, behind `check:phpcs` — `composer.json:6` | CONFIRMED | Line 6 `"squizlabs/php_codesniffer": "^3.12"`; script `"check:phpcs": "vendor/bin/phpcs --standard=./phpcs.xml"`. |
| 83 | prestashop/php-dev-tools `^4.0`, the fixer preset used by `PacketeryCsFixerConfig` — `PacketeryCsFixerConfig.php#getRules` | CONFIRMED | Line 13 `"prestashop/php-dev-tools": "^4.0"`; `PacketeryCsFixerConfig extends PrestaShop\CodingStandards\CsFixer\Config` and `getRules()` starts from `parent::getRules()`. |
| 85–86 | transitive packages and the remaining development tools are left out of the table | CONFIRMED | The four dev packages not in the table (slevomat/coding-standard, editorconfig-checker, phpstan-strict-rules, phpstan/extension-installer) are all `require-dev` tooling with no runtime effect. |
| 86–88 | two PHP extensions are hard requirements: install refuses without `curl`, configuration page reports missing `soap` — `packetery/packetery.php:277` | CONFIRMED | Line 277 `if (!extension_loaded('soap'))` inside `getContent()`, adding `displayError`; `install()` line 114 `if (extension_loaded('curl') === false) { … return false; }`. |
| 93–94 | the first two keys are configuration rows in the shop database, the last three are PHP constants defined outside the repository | CONFIRMED | `ConfigHelper::KEY_APIPASS` / `KEY_ESHOP_ID` are `\Configuration` keys; the other three are read through `defined()` in `SoapApi`, `ApiClientFacade` and `VersionChecker`. |
| 97 | `PACKETERY_APIPASS`, read in `Tools/ConfigHelper.php`, lives in the `configuration` table — `ConfigHelper.php#KEY_APIPASS` | CONFIRMED | `public const KEY_APIPASS = 'PACKETERY_APIPASS';` line 18. |
| 98 | `PACKETERY_ESHOP_ID`, read in `Order/OrderExporter.php`, stored per shop — `ConfigHelper.php#KEY_ESHOP_ID` | CONFIRMED | `ConfigHelper.php:19` declares the key; line 35 maps it to `self::BEHAVIOR_SEPARATE`; `OrderExporter.php:80` reads it with `$packeteryOrder['id_shop_group'], $idShop`. |
| 99 | `_PACKETERY_SOAP_WSDL_URL_`, read in `Module/SoapApi.php`, deployment constant, unset falls back to built-in default | CONFIRMED | `resolveWsdlUrl()` line 50–55: `if (defined('_PACKETERY_SOAP_WSDL_URL_') && _PACKETERY_SOAP_WSDL_URL_ !== '') { return … } return self::WSDL_URL;`. |
| 100 | `_GITHUB_ACCESS_TOKEN_`, read in `Module/ApiClientFacade.php`, deployment constant — `ApiClientFacade.php:37` | CONFIRMED | Line 37 `$token = defined('_GITHUB_ACCESS_TOKEN_') ? _GITHUB_ACCESS_TOKEN_ : null;`. |
| 101 | `_PACKETERY_ALLOWED_RELEASE_TYPES_`, read in `Module/VersionChecker.php`, deployment constant — `VersionChecker.php:90` | CONFIRMED | Line 90 `defined('_PACKETERY_ALLOWED_RELEASE_TYPES_') && is_array(_PACKETERY_ALLOWED_RELEASE_TYPES_)`. |
| 103–105 | `PACKETERY_APIPASS` is the credential every Packeta call authenticates with; the API key of the carrier feed and the widget is derived from it — `ConfigHelper.php#getValidApiKey` | CONFIRMED | `getValidApiKey()` line 174: `$apiPass = $this->getApiPass(); … $apiKey = self::getApiKeyFromApiPass($apiPass);` — no separate stored key; the SOAP calls pass the API password directly. |
| 105–107 | whether the three constants are defined cannot be answered here; the gap is recorded in `manifest.yaml` under `unknowns` | CONFIRMED | `manifest.yaml` `unknowns` carries the question "Whether the optional constants `_PACKETERY_SOAP_WSDL_URL_`, `_PACKETERY_ALLOWED_RELEASE_TYPES_`, `_GITHUB_ACCESS_TOKEN_` and `_PACKETERY_DEBUG_LOG_` are defined in a real installation…". |
| 111–115 | no documented alias; `manifest.yaml` carries no `aliases` section; module class, module name, directory and translation domain all use `packetery` — `packetery.php#MODULE_SLUG` | CONFIRMED | `manifest.yaml` has no `aliases` key; `packetery.php:64` `public const MODULE_SLUG = 'packetery';`, line 74 `$this->name = 'packetery';`, directory `packetery/`, translation keys `'<{packetery}prestashop>packetery_…'` in `packetery/translations/cs.php`. |
| 115–116 | the seven module ids were coined by this documentation run rather than taken from a name the code uses | CONFIRMED | `manifest.yaml` lists seven modules and states "The module map of the repository was supplied by the run"; no `packetery-order` / `packetery-tools` / `packetery-admin-ui` identifier occurs in the PHP source (only the unrelated CSS class `packetery-order-status`). |

CONFIRMED 41 · INCORRECT 2 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/dependencies.md`. `confidence` was set to `reviewed`.
