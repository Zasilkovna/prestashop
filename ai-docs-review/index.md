## ai-docs/index.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 5-7 | front matter `generated-by: skill:generate-docs@0.3.5`, `source-commit: b85243bb…`, `last-generated: 2026-09-22` | CONFIRMED | `ai-docs/manifest.yaml:4-6` carries the same three values. |
| 17 | prestashop is the Packeta delivery module for PrestaShop shops | CONFIRMED | `manifest.yaml:2-3` `description: 'PrestaShop module that integrates the Packeta delivery service into a shop: …'` |
| 20-26 | page table (navigation): manifest.yaml = routes / outbound calls / owned tables / external data / assumptions / blind spots; overview.md = what the repo is, module table, startup, files of no module, line counts; architecture.md = seven modules, two runtime flows, authorisation, persistence, hook system; dependencies.md = five external systems, links between modules, Composer manifest, environment keys; `reference/` = one page per module | CONFIRMED | Navigation-only, one row per table. Manifest top-level keys are `exposes`, `calls`, `owns_data`, `reads_external_data`, `assumptions`, `unknowns` — the six descriptors map one to one. `ai-docs/overview.md` headings 15/33/56/84 match and its module table carries a "Lines of logic" column. `ai-docs/architecture.md` headings: module composition (15), two runtime-flow sections (58, 81), authentication and authorisation (99), data and persistence (115), cross-cutting mechanisms (136) which opens with "the PrestaShop hook system". `ai-docs/dependencies.md:17` "prestashop communicates with 5 systems outside its own code", plus links between modules (42), external packages (69), environment configuration (90). |
| 28-36 | the seven module pages and their one-line scopes (packetery-module entry point/install/hooks/settings/SOAP facade; packetery-order order row/packet submission/labels/exports; packetery-carrier pairing and catalogue; packetery-admin-ui the four admin controllers and their grids; packetery-packet-tracking the cron controller, its five tasks and the packet-status history; packetery-checkout storefront controller, pickup-point validation, theme adapters; packetery-tools config keys, DB wrapper, HTTP client, logging, exceptions) | CONFIRMED | Navigation-only prose listing the same seven pages, one row. All seven files exist under `ai-docs/reference/`. "four admin controllers" matches `manifest.yaml:56-58` (`PacketeryOrderGrid`, `PacketeryCarrierGrid`, `PacketeryLogGrid`, `PacketerySetting`); "five tasks" matches `manifest.yaml:53-54` ("The five cron task names are treated as separate `exposes` entries"). |
| 40 | prestashop contains 7 modules | CONFIRMED | `manifest.yaml:82-110` lists exactly 7 entries under `modules:`. |
| 42-50 | module table: name, path and document for all seven modules | CONFIRMED | Table copies `manifest.yaml` `modules:` — same seven names, same `path` values (`packetery/libs/Module`, `packetery/libs/Order`, `packetery/libs/Carrier`, `packetery/controllers/admin`, `packetery/libs/PacketTracking`, `packetery/libs/Tools`, `packetery/libs/PickupPointValidate`), same order, same `doc` paths; all seven documents exist. |
| 52-53 | every module passed the threshold for a standalone document, so no module is described in overview.md | CONFIRMED | `ai-docs/overview.md:35-36` "All 7 passed the threshold for a standalone `reference/` document, so `overview.md` carries no module summary of its own." |
| 53-55 | the module ids were coined by this run; the repo is one PrestaShop module too large for one reference page and the code declares no internal boundaries | CONFIRMED | `manifest.yaml:51-53` "nothing in the code declares that split"; `manifest.yaml:44-45` "The module map of the repository was supplied by the run"; `manifest.yaml:47` "the one fixed by the run brief". |
| 60 | `manifest.yaml` carries all 32 assumptions under `assumptions:` | CONFIRMED | `manifest.yaml:7-81` contains exactly 32 top-level list entries. |
| 63 | read on branch `docs/packetery` at commit `b85243bb` with a clean working tree | CONFIRMED | `manifest.yaml:12-13` "Generated on branch `docs/packetery` at commit b85243bbeb5f9f91c0bde739f5195b2285b867f9 with a clean working tree." |
| 64-66 | the repo id is the repository name from the origin URL and denotes the Packeta module repo, not the PrestaShop platform, which has no manifest entry | CONFIRMED | `manifest.yaml:49-50` states the same, and `manifest.yaml:22-23` "The PrestaShop platform … is described in prose only and gets no manifest entry." |
| 67-69 | about 9 700 lines of logic, above the 3 000-line ceiling, split into seven pages along namespaces and directories; nothing declares that split; the ids were coined here | CONFIRMED | `manifest.yaml:51-53`, same wording; the "ids coined here" half rests on `manifest.yaml:44-45`/`47`. |
| 70-71 | `modules[].path` names the primary directory of each slice; the full set of covered paths is in each reference page's `covers` front matter | CONFIRMED | `manifest.yaml:74-75`, same sentence (the manifest adds "and in the module table of overview.md"; the bullet is a subset, not a contradiction). |
| 72-74 | line/type/public-method counts come from `ai-docs/ast/map.json`, exclude the `index.php` guard stubs; map generated at `801bb6bd`, `git diff --stat 801bb6b..HEAD` touches no source file, used without regeneration | CONFIRMED | `manifest.yaml:14-21`; `ai-docs/ast/map.json` exists and `801bb6b` resolves in the repository. |
| 75-76 | `packetery/upgrade/`, `packetery/translations/` and the root tooling configuration belong to no module and are excluded from the line counts | CONFIRMED | `manifest.yaml:76-77`, same sentence. |
| 77 | the storefront and back-office JavaScript is not covered by the AST map and was read from source | CONFIRMED | `manifest.yaml:61-62` "The storefront JavaScript under `packetery/views/js/` is not covered by the AST map and was read directly from the source files"; that directory holds `back.js` (back office) alongside `front.js`, so the bullet names the same set. |
| 78-79 | every table lives in the shop's own MySQL database; `store: mysql/prestashop` names engine and logical name only; the real database name was not looked up | CONFIRMED | `manifest.yaml:9-11`, `:55-56` and `:80-81` all say this. |
| 80-81 | configuration values are never copied — only key names, with an anchor at the place the value lives | CONFIRMED | `manifest.yaml:8-9` "Configuration values are never copied into the documentation; only the key names declared on `ConfigHelper` are." The anchor half is borne out by `ai-docs/dependencies.md:95-102`, whose table has a "Where the value lives" column with a `[VERIFY: …]` anchor per key. |
| 82 | SOAP calls are recorded as `kind: other` because the schema has no SOAP kind | CONFIRMED | `manifest.yaml:24-25`. |
| 83-84 | the five cron tasks and the three `checkout` actions are separate `exposes` entries although each group is one registered front controller | CONFIRMED | `manifest.yaml:53-54` (five cron tasks, "the code registers one front controller only") and `manifest.yaml:66-68` (three `checkout?action=…` entries on one registered route). |
| 85-86 | `esterka:writing-technical-docs-in-ste` is not installed, so the prose follows `templates/writing-sections.md` alone | CONFIRMED | `manifest.yaml:59-60`, same sentence. |
| 90-91 | documentation generated from commit `b85243bbeb5f9f91c0bde739f5195b2285b867f9` | CONFIRMED | Front matter `source-commit` (line 6) and `manifest.yaml:5` agree. |
| 91-92 | each module page was verified against the code by an agent that did not write it | CONFIRMED | `ai-docs-review/` holds one record per module page, each headed `## ai-docs/reference/<module>.md @ b85243bb… — 2026-09-22`. |
| 92 | every INCORRECT finding was then fixed by the generator | CONFIRMED | Spot-checked 3 of the 31 INCORRECT rows: the hummingbird selector now reads `.js-delivery-option input[id^=delivery_option]:checked` (`ai-docs/reference/packetery-checkout.md:131`), "three further keys" is now "Six further keys" (`ai-docs/reference/packetery-module.md:170`), and the "no other module holds" framing is now "those two are not the repository's only outbound clients" (`ai-docs/reference/packetery-module.md:42-43`). Sampled, not exhaustive. |
| 96 | reference/packetery-module.md — reviewed — 85/91 CONFIRMED, 6 INCORRECT | CONFIRMED | Front matter `confidence: reviewed`; `ai-docs-review/reference-packetery-module.md:103` `CONFIRMED 85 · INCORRECT 6 · UNCERTAIN 0 · NOT FOUND 0` → 85/91. |
| 97 | reference/packetery-order.md — reviewed — 82/85 CONFIRMED, 3 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 97 `CONFIRMED 82 · INCORRECT 3 · UNCERTAIN 0 · NOT FOUND 0` → 82/85. |
| 98 | reference/packetery-carrier.md — reviewed — 70/75 CONFIRMED, 4 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 87 `CONFIRMED 70 · INCORRECT 4 · UNCERTAIN 1 · NOT FOUND 0` → 70/75. |
| 99 | reference/packetery-admin-ui.md — reviewed — 82/85 CONFIRMED, 2 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 97 `CONFIRMED 82 · INCORRECT 2 · UNCERTAIN 1 · NOT FOUND 0` → 82/85. |
| 100 | reference/packetery-packet-tracking.md — reviewed — 93/98 CONFIRMED, 5 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 110 `CONFIRMED 93 · INCORRECT 5 · UNCERTAIN 0 · NOT FOUND 0` → 93/98. |
| 101 | reference/packetery-tools.md — reviewed — 78/82 CONFIRMED, 4 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 94 `CONFIRMED 78 · INCORRECT 4 · UNCERTAIN 0 · NOT FOUND 0` → 78/82. |
| 102 | reference/packetery-checkout.md — reviewed — 82/89 CONFIRMED, 7 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 101 `CONFIRMED 82 · INCORRECT 7 · UNCERTAIN 0 · NOT FOUND 0` → 82/89. |
| 106 | overview.md — draft — not yet verified | CONFIRMED | `ai-docs/overview.md` front matter `confidence: draft`; no `ai-docs-review/overview.md` exists. |
| 107 | architecture.md — draft — not yet verified | CONFIRMED | `ai-docs/architecture.md` front matter `confidence: draft`; no `ai-docs-review/architecture.md` exists. |
| 108 | dependencies.md — draft — not yet verified | CONFIRMED | `ai-docs/dependencies.md` front matter `confidence: draft`; no `ai-docs-review/dependencies.md` exists. |
| 109 | index.md — draft — not yet verified | CONFIRMED | Own front matter `confidence: draft` (line 9); true of the state this record verifies — `ai-docs-review/index.md` is this file. |
| 111-113 | no page reached `verified`; a page with an INCORRECT row rises to `reviewed` once fixed, `verified` needs a second pass by someone who neither wrote nor fixed it; the records live in `ai-docs-review/` and are never published | CONFIRMED | No `ai-docs/**/*.md` front matter carries `confidence: verified` (all are `draft` or `reviewed`). The promotion wording matches `.claude/skills/generate-docs/templates/verify.md:38-43`. `ai-docs-review/` exists outside `ai-docs/`. |
| 117-119 | the blind spots are carried machine-readably by `manifest.yaml` under `unknowns`; the tables hold, in order, the Packeta side, the deployment, and the module's own code | CONFIRMED | `manifest.yaml:370-451` is `unknowns:`; the three tables group as stated — table 1 is all Packeta-side questions, table 2 all deployment/hosting/shop-configuration questions, table 3 all questions about this module's own code. |
| 121-127 | unknowns table 1 (Packeta side): 5 rows — createPacket attribute contract, carrier-feed schema, `CarrierVendors::getVendors` countries/vendor groups, `CARRIERS_SUPPORTING_AGE_VERIFICATION` ids, packet attribute faults | CONFIRMED | Copies the manifest, one row per table. All five `question` / `checked` / `resolved_by` triples reproduce `manifest.yaml` word for word. |
| 129-137 | unknowns table 2 (deployment): 7 rows — log file retention, `packetery_log` retention, the four optional constants, employee profiles for the admin tabs, which scheduler calls `cron`, which systems may call the cron route, which systems reach the configuration page | CONFIRMED | Copies the manifest, one row per table. All seven triples reproduce `manifest.yaml` word for word. |
| 139-145 | unknowns table 3 (module's own code): 5 rows — the missing `PurgeLogs` configuration URL, back-office pages `back.js` binds, the `wait` CSS class, the `PacketeryCheckoutModuleUnknown` adapter, the configuration keys that survive uninstall | CONFIRMED | Copies the manifest, one row per table. All five triples reproduce `manifest.yaml` word for word. |
| 115-145 | completeness: the section reproduces the manifest's `unknowns` set | CONFIRMED | `manifest.yaml` has exactly 17 `- question:` entries; the three tables carry 5 + 7 + 5 = 17. Nothing added, nothing dropped, and no question reworded into a different claim — only the `blocks:` field is left out, which is not a claim the prose makes. |
| 22-50 | every link resolves | CONFIRMED | `manifest.yaml`, `overview.md`, `architecture.md`, `dependencies.md` and all seven `reference/packetery-*.md` targets exist; `reference/` is written as code, not as a link. |

CONFIRMED 42 · INCORRECT 0 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0

## ai-docs/index.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22 (second pass)

| line | claim | class | evidence |
|---|---|---|---|
| 17 | "prestashop is the Packeta delivery module for PrestaShop shops" | CONFIRMED | `manifest.yaml:1-3` `service: prestashop` / `description: 'PrestaShop module that integrates the Packeta delivery service into a shop: …'`. |
| 20-26 | the page table (navigation-only, one row): five targets and what each holds | CONFIRMED | `manifest.yaml` top-level keys are `exposes`, `calls`, `owns_data`, `reads_external_data`, `assumptions`, `unknowns` — the six things the row names. `overview.md` headings: what the repo does / modules / startup and configuration / what belongs to no module, with a `Lines of logic` column. `architecture.md` headings: module composition, two `runtime flow …` sections, authentication and authorisation, data and persistence, cross-cutting mechanisms (the hook system). `dependencies.md` headings: links outside the repo, links between modules, external packages, environment configuration. `reference/` holds one page per module. |
| 28-36 | the seven module pages and what each covers (navigation-only, one row) | CONFIRMED | All seven links resolve. Spot checks: admin-ui "four admin controllers" — `packetery/controllers/admin/` holds exactly four `*Controller.php`; packet-tracking "five tasks" — `manifest.yaml` `exposes` lists `cron?task=` DeleteLabels, DownloadCarriers, GetConsignPassword, PurgeLogs, UpdatePacketStatus; tools "configuration keys, database wrapper, HTTP client, logging and exceptions" — `packetery/libs/Tools/` holds `ConfigHelper.php`, `DbTools.php`, `HttpClientWrapper.php`, `Logger.php`, `Exception/`. |
| 40 | "prestashop contains 7 modules" | CONFIRMED | `manifest.yaml` `modules:` has 7 entries; `overview.md:35` "prestashop consists of 7 modules". |
| 42-50 | the module table (copies the manifest, one row): name, path and document for all seven | CONFIRMED | Names, paths and order reproduce `manifest.yaml:83-110` exactly (module/Module, order/Order, carrier/Carrier, admin-ui/controllers/admin, packet-tracking/PacketTracking, tools/Tools, checkout/PickupPointValidate). All seven directories exist in the repository and all seven `reference/*.md` targets exist. |
| 52-53 | "Every module … passed the threshold for a standalone document, so no module is described in overview.md" | CONFIRMED | `overview.md:35-36` "All 7 passed the threshold for a standalone `reference/` document, so `overview.md` carries no module summary of its own." |
| 53-55 | "The module ids were coined by this run … the code declares no internal boundaries" | CONFIRMED | `manifest.yaml` assumption 20: "…split into seven reference pages along namespaces and directories; nothing in the code declares that split." |
| 60 | "`manifest.yaml` carries all 32 of them under `assumptions:`" | CONFIRMED | `assumptions:` in `manifest.yaml` parses to exactly 32 entries. |
| 63 | read on branch `docs/packetery` at `b85243bb` with a clean working tree | CONFIRMED | `manifest.yaml` assumption 3, verbatim. |
| 64-66 | repo id `prestashop` is the repository name from the origin URL; denotes the module repo, not the platform | CONFIRMED | `manifest.yaml` assumptions 19 and 8. |
| 67-69 | one module of about 9 700 lines, above the 3 000-line ceiling, split into seven pages | CONFIRMED | `manifest.yaml` assumption 20; the `lines:` values of the seven modules sum to 9735. |
| 70-71 | `modules[].path` names the primary directory; full set of paths in each page's `covers` | CONFIRMED | `manifest.yaml` assumption 28. |
| 72-74 | counts come from `ai-docs/ast/map.json`, exclude `index.php` stubs; map generated at `801bb6bd`, `git diff --stat 801bb6b..HEAD` touches no source file | CONFIRMED | `manifest.yaml` assumptions 4, 5, 6, 11, 12 (source and the stub exclusion) and 7 (the map commit and the diff). |
| 75-76 | `packetery/upgrade/`, `packetery/translations/` and root tooling config belong to no module and are out of the line counts | CONFIRMED | `manifest.yaml` assumption 29, verbatim in substance. |
| 77 | "The storefront and back-office JavaScript is not covered by the AST map and was read from source" | CONFIRMED | `manifest.yaml` assumption 23 names the directory `packetery/views/js/`, which holds both the storefront `front.js` and the back-office `back.js` — the index wording is the same set, named by both roles. |
| 78-79 | every table lives in the shop MySQL database; `store: mysql/prestashop` names engine and logical name only | CONFIRMED | `manifest.yaml` assumptions 2, 21, 25 and 32. |
| 80-81 | configuration values are never copied, only key names | CONFIRMED | `manifest.yaml` assumption 1. |
| 82 | SOAP calls recorded as `kind: other` because the schema has no SOAP kind | CONFIRMED | `manifest.yaml` assumption 9. |
| 83-85 | the five cron tasks and the three `checkout` actions are separate `exposes` entries although each group has one registered front controller | CONFIRMED | `manifest.yaml` assumptions 13 and 26; `exposes` holds `checkout?action=fetchExtraContent|saveAddressInCart|savePickupPointInCart` and five `cron?task=…` entries. |
| 86 | the skill `esterka:writing-technical-docs-in-ste` is not installed, so the prose follows `templates/writing-sections.md` alone | CONFIRMED | `manifest.yaml` assumption 22, verbatim. |
| 90-91 | documentation generated from commit `b85243bbeb5f9f91c0bde739f5195b2285b867f9` | CONFIRMED | Front matter `source-commit:` and `manifest.yaml` `source_commit:` carry the same hash. |
| 91-92 | "Each module page was verified … by an agent that did not write it; every INCORRECT finding was then fixed by the generator" | CONFIRMED | `ai-docs-review/` holds a record for each of the seven module pages, and each ends with a "Generator follow-up (2026-09-22): every INCORRECT row above was fixed in …" line. Spot check on a shared page: the architecture record's INCORRECT rows for "21 hooks" and "Three of the hooks are dispatched by this repository itself" are both corrected in `ai-docs/architecture.md:161-162,169-171` ("registers 20 hooks … 19 fixed names plus one of two", "no `Hook::exec` call exists anywhere under `packetery/`"). |
| 96 | reference/packetery-module.md — reviewed — 85/91 CONFIRMED, 6 INCORRECT | CONFIRMED | Front matter `confidence: reviewed`; `ai-docs-review/reference-packetery-module.md:103` `CONFIRMED 85 · INCORRECT 6 · UNCERTAIN 0 · NOT FOUND 0` → 85/91. |
| 97 | reference/packetery-order.md — reviewed — 82/85 CONFIRMED, 3 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 97 `CONFIRMED 82 · INCORRECT 3 · UNCERTAIN 0 · NOT FOUND 0` → 82/85. |
| 98 | reference/packetery-carrier.md — reviewed — 70/75 CONFIRMED, 4 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 87 `CONFIRMED 70 · INCORRECT 4 · UNCERTAIN 1 · NOT FOUND 0` → 70/75. |
| 99 | reference/packetery-admin-ui.md — reviewed — 82/85 CONFIRMED, 2 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 97 `CONFIRMED 82 · INCORRECT 2 · UNCERTAIN 1 · NOT FOUND 0` → 82/85. |
| 100 | reference/packetery-packet-tracking.md — reviewed — 93/98 CONFIRMED, 5 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 110 `CONFIRMED 93 · INCORRECT 5 · UNCERTAIN 0 · NOT FOUND 0` → 93/98. |
| 101 | reference/packetery-tools.md — reviewed — 78/82 CONFIRMED, 4 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 94 `CONFIRMED 78 · INCORRECT 4 · UNCERTAIN 0 · NOT FOUND 0` → 78/82. |
| 102 | reference/packetery-checkout.md — reviewed — 82/89 CONFIRMED, 7 INCORRECT | CONFIRMED | Front matter `reviewed`; record line 101 `CONFIRMED 82 · INCORRECT 7 · UNCERTAIN 0 · NOT FOUND 0` → 82/89. |
| 106 | overview.md — reviewed — 28/35 CONFIRMED, 7 INCORRECT | CONFIRMED | `ai-docs/overview.md` front matter `confidence: reviewed`; `ai-docs-review/overview.md:42` `CONFIRMED 28 · INCORRECT 7 · UNCERTAIN 0 · NOT FOUND 0` → 28/35. |
| 107 | architecture.md — draft — 39/50 CONFIRMED, 11 INCORRECT, "all fixed, but the ratio stays under 80 %" | CONFIRMED | `ai-docs/architecture.md` front matter `confidence: draft`; `ai-docs-review/architecture.md:57` `CONFIRMED 39 · INCORRECT 11 · UNCERTAIN 0 · NOT FOUND 0` → 39/50 = 78 %. |
| 108 | dependencies.md — reviewed — 41/43 CONFIRMED, 2 INCORRECT | CONFIRMED | `ai-docs/dependencies.md` front matter `confidence: reviewed`; `ai-docs-review/dependencies.md:49` `CONFIRMED 41 · INCORRECT 2 · UNCERTAIN 0 · NOT FOUND 0` → 41/43. |
| 109 | index.md — `reviewed` | INCORRECT | This page's own front matter says otherwise: `ai-docs/index.md:9` `confidence: draft`. No record in `ai-docs-review/index.md` raised it — the first-pass record (line 48, `INCORRECT 0`) carries no "Generator follow-up" line, unlike every other record in the directory. The table states a confidence the page does not have. |
| 109 | index.md — 42/42 CONFIRMED, 0 INCORRECT | CONFIRMED | `ai-docs-review/index.md:48` `CONFIRMED 42 · INCORRECT 0 · UNCERTAIN 0 · NOT FOUND 0` → 42/42. |
| 111-113 | "No page of prestashop reached `verified`"; a page with an INCORRECT row rises to `reviewed` once fixed, `verified` needs a second pass by someone who neither wrote nor fixed it | CONFIRMED | No `ai-docs/**/*.md` front matter carries `confidence: verified` (draft ×2, reviewed ×9). The promotion wording matches `templates/verify.md:42-43`: "After fixing an INCORRECT finding the ceiling is `reviewed`; `verified` needs another pass by someone who did not write or fix the document." |
| 113-115 | "`architecture.md` stays `draft` because its confirmed share is under the 80 % the promotion rule asks for, even with every finding fixed" | CONFIRMED | 39 of 50 ratio rows = 78 % < 80 % (`templates/verify.md:40-41`); `ai-docs-review/architecture.md:59` states the same reason, and the page's front matter is `draft`. |
| 115 | "The verification records are in `ai-docs-review/` and are never published" | CONFIRMED | `templates/verify.md:47-48`: "Write the tables to `ai-docs-review/<doc-id>.md` in the repository root — outside `ai-docs/`, so it is never published and the validator ignores it." The directory holds the 11 records. |
| 119-121 | the blind spots are carried by `manifest.yaml` under `unknowns`; the tables hold, in order, the Packeta side, the deployment, and the module's own code | CONFIRMED | `manifest.yaml` has an `unknowns:` key. Table 1 is entirely Packeta-side (SOAP contract, carrier feed, vendor list, age-verification carriers, SOAP faults); table 2 is deployment/installation (log rotation, purge retention, optional constants, employee profiles, scheduler, cron consumers, config-page consumers); table 3 is this module's own code. |
| 125-129 | unknowns table 1 (Packeta side): 5 rows (createPacket contract, carrier-feed schema, `CarrierVendors::getVendors`, `CARRIERS_SUPPORTING_AGE_VERIFICATION`, SOAP faults) | CONFIRMED | Copies the manifest, one row per table. All five `question`/`checked`/`resolved_by` triples match `manifest.yaml` character for character (string-compared). |
| 133-139 | unknowns table 2 (deployment): 7 rows (log-file retention, `packetery_log` retention, the four optional constants, employee profiles, the scheduler, cron consumers, config-page consumers) | CONFIRMED | Copies the manifest, one row per table. All seven triples match `manifest.yaml` character for character. |
| 143-148 | unknowns table 3 (module's own code): 6 rows (`PurgeLogs` URL, the `front.js` widget-library load, the back-office pages `back.js` binds, the `wait` CSS class, `PacketeryCheckoutModuleUnknown`, the keys that survive uninstall) | CONFIRMED | Copies the manifest, one row per table. All six triples match `manifest.yaml` character for character. |
| 117-149 | completeness: the section reproduces the manifest's `unknowns` set | CONFIRMED | `manifest.yaml` `unknowns:` parses to exactly 18 entries; the three tables carry 5 + 7 + 6 = 18, and a programmatic match found no manifest entry missing from the page and no page row without a manifest entry. Only `blocks:` is left out, which the prose does not claim to carry. |
| 22-50 | every link resolves | CONFIRMED | `manifest.yaml`, `overview.md`, `architecture.md`, `dependencies.md` and all seven `reference/packetery-*.md` targets exist relative to `ai-docs/`; `reference/` is written as code, not as a link. |

CONFIRMED 42 · INCORRECT 1 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** the second-pass INCORRECT row was fixed — `ai-docs/index.md` front matter now reads `confidence: reviewed`, and the status table row for `index.md` reports the second pass. `verified` would need a third pass on the corrected text.
