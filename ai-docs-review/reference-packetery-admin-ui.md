## ai-docs/reference/packetery-admin-ui.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17 | The module is four admin controllers, the Smarty templates they render and three JavaScript files | CONFIRMED | `packetery/controllers/admin/` holds exactly `PacketeryOrderGridController.php`, `PacketeryCarrierGridController.php`, `PacketeryLogGridController.php`, `PacketerySettingController.php`; `packetery/views/js/` holds `back.js`, `collectionPrint.js`, `collectionPrintBulkAction.js` (plus `front.js`/`checkout-modules`, which the front matter does not cover) |
| 19 | Each controller extends `ModuleAdminController` | CONFIRMED | all four class declarations, e.g. `class PacketeryOrderGridController extends ModuleAdminController` (:29) |
| 19-22 | Each becomes a tab under the parent tab `Packetery`, which the installer creates together with `PacketeryOrderGrid`, `PacketeryCarrierGrid`, `PacketerySetting`, `PacketeryLogGrid` [VERIFY: Installer.php#insertMenuItems] | CONFIRMED | `packetery/libs/Module/Installer.php:70-103`, `$menuConfig` — parent `'class' => 'Packetery'` under `SELL`, then the four children in that order |
| 24-26 | The order grid lists shop orders joined with the Packeta order data and the newest packet status | CONFIRMED | `PacketeryOrderGridController.php:85-99` — join on `packetery_order` and a sub-select over `packetery_packet_status` with `MAX(event_datetime)` |
| 26-27 | It adds Packeta columns: pickup point, tracking number, packet status, editable weight [VERIFY: …#fields_list] | CONFIRMED | `fields_list` :121-187 — `name_branch` ("Pickup point / Carrier"), `tracking_number`, `status_code` ("Packet status"), `weight` with `'type' => 'editable'` |
| 26-27 | Bulk actions submit packets, print labels, export CSV and print the bill of delivery | CONFIRMED | `bulk_actions` :189-211 |
| 28-30 | The carrier grid lists shop carriers with their Packeta mapping and opens the carrier edit form as a view page [VERIFY: …#renderView] | CONFIRMED | `PacketeryCarrierGridController.php:39-45` (`packetery_address_delivery` join, `id_branch`), `renderView()` :168-181 guarded by `Tools::getIsset('viewcarrier')` |
| 31-33 | The log grid is a read-only list over `packetery_log`, filterable by status, action and order [VERIFY: …#packetery_log] | CONFIRMED | `PacketeryLogGridController.php:29` `$this->table = 'packetery_log'`, `$this->bulk_actions = []` :92, no row actions; `filter_key` on `status`, `order_id`, `action` — note `date` also carries a `filter_key` (:72), so the list of three is not exhaustive |
| 34-36 | `PacketerySetting` has no page of its own and redirects to the `AdminModules` configuration screen [VERIFY: …#initContent] | CONFIRMED | `PacketerySettingController.php:13-20` — only `initContent()`, which calls `Tools::redirectAdmin(... 'AdminModules', ['configure' => …])` |
| 38-40 | The grids declare the SQL fragments `_select`, `_join`, `_where`, `_orderBy` and a `fields_list` instead of writing queries and HTML | CONFIRMED | order grid :55-121, carrier grid :39-49 — note `PacketeryLogGridController` declares no `_join` (only `_select`, `_where` in `renderList`, `_orderBy`) |
| 40-41 | The rows are post-processed by hook handlers that live outside packetery-admin-ui | CONFIRMED | `packetery/packetery.php:1611` and `:1744`, both in the module main class, not in the controllers |
| 41 | Column values are rendered through the callbacks the `fields_list` names | CONFIRMED | `'callback' => 'getTrackingLink' / 'getIconForBoolean' / 'getWeightEditable' / 'getTranslatedPacketStatus'` etc. |
| 43 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 47 | Four back-office routes and 39 public methods across the four controller classes | CONFIRMED | `grep -c "^    public function"`: 22 + 7 + 9 + 1 = 39 |
| 48 | A route is addressed by its tab class name plus the PrestaShop admin token | CONFIRMED | `Packetery::getAdminLink()` `packetery.php:1378-1397` takes the tab class name and calls `$this->context->link->getAdminLink($controller, true, …)` (with token); used as `getAdminLink('PacketeryOrderGrid', …)` |
| 48-49 | Authorisation is the employee tab permission, and no controller overrides it | CONFIRMED | no `viewAccess` (or any permission override) anywhere in `packetery/controllers/` |
| 53 | Route table row `PacketeryOrderGrid` — order list with Packeta columns, row actions and five bulk actions | CONFIRMED | `addRowAction('action')` :579, `bulk_actions` has exactly five entries |
| 54 | Route table row `PacketeryCarrierGrid` — carrier list plus the edit view opened with `viewcarrier` | CONFIRMED | `renderView()` :170 `Tools::getIsset('viewcarrier')`; `addRowAction('edit')` :151 |
| 55 | Route table row `PacketeryLogGrid` — read-only log list narrowed to one order by `id_order` | CONFIRMED | `PacketeryLogGridController::renderList()` :225-227 `if (Tools::getValue('id_order')) { $this->_where = 'AND \`a\`.\`order_id\` = …'; }` |
| 56 | Route table row `PacketerySetting` — menu item only, redirects to the module configuration page | CONFIRMED | as line 34-36 |
| 58-60 | Bulk actions `CreatePacket`, `LabelPdf`, `CarrierLabelPdf`, `CsvExport`, `CollectionPrint` dispatched to the matching `processBulk` methods [VERIFY: …#bulk_actions] | CONFIRMED | `bulk_actions` :189-211 and `processBulkCreatePacket` :254, `processBulkLabelPdf` :364, `processBulkCarrierLabelPdf` :384, `processBulkCsvExport` :467, `processBulkCollectionPrint` :490 |
| 60-63 | Two-step label flow: `renderList` shows an offset form when the label format allows an offset, and only the request recognised by `submitPrepareLabels` streams the PDF [VERIFY: …#prepareLabels] | CONFIRMED | `renderList()` :554-572 sets `prepareLabelsMode` when `$maxOffset !== 0`, otherwise streams immediately; `processBulkLabelPdf`/`processBulkCarrierLabelPdf` are guarded by `Tools::isSubmit('submitPrepareLabels')` :366, :386; `prepareLabels()` :336-356 sends the `application/pdf` headers and `exit`s |
| 63-66 | Packeta labels and carrier labels are split by the `is_carrier` and `is_ad` flags [VERIFY: …#prepareOnlyCarrierPacketNumbers] | CONFIRMED | :290-323 — `prepareOnlyCarrierPacketNumbers` keeps rows where either flag is true, `prepareOnlyInternalPacketNumbers` keeps rows where both are false |
| 68-70 | Per-row links: `print` and `cancel` with a tracking number, `submit` without one, `cancel` gone once the packet moved past the first tracked status [VERIFY: …#getActionLinks] | CONFIRMED | :793-811 — `cancel` only when `$lastStatusCode === null \|\| $lastStatusCode === PacketStatus::RECEIVED_DATA` |
| 71-72 | The links carry an `action` query parameter turned into `processPrint`, `processCancel`, `processSubmit` | CONFIRMED | `getActionLinkHtml()` :818 `getAdminLink('PacketeryOrderGrid', ['id_order' => …, 'action' => $action])`; the three methods exist :412, :438, :265 |
| 72-75 | `postProcess` scans the whole POST body for `weight_` + order id, normalises decimal comma and spaces, stores each value [VERIFY: …#postProcess] | CONFIRMED | :616-628 — `preg_match('/^weight_(\d+)$/', $key, $matches)`, `str_replace([',', ' '], ['.', ''], $value)`, `$orderRepo->setWeight(...)` |
| 79-81 | Page fragments are rendered through Smarty templates under `packetery/views/templates/admin/`, split into grid overrides, reusable column snippets and standalone documents | CONFIRMED | directory listing: `tery_*_grid/helpers/…`, `grid/*.tpl`, `collectionPrint.tpl`/`carriersInfo.tpl`/… — note the list bodies themselves come from the PrestaShop core helper templates and the carrier edit form from `CarrierAdminForm::getHtml()` (packetery-carrier), so "every page fragment" is loose |
| 81-85 | The three override dirs follow the helper naming convention; each `list_header.tpl` extends the core template and fills `leadin` with the new-version warning; the order grid adds the label-offset form and the bill-of-delivery form there [VERIFY: …list_header.tpl#prepareLabelsMode] | CONFIRMED | all three `list_header.tpl` start with `{extends file="helpers/list/list_header.tpl"}` + `{block name=leadin}` + `$versionUpdateMessageHtml`; the order grid one additionally includes `$collectionPrintFormPath` (:12-14) and the offset form under `{if isset($prepareLabelsMode) && $prepareLabelsMode}` (:15-60). The folder names match PrestaShop's derivation `toUnderscoreCase(substr(get_class($this), 5, -10))` → `tery_order_grid` |
| 89 | `grid/weightEditable.tpl` — text input named after the order id, disabled once a tracking number exists [VERIFY: …#disabled] | CONFIRMED | `{if $disabled}disabled="disabled"{else}name="weight_{$orderId}"{/if}`; `getWeightEditable()` assigns `disabled` from `$row['tracking_number']` |
| 90 | `grid/booleanIcon.tpl` — check or cross icon, used by order and carrier grid [VERIFY: …#prependText] | CONFIRMED | `icon-{($value) ? 'check' : 'remove'}` and `$prependText`; fetched by `getIconForBoolean()` in both grid controllers |
| 91 | `grid/targetBlankLink.tpl` — column value linked to the order or customer detail in a new tab, used by order and log grid [VERIFY: …#columnValue] | CONFIRMED | `<a href="{$link}" target="_blank">{$columnValue}</a>`; fetched by `getColumnLink()` in the order grid (order + customer links) and the log grid |
| 92 | `grid/link.tpl` — icon link with a tooltip title, used by row and edit actions [VERIFY: …#data-original-title] | CONFIRMED | `data-toggle="tooltip" data-original-title="{$title}"`; fetched by `getActionLinkHtml()` (order grid rows) and `displayEditLink()` (carrier grid) |
| 93 | `trackingLink.tpl` — tracking number linked to **the carrier tracking page** [VERIFY: …#trackingNumber] | INCORRECT | the URL is always Packeta's own tracking page, never the carrier's: `Packetery\Module\Helper::TRACKING_URL` (`libs/Module/Helper.php:19`) is a fixed `tracking.packeta.com` pattern and `getTrackingUrl()` only `sprintf`s the packet id into it; the controller assigns `$smarty->assign('trackingUrl', Packetery\Module\Helper::getTrackingUrl($trackingNumber))` (:652) |
| 94 | `collectionPrintForm.tpl` — hidden form posting `packetery_order_ids` to a new tab [VERIFY: …#packetery_order_ids] | CONFIRMED | `<form id="collection-print-form" … target="_blank">` + `<input type="hidden" name="packetery_order_ids">`; `views/css/collectionPrintForm.css` sets `#collection-print-form { display: none; }` |
| 95 | `collectionPrint.tpl` — standalone printable page with barcode, sender, recipient and one row per order [VERIFY: …#showConsignPassword] | CONFIRMED | full `<!DOCTYPE html>` document, `$barcodeImage`/`$barcodeText`, `$sender.*`, `$recipient.*`, `{foreach $orders as $row}`, `{if $showConsignPassword}` |
| 96 | `carriersInfo.tpl` — carrier count, last update time and the manual update button [VERIFY: …#updateCarriersLink] | CONFIRMED | `$totalCarriers`, `$lastCarriersUpdate`, `<a href="{$updateCarriersLink}" …>Manually update the list of carriers</a>` |
| 99-101 | `generateCronInfoBlock.tpl` documents the CRON entry points and the parameters `number_of_days`, `number_of_files`, `max_orders`, `max_order_age_days` [VERIFY: …#max_order_age_days] | CONFIRMED | the four parameter names appear as table cells at :33, :40, :127, :134, alongside four CRON URLs |
| 102-103 | `vendors.tpl` renders the `allowed_vendors` checkboxes per country [VERIFY: …#allowed_vendors] | CONFIRMED | `{foreach $vendorsData as $countryCode => $country}` + `name="allowed_vendors[{$countryCode}][{$vendorGroup.name}]"` |
| 104-105 | `disableCarrierOptions.tpl` disables **already used** options of the `id_branch` select [VERIFY: …#disabledCarriers] | INCORRECT | the disabled options are the Packeta carriers that are no longer supported, not ones already used. `CarrierAdminForm.php:150-157`: `$enabled = $this->apiRepository->isPacketaCarrierEnabled($carrierId); … 'disabled' => !$enabled`, and the matching error text is "This carrier can no longer be selected. Packeta does not support delivery via this carrier anymore." (:130). Nothing in `getDisabledCarrierIds()` (:69-83) looks at whether an option is used by another shop carrier |
| 106-107 | `newVersionMessage.tpl` formats the update notice with its release notes [VERIFY: …#releaseNotes] | CONFIRMED | `{if $releaseNotes}` … `{$releaseNotes}` :11-13, plus `$newVersion`/`$currentVersion`/`$downloadUrl` |
| 109-111 | `back.js` runs on every back-office page, binds the pickup-point and home-delivery widget buttons, asks for confirmation before posting or cancelling a parcel, and hides the Packeta product tab for virtual products [VERIFY: …#disableTabByProductType] | CONFIRMED | registered in `hookActionAdminControllerSetMedia()` (`packetery.php:1465`, docblock "hook used everywhere in administration"); `.open-packeta-hd-widget` :7 and `.open-packeta-widget` :51; `confirm()` on `#process_post_parcel` / `#process_cancel_packet` :86-95; `disableTabByProductType()` :108-116 hides `#link-ModulePacketery` when the type is `2` |
| 112-113 | `collectionPrintBulkAction.js` submits the hidden bill-of-delivery form as soon as the list page loads [VERIFY: …#collection-print-form] | CONFIRMED | `document.addEventListener('DOMContentLoaded', …)` → `getElementById('collection-print-form')` → `.submit()` |
| 113-115 | `collectionPrint.js` opens the browser print dialog on the printable page [VERIFY: …#print] | CONFIRMED | the whole file is `window.print();` |
| 119-120 | The controllers hold no business logic and resolve their services from the module DI container | CONFIRMED | every service is obtained via `$module->diContainer->get(...)`; the controllers only build lists, messages and downloads |
| 120-123 | The order grid pulls `PacketSubmitter`, `Labels`, `CsvExporter`, `PacketCanceller`, `Tracking`, `OrderRepository`, `CollectionPrintHandler`, entry points of module id `packetery-order` [VERIFY: …#PacketSubmitter] | CONFIRMED | `use Packetery\Order\…` :17-23 and the `diContainer->get()` calls; `ai-docs/reference/packetery-order.md` front matter `covers: [packetery/libs/Order, …]` |
| 124-126 | `PacketStatusFactory` and `PacketTrackingRepository` of module id `packetery-packet-tracking` supply the status column and the cancel decision [VERIFY: …#PacketStatusFactory] | CONFIRMED | `getTranslatedPacketStatus()` :752-768 and `getActionLinks()` :786-800; `packetery-packet-tracking.md` covers `packetery/libs/PacketTracking` |
| 128-131 | The carrier grid uses `ApiCarrierRepository`, `CarrierRepository`, `CarrierTools`, `CarrierAdminForm` of module id `packetery-carrier`; `CarrierAdminForm` builds the edit form and the per-row warnings [VERIFY: …#CarrierAdminForm] | CONFIRMED | `PacketeryCarrierGridController.php` :57, :61, :230, :157/:171; `renderView()` → `build()`/`getHtml()`, `renderList()` → `getCarrierWarning()` → `$this->warnings[]`; `packetery-carrier.md` covers `packetery/libs/Carrier`, `packetery/libs/ApiCarrier` |
| 131-133 | The log grid resolves `LogRepository` for the action translations and the status list [VERIFY: …#LogRepository] | CONFIRMED | :49 `diContainer->get(LogRepository::class)`, :78 `'list' => $this->logRepository->getActionTranslations()`, :55-58 the status list built from `LogRepository::STATUS_SUCCESS` / `STATUS_ERROR` |
| 133-136 | The carrier grid reads one-shot messages via `MessageManager` and the order grid reads `PACKETERY_LABEL_FORMAT` / `PACKETERY_CARRIER_LABEL_FORMAT` via `ConfigHelper`, both of module id `packetery-tools` [VERIFY: …#MessageManager] | CONFIRMED | carrier grid :120-130 `getMessageClean('info')` / `('warning')`; order grid :541 and :552 `ConfigHelper::get('PACKETERY_CARRIER_LABEL_FORMAT')` / `('PACKETERY_LABEL_FORMAT')`; `packetery-tools.md` covers `packetery/libs/Tools` |
| 137-139 | All three grids ask `VersionChecker` of module id `packetery-module` and put the rendered warning into the list header [VERIFY: …#isNewVersionAvailable] | CONFIRMED | order grid :581-584, carrier grid :146-149, log grid :230-233, each assigning `tpl_list_vars['versionUpdateMessageHtml']`; `packetery-module.md` covers `packetery/libs/Module/` |
| 141-144 | The controllers couple to PrestaShop directly: extend `ModuleAdminController`, describe lists with `_select`/`_join`/`fields_list`, read with `Tools::getValue`, redirect with `Tools::redirectAdmin`, render snippets with their own `Smarty` instances | CONFIRMED | all present; `Tools::redirectAdmin` at `PacketerySettingController.php:17` and `PacketeryOrderGridController.php:521`; `new Smarty()` at :650, :705, :723, :739, :820 — note the log grid declares no `_join` |
| 144-146 | `actionPacketeryOrderGridListingResultsModifier` and `actionPacketeryCarrierGridListingResultsModifier` are named after these controllers | CONFIRMED | `packetery.php:1437`, `:1439` (registration) and `:1611`, `:1744` (handlers); the names match the tab class names |
| 146 | They are fired by the PrestaShop list helper, not by packetery-admin-ui | UNCERTAIN | the "not by packetery-admin-ui" half is verifiable — there is no `Hook::exec` for these names anywhere in `packetery/`. The firing side is PrestaShop core, which is not vendored in this repository, so the attribution to the *list helper* (rather than `AdminController::getList()`) cannot be checked here |
| 147 | Their handlers compute the fallback weight and the carrier zones that the grids display | CONFIRMED | `packetery.php:1616-1625` `Weight\Calculator::getFinalWeight()` → `$order['weight']`; `:1753-1756` `CarrierTools::getZonesAndCountries()` → `$carrier['zones']` / `$carrier['countries']` |
| 151-152 | Two Packeta systems outside the repository, one from the server and one from the employee browser | CONFIRMED | server-side SOAP via `SoapApi`; browser-side widget library via `back.js` |
| 154 | `calls → packeta-soap-api (sync, SOAP)` | CONFIRMED | `diContainer->get(SoapApi::class)` → `getPacketIdsWithCarrierNumbers()` in the order grid; `libs/Module/SoapApi.php` is a `SoapClient` wrapper |
| 155 | `calls → packeta-widget (sync, HTTPS)` | CONFIRMED | `back.js:9` and `:53` `$.getScript(<https widget library url>)` |
| 157-160 | The order grid calls `getPacketIdsWithCarrierNumbers` before rendering the offset form and again before building the PDF; an empty result stops the flow and points the employee at the Packeta log [VERIFY: …#getPacketIdsWithCarrierNumbers] | CONFIRMED | `renderList()` :544 and `processBulkCarrierLabelPdf()` :391 (also `processPrint()` :425); both empty-result branches set a warning naming the Packeta log (:393, :546) and `hasBulkLabelPrintingError = true` |
| 161-162 | The request is sent by the SOAP client of module id `packetery-module`, and the endpoint is the WSDL constant that lives there [VERIFY: SoapApi.php#WSDL_URL] | CONFIRMED | `packetery/libs/Module/SoapApi.php:28` declares `public const WSDL_URL`; note `:51-55` prefers the `_PACKETERY_SOAP_WSDL_URL_` define when it is set, so the constant is the default, not the only possible endpoint |
| 164-166 | `back.js` loads the widget library with `jQuery.getScript` whenever the page contains a widget button and logs a console error when it cannot be loaded [VERIFY: …#getScript] | CONFIRMED | :8-11 (`$widgetHdButton.length === 1`) and :52-56 (`$widgetButton.length === 1`), each with `.fail(function () { console.error(…) })` |
| 166 | The library address is a literal in that file | CONFIRMED | hardcoded string arguments of `$.getScript` at :9 and :53 |
| 166-168 | The picked pickup point or the validated address is written back into the hidden form fields `pickup_point` and `address` | CONFIRMED | `back.js:39` `$('.packetery form input[name="address"]').val(...)`, `:73` `input[name="pickup_point"]`; both declared `<input type="hidden">` in `views/templates/hook/displayOrderMain.tpl:62` and `:85` |
| 168 | The visible parts of the page are rewritten from the widget result | CONFIRMED | `back.js:40-45` (`.packetery-street`, `.packetery-city`, `.packetery-zip`, `.packetery-county`, `.packetery-gps`, `.validatedAddress`) and `:74-78` (`.picked-delivery-place`) |
| 169-171 | The buttons, the API key and the widget options come from the order-detail template of module id `packetery-module` [VERIFY: displayOrderMain.tpl#open-packeta-hd-widget] | CONFIRMED | `displayOrderMain.tpl:52-53` and `:82-83` render the two buttons with `data-widget-options="{$widgetOptions}"`; `$widgetOptions` is built in `packetery.php:1273-1320` and carries `'apiKey' => $apiKey`; `packetery-module.md` covers `packetery/views/templates/hook/` |
| 175-177 | The packet status column falls back to an empty cell for an unknown status code, with a TODO naming a database column that does not exist yet [VERIFY: …#TODO] | CONFIRMED | `:759-767` — `if (isset($packetStatuses[$packetStatusCode])) { … } // TODO: after adding a new column code_text to the db … return '';`; `code_text` appears nowhere else in `packetery/` except the twin TODO in `libs/Order/OrderDetailView.php:61` — no schema defines it |
| 178-179 | The order grid's pagination sizes are commented out so it uses the PrestaShop default, while the log grid sets its own `_pagination` [VERIFY: …:114] | CONFIRMED | `PacketeryOrderGridController.php:114` `// $this->_pagination = [20, 50, 100, 300, 1000];`; `PacketeryLogGridController.php:45` `$this->_pagination = [20, 50, 100, 300];` |
| 180-181 | In the same constructor the shop filter overwrites the shop-group filter, because both assignments write the whole `_where` string [VERIFY: …:108] | CONFIRMED | `:104-109` — two consecutive `$this->_where = ' AND …'` assignments, the second (`id_shop`, :108) replacing the first (`id_shop_group`, :105) |
| 183-185 | Each rendering helper creates a fresh `Smarty` instance and fetches a template by a path relative to the controller file, so the templates are outside the PrestaShop template cache and theme overriding [VERIFY: …#getColumnLink] | CONFIRMED | `new Smarty()` + `$smarty->fetch(__DIR__ . '/../../views/templates/admin/…')` at :650-654, :705-711, :723-726, :739-744, :820-826 (and the carrier/log grids); a bare `Smarty` instance is not the PrestaShop context smarty, and `__DIR__` bypasses the override lookup |
| 186-187 | The controllers also instantiate the module class directly instead of receiving it, once per controller instance [VERIFY: …#getModule] | CONFIRMED | `getModule()` in all three grid controllers lazily does `$this->packetery = new Packetery();` (carrier grid :213-220) — note `PacketerySettingController` is the exception, it uses the injected `$this->module` |
| 189-190 | The widget library address is hardcoded in `back.js` [VERIFY: back.js:9] | CONFIRMED | `:9` is the `$.getScript(<url literal>)` call (the same literal is repeated at `:53`) |
| 190-191 | The releases page behind the "Read more" link is hardcoded [VERIFY: newVersionMessage.tpl:15] | CONFIRMED | `:15` is a literal `href` inside `{if $showReadMoreLink === true}` |
| 191-194 | `back.js` toggles the Packeta product tab from a `setTimeout` scheduled 500 ms after load, a documented workaround for PS 1.6 hiding any tab whose label contains "pack" [VERIFY: back.js:98] | CONFIRMED | `:98` is exactly that comment, `:99-106` is `setTimeout(function () { disableTabByProductType(); … }, 500)` |

### manifest.part.packetery-admin-ui.yaml

| entry | class | evidence |
|---|---|---|
| `service: prestashop` | CONFIRMED | matches the `repo: prestashop` front-matter key of every `ai-docs/reference/*.md` |
| `source_commit: b85243bb…` | CONFIRMED | `git rev-parse HEAD` = `b85243bbeb5f9f91c0bde739f5195b2285b867f9`, identical to the document front matter |
| assumptions[0] — generated from branch `docs/packetery` at that commit, tree clean | CONFIRMED | `git rev-parse --abbrev-ref HEAD` = `docs/packetery`; the commit matches. Tree cleanliness at generation time is not re-checkable now (the generated `ai-docs/` files are the only untracked paths) |
| assumptions[1] — routes recorded by tab class name, no path written | CONFIRMED | `Packetery::getAdminLink()` (`packetery.php:1378`) is always called with the tab class name; no route path exists in the module |
| assumptions[2] — `auth: prestashop-employee-tab-permission` names the scheme only; no `viewAccess` override | CONFIRMED | no `viewAccess` in `packetery/controllers/` |
| assumptions[3] — `print`, `cancel`, `submit`, `showCollectionPrint` and the bulk actions are query parameters of the one route | CONFIRMED | `getActionLinkHtml()` :818 `['id_order' => …, 'action' => $action]`; `CollectionPrintHandler.php:60-63` `getAdminLink('PacketeryOrderGrid', ['action' => 'showCollectionPrint'])`; bulk actions are PrestaShop `submitBulk…` POST keys, not registered tabs |
| exposes: `PacketeryOrderGrid` (kind html, consumers [unknown], evidence `…#PacketeryOrderGridController`) | CONFIRMED | class exists at `PacketeryOrderGridController.php:29`; `kind: html` is a permitted value per `templates/manifest.yaml`; notes match `bulk_actions` / `action` dispatch |
| exposes: `PacketeryCarrierGrid` (evidence `…#PacketeryCarrierGridController`) | CONFIRMED | class at `PacketeryCarrierGridController.php:18`; `viewcarrier` → `CarrierAdminForm` at :170-176 |
| exposes: `PacketeryLogGrid` (evidence `…#PacketeryLogGridController`) | CONFIRMED | class at `PacketeryLogGridController.php:14`; `id_order` narrowing at :225-227 |
| exposes: `PacketerySetting` (evidence `…#initContent`) | CONFIRMED | `PacketerySettingController::initContent()` :13-20 redirects to `AdminModules` |
| calls: `packeta-soap-api` (kind other, evidence `…#getPacketIdsWithCarrierNumbers`) | CONFIRMED | the symbol is called in `PacketeryOrderGridController.php` at :391, :425, :544 and defined in `libs/Module/SoapApi.php:184`; `other` is a permitted `kind` |
| calls: `packeta-widget` (kind other, evidence `back.js#getScript`) | CONFIRMED | `$.getScript` at `back.js:9` and `:53`, purpose matches the pickup-point / home-delivery widget usage |
| unknowns[0] — which employee profiles may open the four tabs | CONFIRMED | genuinely undeterminable from code: the tabs are created without permission rows (`Installer::insertMenuItems`) and no controller sets access |
| unknowns[1] — which back-office pages besides the order detail carry the widget buttons | CONFIRMED | `back.js` is loaded on every admin page (`packetery.php:1465`) but only guards on `.open-packeta-widget` / `.open-packeta-hd-widget`; the only markup in this repo is `views/templates/hook/displayOrderMain.tpl` |

CONFIRMED 82 · INCORRECT 2 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/reference/packetery-admin-ui.md` (and in its manifest fragment where the row pointed there). `confidence` was set to `reviewed`; `verified` needs another pass by someone who did not write or fix the document.
