## ai-docs/reference/packetery-checkout.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 25–27 | Puts the pickup-point widget into the delivery step and keeps the choice in the cart until the order is placed | CONFIRMED | `front.js:325` `PacketaModule.ajax.savePickupPointInCart(...)` → `checkout.php` `case 'savePickupPointInCart'` → `OrderSaver::savePickupPointInCartGetJson()` (`packetery/libs/Order/OrderSaver.php:224`), row keyed by cart |
| 27–30 | One front controller receives the posts and hands each to the class that persists or renders the result [checkout.php#PacketeryCheckoutModuleFrontController] | CONFIRMED | `packetery/controllers/front/checkout.php` — single `switch (Tools::getValue('action'))` with three `diContainer->get(...)` branches |
| 31–33 | The PHP services answer: pickup point still acceptable, delivery country, age-limited goods, weight in kilograms | CONFIRMED | `PickupPointValidator::validate`, `AddressTools::getCountryFromCart`, `CartService::isAgeVerificationRequired`, `Weight\Converter::getKilograms` |
| 35–37 | `widget.tpl` renders the button plus a hidden input per widget field [widget.tpl#packeta-branch-id] | CONFIRMED | `widget.tpl:5-53` — `#open-packeta-widget` and ten hidden inputs incl. `class="packeta-branch-id"` |
| 37–40 | `widgetHd.tpl` renders the home-delivery variant and records the outcome in `addressValidated` [widgetHd.tpl#addressValidated] | CONFIRMED | `widgetHd.tpl:11` `<input type="hidden" id="addressValidated" …>`; button `open-packeta-widget-hd` labelled "Validate delivery address" |
| 40–43 | `display-before-carrier.tpl` carries the page configuration as a JSON attribute and starts the JS once delivery options are on the page [#packetaModuleConfig] | CONFIRMED | `display-before-carrier.tpl:11-19` — `data-packetaModuleConfig`, `JSON.parse(...)`, `PacketaModule.runner.onBeforeCarrierLoad()`; that runner returns unless `module.findDeliveryOptions().length !== 0` |
| 45 | 411 lines of PHP in 10 types with 18 public methods | CONFIRMED | `ai-docs/ast/map.json`: the 10 covered PHP files (index.php guards excluded) sum to 411 lines, 10 types, 18 methods |
| 45–46 | The storefront JavaScript is not covered by the AST map | CONFIRMED | `map.json` contains 0 files with a `.js` path |
| 46–49 | Controller runs with `$auth = false`, `$ajax = true`; the only gate is `token` vs `Tools::getToken('ajax_front')`, a mismatch returns with no output [checkout.php#display] | CONFIRMED | `checkout.php:18-32` — `public $auth = false; public $ajax = true;` and `if ($token !== $real_token) { return; }` |
| 51 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 55–56 | One module front controller `checkout`, dispatched by the `action` request value into three branches | CONFIRMED | `checkout.php:33-47` — three `case` labels, no `default` |
| 60 | `savePickupPointInCart` sets the JSON content type and echoes `OrderSaver::savePickupPointInCartGetJson` | CONFIRMED | `checkout.php:35-38` `header('Content-Type: application/json'); echo $orderSaver->savePickupPointInCartGetJson();` |
| 61 | `fetchExtraContent` echoes `Cart::packeteryCreateExtraContent`, the extra content PS 1.6 cannot hook | CONFIRMED | `checkout.php:39-42`; `front.js:56-58` calls `addAllExtraContents` only when `PacketaModule.tools.isPS16()` |
| 62 | `saveAddressInCart` calls `Ajax::saveAddressInCart`, which writes the validated address itself; no response body | CONFIRMED | `checkout.php:43-46` (no `echo`); `packetery/libs/Order/Ajax.php:41-75` writes the `packetery_order` row and returns void |
| 68 | `PickupPointValidator::validate(PickupPointValidateRequest)` — gets API key, calls the API, logs, returns a valid response on failure | CONFIRMED | `PickupPointValidator.php:56-93` — both `catch` blocks `return new PickupPointValidateResponse(true, [])` after `logRepository->insertRow(..., 'error')` |
| 69 | `createPickupPointValidateRequest(array, CartCore, array)` builds the request from order row, cart and carrier row | CONFIRMED | `PickupPointValidator.php:95-145` signature and body |
| 70 | `PickupPointValidate::createWithValidApiKey($apiKey, HttpClientWrapper)` is a wrapper over a private constructor | CONFIRMED | `PickupPointValidate.php:31` `private function __construct(...)`, `:44` `public static function createWithValidApiKey(...)` |
| 71 | `CartService::isAgeVerificationRequired(CartCore): bool` — true as soon as one product has the for-adults attribute [#isForAdults] | CONFIRMED | `CartService.php:26-42` — `$isAgeVerificationRequired = $productAttributes->isForAdults(); if (...) { break; }` |
| 72 | `AddressTools::hasValidatedAddress(array): bool` — non-empty `zip` counts as a validated address | CONFIRMED | `AddressTools.php:19-23` `return (bool) $orderData['zip'];` |
| 73 | `AddressTools::getCountryFromCart(CartCore): string` — lowercase ISO code, empty string without a delivery address | CONFIRMED | `AddressTools.php:30-39` `strtolower(\CountryCore::getIsoById(...))`, `return '';` |
| 74 | `Calculator::getComputedOrDefaultWeight(OrderCore)` — converts, substitutes the default package weight at zero, adds packaging weight, null when the sum stays zero | CONFIRMED | `Calculator.php:18-38` |
| 75 | `Calculator::getFinalWeight(array)` — stored weight when set, otherwise the computed one | CONFIRMED | `Calculator.php:49-58` `if ($packeteryOrder['weight'] !== null) { return (float) …; }` |
| 76 | `Converter::getKilograms($value)` — multiplies by the unit factor, null for an unmapped unit | CONFIRMED | `Converter.php:27-36` |
| 77 | `Converter::isKgConversionSupported(): bool` — true when the shop unit has a factor | CONFIRMED | `Converter.php:41-46` `return isset(self::$mapping[$unit]);` |
| 79–80 | "The remaining public members are the constructors and the two `getSubmittableData` accessors" | INCORRECT | Of the 18 public methods, 11 are named in the two tables; the 7 left are 4 constructors + 2 `getSubmittableData` **+ `PickupPointValidate::validate(PickupPointValidateRequest): PickupPointValidateResponse`** (`PickupPointValidate.php:48`), which the enumeration omits |
| 84–88 | Without a usable API key the validator logs under `LogRepository::ACTION_PICKUP_POINT_VALIDATE` and returns a response reporting the point as valid | CONFIRMED | `PickupPointValidator.php:58-70`; `LogRepository.php:28` `const ACTION_PICKUP_POINT_VALIDATE = 'pickup-point-validate';` |
| 88–90 | The same for any exception during the request; only an explicit invalid answer blocks the order | CONFIRMED | `PickupPointValidator.php:82-92` catch-all; `ActionValidateStepComplete.php:96-100` blocks only on `isValid() === false` |
| 92–94 | Country from the cart delivery address, carrier id from `CarrierTools::findExternalCarrierId` with `CarrierVendors::INTERNAL_PICKUP_POINT_CARRIER` as fallback | CONFIRMED | `PickupPointValidator.php:100-103`; `CarrierTools.php:116`; `CarrierVendors.php:16` `= 'packeta'` |
| 94–97 | Internal point sent as `id`, external one as `carrierId` + `carrierPickupPointId` | CONFIRMED | `PickupPointValidator.php:139-143` `new ValidatedPoint($externalCarrierId === null ? $idBranch : null, $externalCarrierId, …)` |
| 97–100 | `allowed_vendors` expanded into one entry per country and group; `VENDOR_GROUP_ZPOINT` sent without a group key | CONFIRMED | `PickupPointValidator.php:111-125` `if ($vendorGroup !== CarrierVendors::VENDOR_GROUP_ZPOINT) { $vendorOptions['group'] = $vendorGroup; }` |
| 100–102 | `livePickupPoint` mirrors `CartService::isAgeVerificationRequired` | CONFIRMED | `PickupPointValidator.php:133` passes `$this->cartService->isAgeVerificationRequired($cart)` into the 6th `ValidatedOptions` parameter `?bool $livePickupPoint` |
| 104–107 | `getSubmittableData` runs `array_filter` over the own properties, dropping null/false/empty | CONFIRMED | `ValidatedOptions.php:82`, `ValidatedPoint.php:38` `return array_filter(get_object_vars($this));` |
| 107–110 | Answer accepted only as a JSON object with boolean `isValid` and array `errors`, otherwise `HttpRequestException` | CONFIRMED | `PickupPointValidate.php:62-76` — three `throw new HttpRequestException(...)` branches |
| 116–118 | `front.js` loads the widget library asynchronously, binds on document ready, stops when the page carries no module config [#onThisScriptLoad] | CONFIRMED | `front.js:23-49` `$.getScript(...)`, `$.when(...).done`, `$(PacketaModule.runner.onDocumentLoad)`, `if (typeof PacketaModule.config === 'undefined') return;` |
| 118–121 | Manager walks a fixed adapter list, keeps existing globals, picks the first `isActive()`; `Unknown` deliberately last [#supportedModules] | CONFIRMED | `front.js:583-616` incl. comment "order is important because of false positives (Unknown has to be last)" |
| 125 | `PacketeryCheckoutModulePs16` / `.delivery_option input:checked` / PS 1.6 + delivery options | CONFIRMED | `ps16.js:10-17` |
| 126 | `PacketeryCheckoutModulePs17` / `.delivery-option input:checked` / no version test | CONFIRMED | `ps17.js:6-18` |
| 127 | `PacketeryCheckoutModuleSupercheckout` / `#shipping-method input:checked` | CONFIRMED | `supercheckout.js:8-18` |
| 128 | `PacketeryCheckoutModuleOpcZelarg` / `.delivery_option input:checked` / PS 1.6 + `carriers_section` form | CONFIRMED | `opczelarg.js:11-25` `$('form#carriers_section .delivery_option input')` |
| 129 | `PacketeryCheckoutModuleHummingbird` selects `.js-delivery-option input:checked` | INCORRECT | `hummingbird.js:13` `return $('.js-delivery-option input[id^=delivery_option]:checked');` — the quoted selector drops the `[id^=delivery_option]` filter (the PS 8/9 activation test in the same row is correct: `hummingbird.js:8-11`) |
| 130 | `PacketeryCheckoutModuleUnknown` / `.delivery_option.selected input` | CONFIRMED | `unknown.js:14-16` |
| 132–133 | Every adapter answers the same three questions (inputs, selected input, enable/disable submit) | CONFIRMED | all six files define `findDeliveryOptions`, `getSelectedInput`, `enableSubmitButton`, `disableSubmitButton` |
| 133–135 | Widget markup found by element id `packetery-carrier-` + carrier id [#getWidgetParent] | CONFIRMED | `front.js:618-620` `$('#packetery-carrier-' + this.getCarrierId($selectedInput))`; matches `widget.tpl:5` / `widgetHd.tpl:6` |
| 135–136 | On a delivery change the submit control is disabled first | CONFIRMED | `front.js:87` `module.disableSubmitButton();` with the "just in case this script dies" comment |
| 136–138 | "enabled again only when the carrier is not a Packeta one, when a branch id is present, or when address validation is satisfied" | INCORRECT | Every remaining path ends in `PacketaModule.ui.toggleSubmit()` (`front.js:131,136`), which enables whenever `isPickupPointInvalid` **and** `isAddressValidationUnsatisfied` are false; `isAddressValidationUnsatisfied` needs `addressValidationSetting === 'required'` (`front.js:485-490`). So an HD carrier with `optional`/no validation level gets the submit control re-enabled with no branch id and no validated address |
| 138–140 | PS 1.6 fetches the extra content per carrier over AJAX and caches it per carrier id [#extraContentCache] | CONFIRMED | `front.js:189,215-230` — `extraContentCache: {}`, `'pending'` marker, `fetchExtraContent(carrierId).done(...)` |
| 140–142 | Validated addresses kept per carrier in a page-lifetime object and re-sent on switch back | CONFIRMED | `front.js:566-578` `PacketaModule.storage.addresses = {}`; `front.js:100,133-134` re-posts `validatedAddress` via `saveAddressInCart` |
| 144–147 | `Packeta.Widget.pick` gets API key from config, language, app identity, vendors from `#widget_vendors` or a country; age flag adds `livePickupPoint` [#initializeWidget] | CONFIRMED | `front.js:294-316` |
| 147–149 | The pick callback copies `id`, `name`, `currency`, `pickupPointType`, `carrierId`, `carrierPickupPointId` into the hidden inputs of `widget.tpl` | CONFIRMED | `front.js:326-330` writes `.packeta-branch-id/-name/-currency`, `.packeta-pickup-point-type`, `.packeta-carrier-id`, `.packeta-carrier-pickup-point-id`; all six classes exist in `widget.tpl:32-44` |
| 148–150 | …and copies `place`, `street`, `city`, `zip` into the hidden inputs of `widget.tpl` | INCORRECT | `front.js:331-334` writes `.packeta-place`, `.packeta-street`, `.packeta-city`, `.packeta-zip`, but `widget.tpl:45-53` declares `packeta-point-place`, `packeta-point-street`, `packeta-point-city`, `packeta-point-zip`; a repo-wide grep finds `packeta-place/-street/-city/-zip` only in `front.js` (also read back at `front.js:113-116`), so these four values never reach an input |
| 149–150 | …and posts them to the checkout controller | CONFIRMED | `front.js:339-360` `savePickupPointInCart(...)` with `pickupPoint.place/street/city/zip` taken straight from the widget result |
| 150–152 | The HD button opens the same widget with the `hd` layout and one carrier id [#open-packeta-widget-hd] | CONFIRMED | `front.js:374-395` `layout: 'hd'`, `carrierId: widgetCarriers` with the comment "there will always be one carrier". Note: `#widget_carriers` (`front.js:380`) exists in no template in the repo |
| 152–153 | The returned address is accepted only when its country matches the delivery country, otherwise the country-differs message is shown | CONFIRMED | `front.js:406-424` `if (address.country === country) {…} else { … .text(PacketaModule.config.countryDiffersMessage); }` |
| 153–154 | …"and leaves the submit control disabled" | INCORRECT | The else branch calls `PacketaModule.ui.toggleSubmit()` (`front.js:425`), which disables only when `isAddressValidationUnsatisfied` is true, i.e. only for `addressValidationSetting === 'required'` (`front.js:485-490`); with `optional` the button is enabled after a country mismatch |
| 158 | Reaches exactly one system outside the repository | CONFIRMED | both outbound calls go to the same host: `PickupPointValidate.php:22` and `front.js:26` |
| 163–165 | `PickupPointValidate::validate` posts the request JSON with the API key added to `URL_VALIDATE_ENDPOINT` and reads back `isValid` + `errors` | CONFIRMED | `PickupPointValidate.php:48-78` `$postData['apiKey'] = $this->apiKey; … $this->httpClient->post(self::URL_VALIDATE_ENDPOINT, $options)` |
| 165–167 | The call carries no customer identity, only country, carrier, age flag, vendor list and the point identification | CONFIRMED | `PickupPointValidator.php:127-144` — the only non-null `ValidatedOptions` fields are country, carriers, livePickupPoint, vendors; `ValidatedPoint` holds id / carrierId / carrierPickupPointId |
| 167–169 | The browser reaches the same system again to pull the widget library [#onThisScriptLoad] | CONFIRMED | `front.js:26` `$.getScript(<same host as URL_VALIDATE_ENDPOINT>)` |
| 169–170 | "Both addresses are values and are not copied here" | CONFIRMED | neither the document nor the fragment quotes the URL or the host |
| 172–175 | The controller resolves three collaborators from `$this->module->diContainer`: `OrderSaver` and `Ajax` of packetery-order, `Packetery\Module\Cart` of packetery-module | CONFIRMED | `checkout.php:36,40,44`; `packetery/libs/Order/*` is covered by `ai-docs/reference/packetery-order.md`, `packetery/libs/Module/` by `packetery-module.md` |
| 175–177 | `PickupPointValidator` is constructed with `ConfigHelper`, `LogRepository`, `HttpClientWrapper` (packetery-tools) and the local `CartService` [#__construct] | INCORRECT | The constructor has five parameters: `__construct(ConfigHelper $configHelper, LogRepository $logRepository, \Packetery $module, CartService $cartService, HttpClientWrapper $httpClient)` (`PickupPointValidator.php:42-48`); the enumeration omits `\Packetery $module`, which is a dependency on packetery-module and is used at `:63` for the translated log message |
| 178 | Reaches into packetery-carrier for `CarrierTools` and `CarrierVendors` | CONFIRMED | `PickupPointValidator.php:16-17` uses; `packetery/libs/Carrier` is covered by `packetery-carrier.md` |
| 178–180 | `CartService` depends on `ProductAttributeRepository` of packetery-module [#__construct] | CONFIRMED | `CartService.php:21-24`; `packetery/libs/Product/` is in the `covers` of `packetery-module.md` |
| 180–181 | `PickupPointValidateRequest` and `PickupPointValidateResponse` live in packetery-order | CONFIRMED | namespaces `Packetery\Request` / `Packetery\Response`; `packetery-order.md` covers `packetery/libs/Request`, `packetery/libs/Response` |
| 183–186 | `ActionValidateStepComplete` calls `PickupPointValidator` and `AddressTools::hasValidatedAddress` before the step completes | CONFIRMED | `ActionValidateStepComplete.php:87-89` and `:103` |
| 186–188 | `hookDisplayBeforeCarrier` builds the JSON page configuration the storefront JS reads [packetery.php#packetaModuleConfig] | CONFIRMED | `packetery/packetery.php:899,941` `$this->context->smarty->assign('packetaModuleConfig', json_encode([...]))` |
| 188–189 | packetery-order uses `Weight\Calculator` when it exports an order | CONFIRMED | `packetery/libs/Order/OrderExporter.php:30,73` `$this->weightCalculator->getFinalWeight($packeteryOrder)` |
| 190–193 | Controller extends `ModuleFrontController`, templates are Smarty files fetched by the main module class, storefront code assumes jQuery | CONFIRMED | `checkout.php:15`; `packetery.php:974` `smarty->fetch(... display-before-carrier.tpl)`; `front.js` uses `$`/`$.ajax` throughout |
| 193–197 | Three config keys via `Configuration`: `PS_WEIGHT_UNIT`, `PACKETERY_DEFAULT_PACKAGE_WEIGHT`, `PACKETERY_DEFAULT_PACKAGING_WEIGHT` | CONFIRMED | `Converter.php:29,43` `\Configuration::get('PS_WEIGHT_UNIT')`; `Calculator.php:20-21` `ConfigHelper::get(...)`, which delegates to `\Configuration::get` (`ConfigHelper.php:45-66`) |
| 198–200 | The extra validation round is switched by `ConfigHelper::isApiWidgetValidationModeEnabled`, read by the hook before the validator runs | CONFIRMED | `ActionValidateStepComplete.php:80-89`; `ConfigHelper.php:164-167` |
| 204–207 | Validation fails open: missing key, transport error or unparsable answer all yield a valid response | CONFIRMED | `PickupPointValidator.php:64-70` and `:82-92`. Note: the anchor points at `PickupPointValidate.php#validate`, which only throws; the permissive response is built in `PickupPointValidator.php` |
| 209–211 | Supercheckout declares `toggleExtracContent` while the shared code reads `module.toggleExtraContent`, so the value never arrives | CONFIRMED | `supercheckout.js:36` `toggleExtracContent: true`; `front.js:168-170` `if (typeof module.toggleExtraContent !== 'undefined')` |
| 211–214 | The same adapter cannot enable/disable the submit button; both methods are empty and a validator callback is registered instead | CONFIRMED | `supercheckout.js:21-23` `enableSubmitButton: function () {}, disableSubmitButton: function () {}`; `:39-56` `addSupercheckoutOrderValidator(...)` |
| 216–217 | The `Unknown` adapter is marked as a removal candidate and untestable | CONFIRMED | `unknown.js:8` `// todo remove? Untestable.` |
| 217–220 | The PS 1.6 extra-content path carries an open task about address validation and adds markup only for pickup-point carriers | CONFIRMED | `front.js:209-214` `// TODO: fix address validation in PS 1.6` and `PacketaModule.config.deliveryPointCarrierIds.indexOf(carrierId) >= 0` guard |
| 220–222 | The AJAX helper toggles a `wait` class whose owning checkout module the comment calls unknown [#beforeSend] | CONFIRMED | `front.js:509-511` `// todo: To which checkout module does this css class belong? …` + `$("body").toggleClass("wait")` |
| 222–224 | The automatic widget opening keeps a commented-out guard with an open question | CONFIRMED | `front.js:450-453` `// todo PePa: how could we reach this point with widget already open?` above the commented `$('iframe #packeta-widget')` condition |
| 226–229 | Weight conversion limited to mapped units; other units make `Calculator` treat the order weight as zero, only default weights can save it | CONFIRMED | `Calculator.php:66-73` `if (Converter::isKgConversionSupported()) { … } return 0.0;` combined with `:23-30` |

### manifest.part.packetery-checkout.yaml

| entry | class | evidence |
|---|---|---|
| `service: prestashop` | CONFIRMED | `ai-docs/ast/map.json` `"repo": "prestashop"`, same value in the sibling fragments |
| `source_commit: b85243b…` | CONFIRMED | matches the document front matter and `git log -1` on `docs/packetery` |
| assumption 1 — generated on branch `docs/packetery`, clean tree at b85243b | CONFIRMED | `git status --porcelain` shows no modified tracked file, only the untracked generated `ai-docs/` output; HEAD is b85243b on `docs/packetery` |
| assumption 2 — counts from `ai-docs/ast/map.json`, `index.php` guards excluded | CONFIRMED | recomputed: 411 lines / 10 types / 18 methods only when the six `index.php` entries are dropped |
| assumption 3 — storefront JS not in the AST map, read from source | CONFIRMED | `map.json` holds no `.js` file |
| assumption 4 — the three `exposes` are one route branching on `action` | CONFIRMED | `checkout.php:33-47` single `switch (Tools::getValue('action'))`; the three names are copied verbatim |
| exposes `checkout?action=savePickupPointInCart` (rest, auth csrf-token, evidence anchor, notes) | CONFIRMED | `checkout.php:35-38` sets JSON content type; the `Tools::getToken('ajax_front')` gate at `:28-31`; caller `front.js:526-556` |
| exposes `checkout?action=fetchExtraContent` (html, auth csrf-token, notes about PS 1.6) | CONFIRMED | `checkout.php:39-42`; only `front.js:56-58` (PS 1.6 path) calls it |
| exposes `checkout?action=saveAddressInCart` — notes: "the handler writes the answer itself and the controller echoes nothing" | INCORRECT | `Ajax::saveAddressInCart()` (`packetery/libs/Order/Ajax.php:41-75`) produces no output at all — it only writes the `packetery_order` row and returns; nothing writes a response body for this action |
| calls `packeta-widget` (rest, evidence `PickupPointValidate.php#URL_VALIDATE_ENDPOINT`) | CONFIRMED | `PickupPointValidate.php:22,58` — POST of JSON to that constant |
| unknown 1 — which themes `PacketeryCheckoutModuleUnknown` supports | CONFIRMED | `unknown.js:8` marks it untestable and a removal candidate; no other source names its themes |
| unknown 2 — which checkout module owns the `wait` CSS class | CONFIRMED | `front.js:509-510` comment states it is not Supercheckout PS 1.7, PS 1.7, PS 1.6 5-step nor OPC; the class appears in no template of this repository |

CONFIRMED 82 · INCORRECT 7 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/reference/packetery-checkout.md` (and in its manifest fragment where the row pointed there). `confidence` was set to `reviewed`; `verified` needs another pass by someone who did not write or fix the document.
