## ai-docs/reference/packetery-tools.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 18 | declares 26 types with 52 public methods | CONFIRMED | Counted in the covered dirs: Tools 9 + Tools/Exception 1 + Log 1 + LogWrapper 1 + Exceptions 14 = 26 classes (index.php stubs excluded per the fragment's assumption). Public methods incl. constructors: ConfigHelper 10, DbTools 10, Tools 2, Logger 2, ControllerWrapper 3, HttpClientWrapper 2, MessageManager 3, JsonStructureValidator 2, CheckoutControllerUrlProvider 2, PrestashopLogWrapper 2, LogRepository 9, AggregatedException 2, VersionCheckerException 1, InvalidApiKeyException 2 = 52 |
| 18 | "in four namespaces" | INCORRECT | Those 26 types live in five namespaces. `packetery/libs/Tools/Exception/InvalidApiKeyException.php:9` `namespace Packetery\Tools\Exception;` — the document itself names it at line 101 as a namespace distinct from `Packetery\Tools` |
| 18–21 | the roles of `Packetery\Tools`, `Packetery\Log`, `Packetery\LogWrapper`, `Packetery\Exceptions` | CONFIRMED | Tools holds ConfigHelper/DbTools/HttpClientWrapper/MessageManager/ControllerWrapper/JsonStructureValidator; `libs/Log/LogRepository.php:38` `public static $tableName = 'packetery_log';`; `libs/LogWrapper/PrestashopLogWrapper.php:30` calls `\PrestaShopLogger::addLog`; `libs/Exceptions/` holds 14 exception classes |
| 21–24 | almost every class is a thin adapter over a PrestaShop global class, so version differences stay in one place [VERIFY: ControllerWrapper.php#registerStylesheet] | CONFIRMED | `ControllerWrapper.php:43-50` — `registerStylesheet` branches on `Tools::version_compare(_PS_VERSION_, '1.7.0.0', '<')`; same pattern in `Tools.php:32` for 1.7.6 |
| 22–23 | "written so that the rest of the code does not call `Configuration`, `Db` or `ToolsCore` directly" | INCORRECT | The rest of the module does call them directly: `libs/Weight/Converter.php:31` `$unit = strtolower(\Configuration::get('PS_WEIGHT_UNIT'));`, `libs/AbstractFormService.php:112` `\Configuration::get('PS_LANG_DEFAULT')`, `libs/Module/Uninstaller.php:163` `\Configuration::deleteByName(ConfigHelper::KEY_APIPASS)` (module keys, not PS core keys), `libs/Module/VersionChecker.php:172` `\Tools::version_compare(...)`. The `Db` half holds (only `DI/ContainerFactory.php:21` and one upgrade script use `Db::getInstance()`) |
| 26–28 | `ConfigHelper` decides per key whether a value is global or per shop and falls back to pre-3.0 values [#getConfigBehavior] | CONFIRMED | `ConfigHelper.php:150-157` `getConfigBehavior`; `:59-64` retries for group alone, then unscoped, with the comment "try to get value set in older module version" |
| 28–30 | `DbTools` turns a silent DB error into a thrown `DatabaseException` and writes the failing query to the file log first | CONFIRMED | `DbTools.php:72-76` `logAndThrow`: `$this->logger->logToFile($this->db->getMsgError() . ', query: ' . $query); throw new DatabaseException(...)` after `getNumberError()` |
| 32–33 | the classes carry no hook handler, no controller and no route | CONFIRMED | No `hook*` method, no `*Controller`/`ModuleFrontController` subclass in `libs/Tools`, `libs/Log`, `libs/LogWrapper`, `libs/Exceptions` |
| 33–35 | all files except `CheckoutControllerUrlProvider` and `CheckoutControllerUrlException` start with the `_PS_VERSION_` guard | CONFIRMED | Scan of the four dirs: the only class files without the guard are `libs/Tools/CheckoutControllerUrlProvider.php` and `libs/Exceptions/CheckoutControllerUrlException.php`. (The four `index.php` directory stubs also have no guard; they are the stubs the fragment's assumption excludes.) |
| 37 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 41–43 | keys are declared as constants on `ConfigHelper` and read/written through `ConfigHelper::get` / `::update` rather than through PrestaShop `Configuration` | CONFIRMED | `ConfigHelper.php:18-28` the 11 `KEY_*` constants; no `\Configuration::get`/`updateValue` on a declared key anywhere outside `ConfigHelper`. (Only removal bypasses it: `Uninstaller.php:163-176` uses `\Configuration::deleteByName`.) |
| 44–45 | the values are written by the installer and by the module's settings form | CONFIRMED | `libs/Module/Installer.php:248-257` `ConfigHelper::update(...)`; `packetery.php:333` `Packetery\Tools\ConfigHelper::update($option, $configValue);` |
| 49 | `KEY_APIPASS` = `PACKETERY_APIPASS`, read by `getApiPass` and `getValidApiKey`, which cuts the first 16 characters off | CONFIRMED | `ConfigHelper.php:18`, `:104-107` `getApiPass`, `:174-187` `getValidApiKey` → `:130-133` `getApiKeyFromApiPass` returns `substr($apiPass, 0, 16)` |
| 50 | `KEY_ESHOP_ID` = `PACKETERY_ESHOP_ID`, read by the order export as the sender label; only key marked `BEHAVIOR_SEPARATE` | CONFIRMED | `ConfigHelper.php:19`, `:33-36` the only entry in `$configBehavior`; `libs/Order/OrderExporter.php:80` `$senderLabel = (string) ConfigHelper::get('PACKETERY_ESHOP_ID', $packeteryOrder['id_shop_group'], $idShop);`, used at `:118` |
| 51 | `KEY_ID_PREFERENCE` = `PACKETERY_ID_PREFERENCE`, read by `isOrderNumberByReference`, compared against `Packetery::ID_PREF_REF` | CONFIRMED | `ConfigHelper.php:26`, `:159-162` `return self::get(self::KEY_ID_PREFERENCE) === \Packetery::ID_PREF_REF;` |
| 52 | `KEY_WIDGET_VALIDATION_MODE` = `PACKETERY_WIDGET_VALIDATION_MODE`, read by `isApiWidgetValidationModeEnabled` | CONFIRMED | `ConfigHelper.php:25`, `:164-167` |
| 53 | `KEY_USE_PS_CURRENCY_CONVERSION` = `PACKETERY_USE_PS_CURRENCY_CONVERSION`, read by the order export when it converts the COD amount | CONFIRMED | `ConfigHelper.php:24`; `OrderExporter.php:194` and `:232` gate the conversion, and the converted total feeds the COD value at `:55-60` (`$codResolver->resolveCodValue($packeteryOrder, (float) $total)`) |
| 54 | `KEY_SHOW_CONSIGN_PASSWORD` = `PACKETERY_SHOW_CONSIGN_PASSWORD`, read by the consign-password settings | CONFIRMED | `ConfigHelper.php:27`; `libs/Order/ConsignPasswordSettings.php:32` |
| 55 | `KEY_CONSIGN_PASSWORD_RETRIEVAL` = `PACKETERY_CONSIGN_PASSWORD_RETRIEVAL`, same class, chooses immediate vs cron | CONFIRMED | `ConfigHelper.php:28`; `ConsignPasswordSettings.php:36-39` compares with `MODE_CRON`, else `MODE_IMMEDIATE` |
| 57–58 | four further keys cache the version check; `ConfigHelper` declares them, the version checker writes and reads them | CONFIRMED | `ConfigHelper.php:20-23`; `VersionChecker.php:64,74-79` write, `:154,170,186-191` read |
| 62 | `KEY_LAST_VERSION_CHECK_TIMESTAMP` = `PACKETERY_LAST_VERSION_CHECK_TIMESTAMP`, read to decide whether the next check is due | CONFIRMED | `ConfigHelper.php:20`; `VersionChecker.php:152-159` `shouldCheckApi` |
| 63 | `KEY_LAST_VERSION` = `PACKETERY_LAST_VERSION`, compared against the installed version | CONFIRMED | `ConfigHelper.php:22`; `VersionChecker.php:167-176` `isNewVersionAvailable` |
| 64 | `KEY_LAST_VERSION_URL` = `PACKETERY_LAST_VERSION_URL`, assigned to the admin template as the download link | CONFIRMED | `ConfigHelper.php:21`; `VersionChecker.php:186` `$smarty->assign('downloadUrl', ConfigHelper::get(ConfigHelper::KEY_LAST_VERSION_URL));` |
| 65 | `KEY_LAST_RELEASE_NOTES` = `PACKETERY_LAST_RELEASE_NOTES`, assigned to the admin template | CONFIRMED | `ConfigHelper.php:23`; `VersionChecker.php:191-199` |
| 67–71 | `ConfigHelper::get` has two behaviours: unlisted key read with all scope arguments `null`; `BEHAVIOR_SEPARATE` read per group+shop with two retries | CONFIRMED | `ConfigHelper.php:47-49` `\Configuration::get($key, null, null, null)`; `:51-64` group/shop then group, then unscoped |
| 72–73 | other configuration keys are used as string literals outside `ConfigHelper` and are not declared here | CONFIRMED | e.g. `PACKETERY_LABEL_FORMAT`, `PACKETERY_CRON_TOKEN`, `PACKETERY_DEFAULT_PACKAGE_PRICE` appear only as literals (`Uninstaller.php:165-175`, `controllers/front/cron.php`), none of them declared on `ConfigHelper` |
| 77–78 | 52 public methods as plain PHP classes; no route, console command or public HTTP surface | CONFIRMED | no controller/command class and no route registration in the four covered dirs |
| 82 | `ConfigHelper` — 10 public methods | CONFIRMED | `get, getMultiple, update, getApiPass, getApiKey, getApiKeyFromApiPass, getBackendLanguage, getConfigBehavior(private), isOrderNumberByReference, isApiWidgetValidationModeEnabled, getValidApiKey` → 10 public |
| 83 | `DbTools` — 10 public methods, named `getRows, getRow, getValue, execute, insert, update, delete, executeQueries, getPairs` | CONFIRMED | `DbTools.php:29,42,86,105,126,144,167,192,218,238` — 9 named + constructor = 10 |
| 83 | "each of them raising `DatabaseException` on failure" | INCORRECT | `getPairs` neither touches the database nor throws: `DbTools.php:238-241` `return array_combine(array_column($result, $indexKey), array_column($result, $valueKey));` — no `@throws`, no `logAndThrow` |
| 84 | `Tools` — 2 methods; `getValue` re-implements the 1.7.6 request reader for older releases; `sanitizeFloatValue` normalises commas and spaces | CONFIRMED | `Tools.php:28-52` with the comment about stripslashes and JSON in POST; `:54-57` `str_replace([',', ' '], ['.', ''], $value)` |
| 85 | `Logger` — 2 methods; appends one timestamped line, returns `false` when file or directory is not writable | CONFIRMED | `Logger.php:29-49`: writability checks return `false`, `fwrite(..., date('Y-m-d H:i:s') . ' ' . $message . PHP_EOL)` |
| 86 | `ControllerWrapper` — 3 methods; falls back to `addJS`/`addCSS` below 1.7.0.0 | CONFIRMED | `ControllerWrapper.php:19,29,43`; `:32` `addJS`, `:46` `addCSS` |
| 87 | `HttpClientWrapper` — 2 methods; a client per call, body as string | CONFIRMED | `HttpClientWrapper.php:32-42,50-60` — `HttpClient::create($options)` inside each method, `return $result->getContent();` |
| 88 | `MessageManager` — 3 methods; one message per error level in the cookie under the `packetery_` prefix; `getMessageClean` reads and unsets | CONFIRMED | `MessageManager.php:16` `PREFIX = 'packetery_'`, `:21,34,45`; `:48-49` `__get` then `__unset` |
| 89 | `JsonStructureValidator` — 2 methods; checks decoded JSON against a nested array of type names, recursing | CONFIRMED | `JsonStructureValidator.php:30,46`; `:52-55` recursion; `:70-88` `isValidType` |
| 90 | `CheckoutControllerUrlProvider` — 2 methods; builds the front checkout link and appends `action=`, honouring friendly URLs and multistore | CONFIRMED | `CheckoutControllerUrlProvider.php:21,49`; `:36` `getModuleLink(\Packetery::MODULE_SLUG, 'checkout')`, `:53-57` appends `&action=`/`?action=` |
| 91 | `PrestashopLogWrapper` — 2 static methods with `LEVEL_INFO/WARNING/ERROR` | CONFIRMED | `PrestashopLogWrapper.php:16-18,26,39` |
| 93–94 | "The eleventh helper class, `LogRepository`, contributes the remaining 9 public methods" | INCORRECT | The count 9 is right (`LogRepository.php:44,55,68,94,114,133,141,159,169`), but it is not the remainder: the table's ten classes hold 38, so 38 + 9 = 47, not 52. The other 5 are on the exception classes — `AggregatedException` 2 (`AggregatedException.php:24,32`), `VersionCheckerException` 1 (`:19`), `InvalidApiKeyException` 2 (`:17,22`) |
| 94–96 | the classes are resolved from the container by class name and `ControllerWrapper` is wired with the current PrestaShop controller | CONFIRMED | `packetery.php:1005` `$this->diContainer->get(Packetery\Tools\ControllerWrapper::class)`; `DI/ContainerFactory.php:24-26` registers `\ControllerCore::class` as `$module->getContext()->controller`; `DI/Container.php:55-65` autowires by reflection |
| 100–102 | 14 classes in `Packetery\Exceptions` and `InvalidApiKeyException` in `Packetery\Tools\Exception` | CONFIRMED | `libs/Exceptions/` holds exactly 14 class files (index.php aside); `libs/Tools/Exception/InvalidApiKeyException.php:9` |
| 102–103 | all extend `\Exception` except `InvalidApiKeyException`, which extends `\RuntimeException` | CONFIRMED | every class in `libs/Exceptions` declares `extends \Exception`; `InvalidApiKeyException.php:15` `class InvalidApiKeyException extends \RuntimeException` |
| 104–108 | the list of the 14 exception names | CONFIRMED | the 14 names match the files in `libs/Exceptions/` one for one |
| 110–112 | `AggregatedException` wraps a list of exceptions and hands it back through `getExceptions` | CONFIRMED | `AggregatedException.php:16-35` |
| 112–114 | `VersionCheckerException` offers a named constructor for an unusable releases response | CONFIRMED | `VersionCheckerException.php:19-22` `createForInvalidLatestReleaseResponse`, used at `VersionChecker.php:103,123,146` |
| 116–118 | `Logger::logToFile` appends to a file in the module root and is the only place that receives the failing SQL statement | CONFIRMED | `Logger.php:21` `__DIR__ . '/../../packetery_error.log'` (= module root); `DbTools.php:69,74` are the only calls that pass `', query: ' . $query` |
| 118–120 | `PrestashopLogWrapper` writes into the PrestaShop log with a severity, `logException` folds message, file and line into one line | CONFIRMED | `PrestashopLogWrapper.php:26-34`; `:41-42` `"{$message} Error: {$exception->getMessage()} in {$exception->getFile()}:{$exception->getLine()}"` |
| 120–121 | the `packetery_log` table records business actions | CONFIRMED | `LogRepository.php:22-30` nine `ACTION_*` constants (packet sending, label print, …) |
| 121–123 | `DbTools` writes the query to the file log, then throws a `DatabaseException` whose message points at that log instead of repeating the statement | CONFIRMED | `DbTools.php:74-75` — `throw new DatabaseException($this->db->getMsgError() . ', see details in Packeta log');` |
| 124–126 | `executeQueries` is the one method that swallows the exception, copies the message into the PrestaShop log and continues, or stops when asked | CONFIRMED | `DbTools.php:44-57` — `catch (DatabaseException $exception) { \PrestaShopLogger::addLog(...); if ($returnFalseOnException) { return false; } }` |
| 130–131 | one owned table `packetery_log`, created and dropped by `LogRepository` from SQL built with the shop table prefix | CONFIRMED | `LogRepository.php:125-152` `getPrefixedTableName()` = `_DB_PREFIX_ . self::$tableName`, used by `getCreateTableSql`/`getDropTableSql` (the statements are executed by the installer/uninstaller) |
| 132 | the repository is the only writer; the admin log grid reads the same table directly | CONFIRMED | only `LogRepository::insert`/`purge` touch the table for write; `controllers/admin/PacketeryLogGridController.php:29` `$this->table = 'packetery_log';` with `list_no_link` and a plain `_select`; consistent with `ai-docs/reference/packetery-admin-ui.md:31` ("read-only list") |
| 136 | `id` int(11) auto increment, primary key | CONFIRMED | `LogRepository.php:144` `` `id` int(11) NOT NULL AUTO_INCREMENT ``, `:150` `PRIMARY KEY  (\`id\`)` |
| 137 | `order_id` int(10) nullable; `insertRow` stores `null` for `0` or `'0'` | CONFIRMED | `LogRepository.php:145` `` `order_id` int(10) NULL ``; `:98` `'order_id' => $orderId === 0 \|\| $orderId === '0' ? null : $orderId` |
| 138 | `params` text not null; parameter array as JSON with unescaped unicode, escaped for SQL | CONFIRMED | `LogRepository.php:146`; `:99` `$this->dbTools->db->escape(json_encode($params, JSON_UNESCAPED_UNICODE))` |
| 139 | `status` varchar(20) default empty; `STATUS_SUCCESS` or `STATUS_ERROR` | CONFIRMED | `LogRepository.php:147`, `:19-20` |
| 140 | `action` varchar(45) default empty; one of the nine `ACTION_` constants | CONFIRMED | `LogRepository.php:148`, `:22-30` |
| 141 | `date` datetime not null, written from `DateTimeImmutable` in the PHP default timezone | CONFIRMED | `LogRepository.php:149`; `:102` `(new \DateTimeImmutable('now'))->setTimezone(new \DateTimeZone(date_default_timezone_get()))` |
| 143–146 | the nine action constants, by name | CONFIRMED | `LogRepository.php:22-30` — the nine names match exactly |
| 146–147 | `getActionTranslations` maps each of them to a translated label | CONFIRMED | `LogRepository.php:68-81` — all nine constants are keys of the returned map |
| 147–148 | `getTranslatedAction` returns the raw value when a label is missing | CONFIRMED | `LogRepository.php:55-63` `if (!isset($translations[$action])) { return $action; }` |
| 148–150 | `purge` deletes rows older than a number of days supplied by the caller | CONFIRMED | `LogRepository.php:159-162` `'\`date\` < DATE_SUB(NOW(), INTERVAL ' . (int) $logExpirationDays . ' DAY)'` |
| 150 | `hasAnyByOrderId` answers whether an order has any log row | CONFIRMED | `LogRepository.php:169-172` `SELECT "1" ... WHERE order_id = ...` |
| 153–157 | one outbound counterparty of its own: `calls → mysql (sync, SQL)` | CONFIRMED | matches the fragment's single `calls` entry; no other outbound counterparty is fixed in these classes |
| 159–161 | every `DbTools` statement goes to the shop MySQL over the `Db` instance the container hands it; the module opens no connection of its own | CONFIRMED | `DbTools.php:29-33` takes `\Db` by constructor; `DI/ContainerFactory.php:20-22` `return \Db::getInstance();` — no other connection code in the module |
| 161–163 | `HttpClientWrapper` makes outbound HTTP calls but URL and options are arguments, so the counterparty is the caller's choice | CONFIRMED | `HttpClientWrapper.php:32,50` `get($url, array $options = [])` / `post($url, array $options = [])` |
| 163–167 | `Tools` extends `\ToolsCore`, `ControllerWrapper` wraps `\ControllerCore`, `MessageManager` writes into the cookie, `PrestashopLogWrapper` calls `PrestaShopLogger`, `HttpClientWrapper` builds on the Symfony HTTP client | CONFIRMED | `Tools.php:17`; `ControllerWrapper.php:16-22`; `MessageManager.php:37` `$context->cookie->__set($key, $message);`; `PrestashopLogWrapper.php:30`; `HttpClientWrapper.php:14` `use Symfony\Component\HttpClient\HttpClient;` |
| 171–173 | `getValidApiKey` is declared `: string` but returns `false` when no credential is stored | CONFIRMED | `ConfigHelper.php:174-179` `public function getValidApiKey(): string { $apiPass = $this->getApiPass(); if ($apiPass === false) { return false; } ...` |
| 174–175 | `getApiKey` is marked deprecated in favour of `getValidApiKey` and is still present | CONFIRMED | `ConfigHelper.php:110-123` `@deprecated` / `@see getValidApiKey` |
| 176–177 | `LogRepository::insertRow` defaults its status argument to a string literal instead of `STATUS_SUCCESS` | CONFIRMED | `LogRepository.php:94` `public function insertRow($action, array $params, $status = 'success', $orderId = null)` |
| 178–180 | `HttpClientWrapper` catches the typed Symfony failures and rethrows a plain `\Exception`, so a caller cannot tell a transport error from any other failure | CONFIRMED | `HttpClientWrapper.php:37-38` `catch (TransportExceptionInterface $e) { throw new \Exception($e->getMessage()); }`; `post` catches the wider `ExceptionInterface` at `:55` |
| 181–182 | `MessageManager` keeps one message per error level because the cookie cannot hold arrays | CONFIRMED | `MessageManager.php:27` "We are working with one message, Cookie does not allow arrays."; `:62` key = `PREFIX . $errorLevel` |
| 183–184 | the file log has no rotation and no size limit; `Logger` only appends | CONFIRMED | `Logger.php:38` `fopen($this->errorLogFilePath, 'ab')` — no truncation, size check or rotation anywhere in the class |

### manifest.part.packetery-tools.yaml

| entry | class | evidence |
|---|---|---|
| `service: prestashop` | CONFIRMED | matches the document front matter (`repo: prestashop`) and the sibling fragments (`manifest.part.packetery-order.yaml:1`) |
| `source_commit: b85243bb…` | CONFIRMED | `git rev-parse HEAD` = `b85243bbeb5f9f91c0bde739f5195b2285b867f9`, same as the document front matter |
| assumption: documented from branch `docs/packetery` at that commit with a clean working tree | CONFIRMED | branch `docs/packetery`, HEAD as above; the only untracked paths are the generated `ai-docs/` outputs themselves, no source change |
| assumption: counts come from `ai-docs/ast/map.json` and exclude the `index.php` guard stubs | CONFIRMED | `ai-docs/ast/map.json` exists; my own count reproduces 26 types / 52 public methods only when the four `index.php` stubs are excluded |
| assumption: `store: mysql/prestashop` names engine and logical database only | CONFIRMED | no database name is resolvable in the module; `DI/ContainerFactory.php:21` takes whatever `\Db::getInstance()` returns |
| assumption: configuration values are never copied, only key names | CONFIRMED | the document quotes key names and constants only; no stored value appears anywhere in it |
| `calls: mysql` (kind db, evidence `DbTools.php#execute`) | CONFIRMED | `DbTools.php:144-154` `execute` runs `$this->db->execute($sql, $useCache)`; every other DbTools method goes to the same `\Db` |
| `owns_data: packetery_log` (evidence `LogRepository.php#getCreateTableSql`, notes on writer/purge/grid) | CONFIRMED | `LogRepository.php:141-152` create SQL; `:94-120` `insertRow`/`insert`; `:159-162` `purge`; `PacketeryLogGridController.php:29` reads it |
| unknown: lifetime of the module's error log file | CONFIRMED | nothing in `Logger` or its callers rotates, truncates or deletes `packetery_error.log`; only `fopen(..., 'ab')` at `Logger.php:38` |
| unknown: retention period for `packetery_log`, `purge` takes days as argument with no default | CONFIRMED | `LogRepository.php:159` `public function purge($logExpirationDays)` — no default. The scheduling caller named as `resolved_by` exists outside this module: `libs/Cron/Tasks/PurgeLogs.php:43-45` |

CONFIRMED 78 · INCORRECT 4 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/reference/packetery-tools.md` (and in its manifest fragment where the row pointed there). `confidence` was set to `reviewed`; `verified` needs another pass by someone who did not write or fix the document.
