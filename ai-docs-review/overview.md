## ai-docs/overview.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17 | prestashop is a single PrestaShop module connecting a shop to the Packeta delivery network | CONFIRMED | One module directory `packetery/` with `packetery/packetery.php` as the module file; `manifest.yaml` `description` says the same. |
| 17–20 | class `Packetery` extends `CarrierModule`, declares name `packetery` and the version, supports PrestaShop from 1.7.7.0 upwards [VERIFY: packetery/packetery.php#ps_versions_compliancy] | CONFIRMED | `packetery/packetery.php:19` `class Packetery extends CarrierModule`; `:74` `$this->name = 'packetery';`; `:76` `$this->version = '3.4.0';`; `:103` `$this->ps_versions_compliancy = ['min' => '1.7.7.0', 'max' => _PS_VERSION_];` |
| 20–24 | pickup point / validated home address at checkout, carrier pairing, packet submission, labels, bill of delivery, packet-status polling [VERIFY: packetery/packetery.php#getModuleHooksList] | CONFIRMED | `getModuleHooksList` at `packetery.php:1422` registers `displayBeforeCarrier`, `displayCarrierExtraContent`, `actionValidateOrder`, `actionCarrierUpdate`; the remaining features are `packetery/libs/Carrier/CarrierRepository.php:239 setPacketeryCarrier`, `packetery/libs/Order/PacketSubmitter.php`, `packetery/libs/Order/Labels.php`, `packetery/libs/Order/CollectionPrintService.php` ("Print bill of delivery", `PacketeryOrderGridController.php:208`), `packetery/libs/PacketTracking/PacketTrackingCron.php`. |
| 26–29 | no runtime of its own; every entry point is a hook handler, a registered admin controller or a registered front controller [VERIFY: packetery/libs/Module/Installer.php:52] | CONFIRMED | `Installer.php:52` `&& $this->module->registerHook($this->module->getModuleHooksList())`, `:53` `insertMenuItems()`; entry points are only `packetery/controllers/admin/*Controller.php` (4) and `packetery/controllers/front/{checkout,cron}.php`. |
| 29–30 | ships no server, no queue consumer and no scheduler | CONFIRMED | No daemon/bin/worker entry point in the tree; `manifest.yaml` unknown "Which scheduler calls the `cron` front controller" is resolved "outside the repository". |
| 31 | `> ⚠ add business context (elicitation)` | PENDING | Standard elicitation placeholder. |
| 35 | prestashop consists of 7 modules | CONFIRMED | `manifest.yaml` `modules:` lists exactly 7 entries. |
| 35–36 | all 7 passed the threshold for a standalone `reference/` document, so overview carries no module summary | CONFIRMED | `ai-docs/reference/` holds 7 files, one per module name. |
| 36–40 | boundaries not declared in the code; one module of about 9 700 lines of logic, above the 3 000-line ceiling, split along namespaces and directories; `path` names the primary directory | CONFIRMED | Module lines sum to 9735 (2940+2567+1195+940+939+743+411); `manifest.yaml` assumptions repeat both statements verbatim. |
| 42–50 | module table (name, path, lines, document) | CONFIRMED | Every row equals the matching `manifest.yaml` `modules[]` entry (name, path, lines, doc); all 7 link targets exist under `ai-docs/reference/`. |
| 52–54 | counts come from `ai-docs/ast/map.json` and leave out the `index.php` guard stubs, the migration scripts and the translation files [VERIFY: ai-docs/ast/map.md:19] | CONFIRMED | Recomputed from `map.json`: 11397 total − 416 (`index.php` stubs) − 493 (`packetery/upgrade/*`) − 710 (`translations/cs.php`+`sk.php`) − 43 (root tooling PHP) = 9735, and each module total is reproduced exactly (e.g. controllers/admin = 940, libs/Carrier+libs/ApiCarrier = 1195). `map.md:19` is the project totals row `\| prestashop \| . \| 189 \| 11397 \| …`. |
| 58–59 | starts when PrestaShop includes `packetery/packetery.php`, which requires `packetery/autoload.php` first | CONFIRMED | `packetery/packetery.php:15` `require_once dirname(__FILE__) . '/autoload.php';` — the first statement after the `_PS_VERSION_` guard. |
| 59–61 | autoload registers an `spl_autoload_register` callback mapping the `Packetery` namespace onto `packetery/libs/` [VERIFY: packetery/autoload.php#spl_autoload_register] | CONFIRMED | `packetery/autoload.php:12–19` `spl_autoload_register(static function ($class) { $filePath = __DIR__ . '/libs/' . str_replace(['\\\\', 'Packetery'], ['/', ''], $class) . '.php'; …});` |
| 62–63 | and then defines the constant `PACKETERY_PLUGIN_DIR` [VERIFY: packetery/packetery.php#PACKETERY_PLUGIN_DIR] | CONFIRMED | `packetery/packetery.php:17` `defined('PACKETERY_PLUGIN_DIR') \|\| define('PACKETERY_PLUGIN_DIR', dirname(__FILE__));` — after the require on line 15. |
| 63 | `PACKETERY_PLUGIN_DIR` is the constant "that every path in the module is built from" | INCORRECT | The constant has exactly one use in the whole module: `packetery/libs/Cron/Tasks/DeleteLabels.php:51` `glob(PACKETERY_PLUGIN_DIR . '/labels/*.pdf', GLOB_NOSORT)`. Other paths are built from `__DIR__` (`autoload.php:14`), `dirname(__FILE__)` (`packetery.php:15`) and `$this->_path` (`packetery.php:1464`). |
| 63–64 | the constructor builds the dependency container "before anything else" | INCORRECT | `packetery/packetery.php:71` `public function __construct()`, then `:74 $this->name = 'packetery';` … `:76 $this->version = '3.4.0';` and three more property assignments run before `:81 $this->diContainer = Packetery\DI\ContainerFactory::create($this);`. |
| 64–65 | stores the container on the public property `diContainer` [VERIFY: packetery/libs/DI/ContainerFactory.php#create] | CONFIRMED | `packetery.php:69` `public $diContainer;`, `:81` assignment; `ContainerFactory.php:16` `public static function create(\Packetery $module): Container`. |
| 65–67 | the main module file deliberately uses no `use` statements, because PrestaShop 1.6 cannot load a main module file that has them [VERIFY: packetery/packetery.php:8] | CONFIRMED | `packetery/packetery.php:8` `* Do not use "use" PHP keyword. PS 1.6 can not load main plugin files with the keyword in them.`; the file has no `use` statement. |
| 69 | the keys in the table below "switch behaviour on at load time" | INCORRECT | None of the six is read at module load. `packetery/libs/Order/OrderExporter.php:194,232` read `KEY_USE_PS_CURRENCY_CONVERSION` during order export; `packetery/libs/Order/ConsignPasswordSettings.php:32` reads `KEY_SHOW_CONSIGN_PASSWORD` when the order detail is rendered; `packetery/libs/Module/VersionChecker.php:58` reads `_PACKETERY_DEBUG_LOG_` inside an exception handler. |
| 73 | `PACKETERY_WIDGET_AUTOOPEN` read in `packetery/packetery.php`, value in the shop's `configuration` table [VERIFY: packetery/packetery.php#widgetAutoOpen] | CONFIRMED | `packetery.php:962` `'widgetAutoOpen' => (bool) Packetery\Tools\ConfigHelper::get('PACKETERY_WIDGET_AUTOOPEN'),`; `ConfigHelper::get` delegates to `\Configuration::get` (`ConfigHelper.php:47`). |
| 74 | `PACKETERY_WIDGET_VALIDATION_MODE` read in `packetery/libs/Tools/ConfigHelper.php` [VERIFY: …#isApiWidgetValidationModeEnabled] | CONFIRMED | `ConfigHelper.php:164–166` `public static function isApiWidgetValidationModeEnabled(): bool { return (bool) self::get(self::KEY_WIDGET_VALIDATION_MODE); }` |
| 75 | `PACKETERY_USE_PS_CURRENCY_CONVERSION` read in `packetery/libs/Tools/ConfigHelper.php` [VERIFY: …#KEY_USE_PS_CURRENCY_CONVERSION] | INCORRECT | `ConfigHelper.php:24` only declares the constant; the readers are `packetery/libs/Order/OrderExporter.php:194` and `:232` and `packetery/packetery.php:1232`. `ai-docs/reference/packetery-tools.md:56` also names "the order export" as the reader. |
| 76 | `PACKETERY_ID_PREFERENCE` read in `packetery/libs/Tools/ConfigHelper.php` [VERIFY: …#KEY_ID_PREFERENCE] | CONFIRMED | `ConfigHelper.php:161` `return self::get(self::KEY_ID_PREFERENCE) === \Packetery::ID_PREF_REF;` |
| 77 | `PACKETERY_SHOW_CONSIGN_PASSWORD` read in `packetery/libs/Tools/ConfigHelper.php` [VERIFY: …#KEY_SHOW_CONSIGN_PASSWORD] | INCORRECT | `ConfigHelper.php:27` only declares the constant; the reader is `packetery/libs/Order/ConsignPasswordSettings.php:32` `if ((bool) ConfigHelper::get(ConfigHelper::KEY_SHOW_CONSIGN_PASSWORD) === false)`. `ai-docs/reference/packetery-tools.md:57` names "the consign-password settings of the order module". |
| 78 | `_PACKETERY_DEBUG_LOG_` read in `packetery/libs/Module/VersionChecker.php`, a PHP constant defined by the shop outside this repository [VERIFY: packetery/libs/Module/VersionChecker.php:58] | CONFIRMED | `VersionChecker.php:58` `&& (defined('_PACKETERY_DEBUG_LOG_') && _PACKETERY_DEBUG_LOG_ === true)` — the only occurrence in the repository, and it is never defined here. |
| 80–81 | the keys that point at Packeta or at GitHub are in `dependencies.md` | CONFIRMED | `ai-docs/dependencies.md:97` `PACKETERY_APIPASS` and `:100` `_GITHUB_ACCESS_TOKEN_`. |
| 81–82 | "the full key list with its readers is in `reference/packetery-tools.md`" | INCORRECT | That page covers 11 `ConfigHelper` constants only and states it itself: `ai-docs/reference/packetery-tools.md` — "Other configuration keys of the module are used as string literals outside `ConfigHelper` and are not declared here." The code holds ~24 further keys (`PACKETERY_CRON_TOKEN`, `PACKETERY_LABEL_FORMAT`, `PACKETERY_ADDRESS_VALIDATION`, `PACKETERY_PACKET_STATUS_TRACKING_*`, …). |
| 86–87 | three groups of files belong to no module and are excluded from the line counts | CONFIRMED | The excluded PHP set of `map.json` is exactly `packetery/upgrade/*` (493), `packetery/translations/{cs,sk}.php` (710) and the root tooling files (43), plus the `index.php` stubs named in the modules section. (Non-PHP `docs/`, `doc/` and `README.md` carry no line count and are outside the map.) |
| 87–89 | migration scripts in `packetery/upgrade/` are one file per released version, each with an `upgrade_module_*` function PrestaShop calls on a newer version [VERIFY: packetery/upgrade/upgrade-3.4.0.php#upgrade_module_3_4_0] | CONFIRMED | 14 files `upgrade-2.1.3.php` … `upgrade-3.4.0.php`; `upgrade-3.4.0.php:17` `function upgrade_module_3_4_0(Packetery $module): bool`. |
| 89–90 | they are described in `reference/packetery-module.md` | CONFIRMED | `ai-docs/reference/packetery-module.md:104–106` describes the same scripts with the same anchor. |
| 90 | the migration scripts are "never edited after release" | INCORRECT | `git log -- packetery/upgrade/upgrade-2.1.3.php` lists 8 later commits, including content changes `317712c PES-1374: min version 1.7` and `13ca49e PES-422: improved PHP compatibility`, besides the licence/CS-fixer passes `7279534`, `b0fe025`. |
| 91–93 | `translations/cs.php` and `sk.php` are generated key-value files PrestaShop fills from the `l()` calls [VERIFY: packetery/translations/cs.php:1] | CONFIRMED | `packetery/translations/cs.php:7–8` `global $_MODULE; $_MODULE = [];` followed by `$_MODULE['<hash>'] = '…'` rows keyed by the module's `l()` strings; only `cs.php`, `sk.php` and the `index.php` stub are present, matching `Installer::TRANSLATED_LANGUAGES = ['cs', 'sk']`. |
| 95–96 | the coding-standard rule set in the repository root [VERIFY: phpcs.xml:1] | CONFIRMED | `/phpcs.xml:1–3` `<ruleset … name="PHP_CodeSniffer"><description>Prestashop coding standards.</description>`. |
| 96–97 | the PHP-CS-Fixer configuration that loads the PrestaShop preset [VERIFY: PacketeryCsFixerConfig.php#getRules] | CONFIRMED | `PacketeryCsFixerConfig.php:3` `use PrestaShop\CodingStandards\CsFixer\Config;`, `:5` `class PacketeryCsFixerConfig extends Config`, `:7–9` `public function getRules(): array { $rules = parent::getRules();` |
| 97–98 | the static-analysis configuration under `phpstan/` | CONFIRMED | `phpstan/phpstan.neon` and `phpstan/phpstan-baseline.neon` exist. |
| 98–99 | they shape the checks declared as Composer scripts and never run in a shop [VERIFY: composer.json:22] | CONFIRMED | `composer.json:15–31` `"scripts"` with `check:phpcs`, `check:phpcsfixer`, `phpstan`; line 22 is `"fix:phpcsfixer": "vendor/bin/php-cs-fixer fix --config=.php-cs-fixer.dist.php"`. The files sit outside `packetery/` and are not shipped to a shop. |

CONFIRMED 28 · INCORRECT 7 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/overview.md`. `confidence` was set to `reviewed`.
