## ai-docs/reference/packetery-carrier.md @ b85243bbeb5f9f91c0bde739f5195b2285b867f9 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 8 | `covers: [packetery/libs/Carrier, packetery/libs/ApiCarrier]` | CONFIRMED | Both directories exist; `Carrier/` holds `CarrierAdminForm.php`, `CarrierRepository.php`, `CarrierTools.php`, `CarrierVendors.php`, `ApiCarrier/` holds `ApiCarrierRepository.php`, `Downloader.php`. |
| 17–21 | The pairing is one row of `packetery_address_delivery`, written by a single method that flips `is_module`/`external_module_name` on selection and clears them on removal [#setPacketeryCarrier] | CONFIRMED | `CarrierRepository.php:239` `setPacketeryCarrier(...)`; `:256` `['is_module' => 0, 'external_module_name' => null, ...]` on removal, `:290` `['is_module' => 1, 'external_module_name' => 'packetery', 'need_range' => 1]` otherwise. |
| 23–26 | A downloader reads the feed, validates that every record carries the fourteen keys the mapper expects, and stores the result in `packetery_carriers` [#validateCarrierData] | CONFIRMED | `Downloader.php:148-174` a single `isset()` over exactly 14 keys (`id, name, country, currency, pickupPoints, apiAllowed, separateHouseNumber, customsDeclarations, requiresEmail, requiresPhone, requiresSize, disallowsCod, maxWeight, available`) — the same 14 `carriersMapper` reads; `ApiCarrierRepository.php:77` `public static $tableName = 'packetery_carriers';`. |
| 26–29 | The internal rows `\Packetery::ZPOINT` and `\Packetery::PP_ALL` are appended with default values before the save [#addNonApiCarriers] | CONFIRMED | `ApiCarrierRepository.php:129-138` fills both ids from `array_column(self::$columnDefinitions, 'defaultPPValue')`; called at `:151` before the insert/update loop. |
| 29–31 | Records that stop appearing in the feed are flagged, not deleted [#setOthersAsDeleted] | CONFIRMED | `ApiCarrierRepository.php:238-242` `update(self::$tableName, ['deleted' => 1], '`id` NOT IN (...)')`. |
| 33–36 | `pickup_point_type` is `internal` for `\Packetery::ZPOINT`, `external` otherwise, or address delivery [#getPickupPointType] | CONFIRMED | `CarrierAdminForm.php:578-588`. |
| 36–39 | `address_validation` (`none`, `required`, `optional`) applies to home delivery, offered only for a country in `ADDRESS_VALIDATION_COUNTRIES` [#ADDRESS_VALIDATION_COUNTRIES] | CONFIRMED | `CarrierRepository.php:20` `['CZ', 'SK']`; `CarrierAdminForm.php:236-271` the radio with those three values is added only when `is_pickup_points` is false and a carrier country matches. |
| 39–42 | `allowed_vendors` JSON narrows the widget to vendor groups per country and is translated into the widget vendor parameter [#getWidgetParameter] | CONFIRMED | `CarrierVendors.php:120-147` decodes the JSON, skips countries `!== $customerCountry`, emits `['group' => …, 'country' => …, 'selected' => true]`. |
| 44 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder. |
| 48–49 | 53 public methods on 6 classes, six of them constructors, so 47 carry behaviour | CONFIRMED | Manual count: 13 + 14 + 3 + 12 + 4 + 7 = 53 public methods incl. 6 `__construct`; `ai-docs/ast/map.json` records the same per-class counts for the 6 types. |
| 49–50 | The classes group into a repository pair over the two tables, an admin form, a vendor catalogue and PrestaShop-facing helpers | CONFIRMED | `CarrierRepository`/`ApiCarrierRepository`, `CarrierAdminForm`, `CarrierVendors`, `CarrierTools`. |
| 54 | `CarrierRepository` method list; `setPacketeryCarrier` decides insert/update/delete and keeps the PrestaShop `carrier` row in step | CONFIRMED | All 12 named methods are public in `CarrierRepository.php`; `:254-292` branches over `existsById` into `deleteById`/`updatePacketery`/`insertPacketery` and always calls `updatePresta`. |
| 55 | `ApiCarrierRepository` method list; owns the table incl. its DDL; `isPacketaCarrierEnabled` treats an empty selection as valid | CONFIRMED | All 13 named methods public; `getCreateTableSql`/`getDropTableSql` at `:169`/`:191`; `:351` `if ($carrierId === '' || $carrierId === null) { return true; }`. |
| 56 | `Downloader` exposes `run`, `validateCarrierData`; `run` never throws on a failed download — returns a message array with `class` `danger` or `success` | CONFIRMED | `Downloader.php:54-94`: `try { fetchAsArray() } catch (\Exception $e) { return [... 'class' => 'danger'] }`, success path `'class' => 'success'`. |
| 57 | `CarrierAdminForm` method list; renders two `HelperForm` forms and handles `submitCarrierForm` / `submitCarrierOptionsForm` | CONFIRMED | `CarrierAdminForm.php:110-114` `build()` adds both; `:131` and `:229` the two `\Tools::isSubmit(...)` checks; `new \HelperForm()` at `:161` and `:334`. |
| 57 | …"and redirects back to the grid after each save" | INCORRECT | Both saves redirect to the carrier's own edit screen, not the list: `:390`/`:428` `\Tools::redirectAdmin($this->tools->getEditLink($this->carrierId));` and `CarrierTools.php:94-101` builds that link with `['id_carrier' => $carrierId, 'viewcarrier' => 1]`, which `PacketeryCarrierGridController::renderView()` (`:168-171`) answers with the edit form again. |
| 58 | `CarrierVendors` method list; fixed vendor-group catalogue, converts stored `allowed_vendors` into widget entries for one customer country | CONFIRMED | `CarrierVendors.php:57-112` (fixed array), `:120-147`. |
| 59 | `CarrierTools` method list; resolves active countries from the carrier's zones and answers two questions about a stored order row | CONFIRMED | `CarrierTools.php:43-61` (`$zoneCountry['active']` filter), `:104` `orderSupportsAgeVerification`, `:116` `findExternalCarrierId`. |
| 61–64 | Internal pickup-point entries are dropped unless the PrestaShop carrier serves a country that has them [#getAvailableCarriers] | CONFIRMED | `CarrierAdminForm.php:526-536`: when `!$hasInternalCountry`, `ZPOINT` is unset and `PP_ALL` too unless `$hasPickupPointCountry`. |
| 62–64 | A carrier that is no longer available stays selectable only when it is the one already stored | CONFIRMED | `CarrierAdminForm.php:152` `if ($carrierId === null || $enabled === true || $carrierId === $carrierData['id_branch'])`. Note: this second filter is in `buildCarrierForm`, not in the anchored `getAvailableCarriers`. |
| 64–66 | Disabled options are greyed out by a Smarty template rendered into the form [#renderDisableCarriersScript] | CONFIRMED | `CarrierAdminForm.php:94-100` fetches `views/templates/admin/disableCarrierOptions.tpl` (file present); injected as the `html` input at `:181-185`. |
| 66–69 | The second form shows the address-validation radio, the vendor checkboxes or the COD radio depending on the paired catalogue entry, and renders nothing when none applies [#buildCarrierOptionsForm] | CONFIRMED | `CarrierAdminForm.php:236` / `:272` / `:284` branches; `:310` `if ($formInputs === []) { return null; }`. |
| 73–76 | Two tables, both in the shop database with the PrestaShop prefix; `packetery_address_delivery` is the shop's own decision, `packetery_carriers` the local copy of the feed rewritten on every download | CONFIRMED | Both queried via `_DB_PREFIX_`; `ApiCarrierRepository::save` re-writes every feed row on each run. |
| 80 | `packetery_address_delivery.id_carrier` int, primary key; renumbered on carrier replacement [#swapId] | CONFIRMED | `Installer.php:203` `` `id_carrier` int NOT NULL PRIMARY KEY ``; `CarrierRepository.php:164-169`. |
| 81 | `id_branch` varchar(255), catalogue id — numeric or an internal constant [#getInternalPickupPointCarriers] | CONFIRMED | `Installer.php` `` `id_branch` varchar(255) NOT NULL ``; `CarrierRepository.php:130-132` filters on `\Packetery::ZPOINT` / `\Packetery::PP_ALL`. |
| 82 | `pickup_point_type` varchar(40), nullable — `internal`, `external` or `NULL` [#getPickupPointType] | CONFIRMED | `Installer.php` `` `pickup_point_type` varchar(40) NULL ``; `CarrierAdminForm.php:578-588`. |
| 83 | `address_validation` varchar(40), nullable — set only for home delivery, defaulting to `none`, forced to `NULL` for a pickup-point carrier [#setPacketeryCarrier] | CONFIRMED | `Installer.php` `` `address_validation` varchar(40) NULL ``; `CarrierRepository.php:271-278`. |
| 84 | `allowed_vendors` text, nullable — JSON country→vendor groups, all allowed by default [#getDefaultAllowedVendors] | CONFIRMED | `Installer.php` `` `allowed_vendors` text NULL ``; `CarrierAdminForm.php:687-701` `// Allow all by default.` + `json_encode`. |
| 85 | `currency_branch` char(3), nullable — copied from the catalogue entry, cleared for the two internal ids [#setPacketeryCarrier] | CONFIRMED | `Installer.php` `` `currency_branch` char(3) NULL ``; `CarrierRepository.php:279-283`. |
| 86 | `packetery_carriers.id` varchar(255), unique — feed id or an internal constant [#getCreateTableSql] | CONFIRMED | `ApiCarrierRepository.php:172` `` `id` varchar(255) NOT NULL ``, `:187` `UNIQUE (`id`)`. |
| 87 | `is_pickup_points` boolean, mapped from the feed key `pickupPoints`; drives the form branch [#carriersMapper] | CONFIRMED | `ApiCarrierRepository.php:20-24` `'apiName' => 'pickupPoints'`; `CarrierAdminForm.php:236`, `:581`. |
| 87 | …"and the checkout" | UNCERTAIN | No checkout code reads `is_pickup_points`; its only readers in the repo are `CarrierAdminForm.php:236,581,583`. It reaches the checkout only indirectly, through the derived `pickup_point_type`. |
| 88 | `disallows_cod` boolean, mapped from `disallowsCod`; hides the COD radio in the options form [#disallows_cod] | CONFIRMED | `ApiCarrierRepository.php:55-59`; `CarrierAdminForm.php:284` `if ((bool) $apiCarrier['disallows_cod'] === false && (bool) $carrierData['is_cod'] === true)`. |
| 89 | `deleted` boolean, set on every row missing from the last feed instead of removing it [#setOthersAsDeleted] | CONFIRMED | `ApiCarrierRepository.php:238-242`. |
| 91–95 | The remaining 11 catalogue columns (`name`, `country`, `currency`, `max_weight`, `available`, `has_carrier_direct_label`, `separate_house_number`, `customs_declarations`, `requires_email`, `requires_phone`, `requires_size`) are declared in the same DDL | CONFIRMED | `ApiCarrierRepository.php:171-188` declares 15 columns; the four covered by the table above plus these 11 is exactly the set. |
| 93–95 | …"and filled from the matching feed keys by one mapping table" [#columnDefinitions] | INCORRECT | Only the boolean columns go through `$columnDefinitions`: `ApiCarrierRepository.php:113-117` `if (isset($columnOptions['type']) && $columnOptions['type'] === 'bool') { $carrierData[$columnName] = ('true' === $carrier[$columnOptions['apiName']]); }`. `name`, `country`, `currency` and `max_weight` are assigned directly in `carriersMapper` (`:107-110`), and `name` is not a key of `$columnDefinitions` at all; `country`, `currency`, `max_weight` appear there with a `defaultPPValue` only and no `apiName`. |
| 95–97 | A boolean column is true only when the feed sends the literal string `true` [#carriersMapper] | CONFIRMED | `ApiCarrierRepository.php:115` `('true' === $carrier[$columnOptions['apiName']])`. |
| 101 | packetery-carrier reaches outside the repository for one thing only, the carrier catalogue | CONFIRMED | `Downloader::downloadJson` is the only outbound call in `libs/Carrier` and `libs/ApiCarrier`. |
| 103 | calls → packeta-pickup-point-api (sync, HTTP/JSON) | CONFIRMED | `Downloader.php:117-126` synchronous `$this->httpClient->get($url)`, response JSON-decoded at `:131-139`; matches the manifest fragment's `calls` entry. |
| 105–108 | The downloader builds the URL from a constant and the configured API key, sends it through the shared HTTP client wrapper and turns any transport failure into a `DownloadException` [#downloadJson] | CONFIRMED | `Downloader.php:119-125` `sprintf(self::API_URL, $this->configHelper->getApiKey())`, `$this->httpClient->get($url)`, `catch (\Exception $exception) { throw new DownloadException(...) }`. |
| 108 | The URL constant lives in `Downloader` [#API_URL] | CONFIRMED | `Downloader.php:21` `private const API_URL = …`. The document names the constant without quoting its value. |
| 108–110 | After a successful save the run stamps `PACKETERY_LAST_CARRIERS_UPDATE` with the current time [#run] | CONFIRMED | `Downloader.php:88` `ConfigHelper::update('PACKETERY_LAST_CARRIERS_UPDATE', time());` right after `save(...)`. |
| 112–114 | A library, not a service: other modules resolve its classes from the DI container and call them in process | CONFIRMED | `$this->diContainer->get(...)` for `CarrierRepository`/`CarrierVendors`/`ApiCarrierRepository` in `packetery.php:160,1348,1351,1364`, `CarrierAdminForm.php:62-66`, `PacketeryCarrierGridController.php:57`. |
| 113–114 | The admin grid constructs `CarrierAdminForm` for the edit screen [#CarrierAdminForm] | CONFIRMED | `PacketeryCarrierGridController.php:171` `new CarrierAdminForm((int) Tools::getValue('id_carrier'), $this->getModule());` inside `renderView()` (also `:157` in `renderList()` for the warnings). |
| 115–116 | The cron task `DownloadCarriers` wraps `Downloader::run` and reports only its failure text [#execute] | CONFIRMED | `Cron/Tasks/DownloadCarriers.php` `execute()`: `if ($result['class'] === 'danger') { return [$result['text']]; } return [];`. |
| 116–118 | The module class renumbers the pairing row when PrestaShop replaces a carrier [#hookActionCarrierUpdate] | CONFIRMED | `packetery.php:157-163` calls `$carrierRepository->swapId(...)` when `$params['id_carrier'] != $params['carrier']->id`. |
| 118 | …and turns a stored pairing into widget vendor parameters [#getAllowedVendorsForOrder] | CONFIRMED | `packetery.php:1345-1367` ends in `$carrierVendors->getWidgetParameter($packeteryCarrier, $country)`. |
| 119–121 | Order handling reads the pairing when it saves an order [#OrderSaver#getPacketeryCarrierById] | CONFIRMED | `Order/OrderSaver.php:75` and `:166` call `$this->carrierRepository->getPacketeryCarrierById(...)`. |
| 121–123 | Pickup-point validation resolves the carrier of a stored order through `CarrierTools` [#findExternalCarrierId] | CONFIRMED | `PickupPointValidate/PickupPointValidator.php:103` `CarrierTools::findExternalCarrierId($orderData);`. |
| 125–126 | It instantiates `\Carrier` and reads its zones | CONFIRMED | `CarrierTools.php:45-47` `new \Carrier($carrierId); $carrier->getZones();`. |
| 126–127 | It queries the platform `carrier` table in a join [#getPacketeryCarriersList] | CONFIRMED | `CarrierRepository.php:66-73` `FROM `…carrier` `c` LEFT JOIN `…packetery_address_delivery` `pad``. |
| 128 | It writes the platform carrier flags [#updatePresta] | CONFIRMED | `CarrierRepository.php:203-207` `update('carrier', $carrierUpdate, …)`. |
| 128–129 | It builds its admin screens with `\HelperForm` and `\Smarty` | CONFIRMED | `CarrierAdminForm.php:161`, `:334` (`\HelperForm`), `:96`, `:469` (`\Smarty`). |
| 129–131 | The DDL of `packetery_address_delivery` is not in this module; only the DDL of `packetery_carriers` is [#getDropTableSql] | CONFIRMED | `Installer.php:202-210` holds the `packetery_address_delivery` DDL; `ApiCarrierRepository.php:169-194` holds the `packetery_carriers` create/drop SQL. |
| 135–138 | Age verification is decided from a two-element array of Packeta carrier ids commented as Czech and Slovak home delivery; a carrier outside that array and outside the pickup-point case is reported as not supporting it [#CARRIERS_SUPPORTING_AGE_VERIFICATION] | CONFIRMED | `CarrierTools.php:20-27` `/** Czech and Slovak Home Delivery. */ … [106, 131]`; `:104-114` returns true for the non-`is_ad`/non-`is_carrier` case, then the `in_array` check, else false. |
| 139–140 | The countries with internal Packeta pickup points are a fixed four-element list in the admin form [#countriesWithInternalPickupPoints] | CONFIRMED | `CarrierAdminForm.php:50` `private static $countriesWithInternalPickupPoints = ['CZ', 'SK', 'HU', 'RO'];`. |
| 140–142 | The vendor groups are a fixed table of the same four countries with two groups each [#getVendors] | CONFIRMED | `CarrierVendors.php:62-111` `cz`, `sk`, `hu`, `ro`, each with `zpoint` and `zbox`. |
| 142–143 | None of these three lists is derived from the downloaded catalogue | CONFIRMED | All three are literals in `CarrierTools.php:24`, `CarrierAdminForm.php:50` and `CarrierVendors.php:62`; nothing reads `packetery_carriers` to build them. |
| 145–149 | The address-validation radio is offered only for a two-element country constant, and the adjacent comment records that following the paired Packeta carrier's country would be better but was avoided because it would have to be read out of the carrier name [#validationPossible] | CONFIRMED | `CarrierAdminForm.php:237-246`, comment at `:238` "It would be better to follow the country of the carrier, but we don't want to find it out from the name." |
| 151–154 | The per-carrier COD flag is marked obsolete in the help text, which recommends not using it and announces its removal; the input renders only when the flag is already on, so it can be turned off but not back on [#is_cod_1] | CONFIRMED | `CarrierAdminForm.php:293` "…is already obsolete, and we recommend not using it. It will be completely removed soon."; the whole input is guarded by `:284` `(bool) $carrierData['is_cod'] === true`; `is_cod_1` option at `:302-305`. |
| 156–158 | `Downloader::run` swallows every exception and reports it as a message array [#run] | INCORRECT | Only the download/parse step is wrapped: `Downloader.php:56-66` `try { $carriers = $this->fetchAsArray(); } catch (\Exception $e) { … }`. `$this->apiCarrierRepository->save(...)` at `:87` is outside the `try` and its `DatabaseException` propagates — `run()`'s own docblock (`:52`) declares `@throws DatabaseException`. |
| 157–158 | A caller cannot distinguish a network failure from an invalid response other than by the message text | CONFIRMED | All three failure paths (`:59`, `:68`, `:78`) return the same shape with `'class' => 'danger'`, differing only in `'text'`. |
| 158–161 | The save is not transactional: rows are inserted or updated one by one and the deletion flag is applied afterwards, leaving the catalogue half-updated on an interrupted run [#save] | CONFIRMED | `ApiCarrierRepository.php:156-166` per-row `update()`/`insert()` loop, then `:166` `setOthersAsDeleted(...)`; no transaction is opened. |

### manifest.part.packetery-carrier.yaml

| entry | class | evidence |
|---|---|---|
| `service: prestashop` | CONFIRMED | Matches the document front matter `repo: prestashop`. |
| `source_commit: b85243bbeb5f9f91c0bde739f5195b2285b867f9` | CONFIRMED | Matches the document front matter and `git rev-parse HEAD`. |
| assumption: generated on branch `docs/packetery` at that commit with a clean working tree | CONFIRMED | Current branch is `docs/packetery`, HEAD is that commit; `git status` shows no modified tracked files (only the untracked ai-docs outputs). |
| assumption: counts come from `ai-docs/ast/map.json`, `index.php` guard stubs excluded | CONFIRMED | `map.json` lists 6 types for `libs/Carrier` + `libs/ApiCarrier` with 13/14/3/12/4/7 methods (= 53, the public ones only) and contains no entry for the `index.php` stubs. |
| assumption: `packetery_address_delivery` under `owns_data` because `CarrierRepository` is the only writer of its columns, although its DDL is in `packetery/libs/Module/Installer.php` | INCORRECT | The DDL half is right (`Installer.php:203`), but "the only writer" is not: the upgrade scripts write the same columns — `upgrade/upgrade-2.1.5.php:56` `$dbTools->insert('packetery_address_delivery', $carriersToPair);` and `upgrade/upgrade-3.0.0.php:75,82,86` `UPDATE `…packetery_address_delivery` SET `address_validation` = "none" …` / `SET `id_branch` = "…"`. They are one-off migrations, but they are writers. |
| assumption: `packetery_carriers` under `reads_external_data`, every row but the two internal ones is a feed copy | CONFIRMED | `ApiCarrierRepository::carriersMapper` (feed rows) + `addNonApiCarriers` (`ZPOINT`, `PP_ALL`). |
| assumption: the PrestaShop platform is prose-only and gets no manifest entry | CONFIRMED | The fragment contains no entry for the `carrier` table, `ObjectModel`, `HelperForm` or hooks. |
| `calls: packeta-pickup-point-api` (rest, evidence `Downloader.php#downloadJson`) | CONFIRMED | `Downloader.php:117-126`; the API key comes from `$this->configHelper->getApiKey()`, matching "keyed by the configured API key". |
| `owns_data: packetery_address_delivery` (mysql/prestashop, evidence `CarrierRepository.php#setPacketeryCarrier`) | CONFIRMED | `CarrierRepository.php:239-295` writes `pickup_point_type`, `address_validation`, `allowed_vendors`, `is_cod`, `name_branch`, `currency_branch`, and deletes the row at `:262` when the pairing is cleared. |
| `reads_external_data: packetery_carriers` (owner packeta-pickup-point-api, evidence `ApiCarrierRepository.php#save`) | CONFIRMED | `ApiCarrierRepository.php:147-167`. |
| unknown: which carriers the two ids in `CARRIERS_SUPPORTING_AGE_VERIFICATION` denote | CONFIRMED | Premise holds: `CarrierTools.php:24-27` has exactly two ids and nothing in the repo resolves them; `blocks` names the existing `CarrierTools::orderSupportsAgeVerification`. |
| unknown: whether the four countries / two vendor groups in `CarrierVendors::getVendors` are the complete offering | CONFIRMED | Premise holds: `CarrierVendors.php:62-111`. |
| unknown: the full schema of the carrier feed; the code validates fourteen required keys | CONFIRMED | Premise holds: `Downloader.php:152-167` checks 14 keys; no response contract is defined anywhere in the repository. |

CONFIRMED 70 · INCORRECT 4 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

**Generator follow-up (2026-09-22):** every INCORRECT row above was fixed in `ai-docs/reference/packetery-carrier.md` (and in its manifest fragment where the row pointed there). `confidence` was set to `reviewed`; `verified` needs another pass by someone who did not write or fix the document.
